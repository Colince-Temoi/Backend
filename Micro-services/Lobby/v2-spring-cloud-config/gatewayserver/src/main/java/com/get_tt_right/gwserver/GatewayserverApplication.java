package com.get_tt_right.gwserver;

import io.github.resilience4j.circuitbreaker.CircuitBreakerConfig;
import io.github.resilience4j.timelimiter.TimeLimiterConfig;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
import org.springframework.cloud.circuitbreaker.resilience4j.ReactiveResilience4JCircuitBreakerFactory;
import org.springframework.cloud.circuitbreaker.resilience4j.Resilience4JConfigBuilder;
import org.springframework.cloud.client.circuitbreaker.Customizer;
import org.springframework.cloud.gateway.filter.ratelimit.KeyResolver;
import org.springframework.cloud.gateway.filter.ratelimit.RedisRateLimiter;
import org.springframework.cloud.gateway.route.RouteLocator;
import org.springframework.cloud.gateway.route.builder.RouteLocatorBuilder;
import org.springframework.context.annotation.Bean;
import org.springframework.http.HttpMethod;
import reactor.core.publisher.Mono;

import java.time.Duration;
import java.time.LocalDateTime;


/** Update as of 17/12/2025
 * Install getttrightbank ms's into the k8s cluster
 * ------------------------------------------------------
 * Finally, we are going to deploy our ms's into the k8s cluster with the help of helm. Previously, in our discussions we were promised that whenever we are using helm, we can install all our ms's with a single command. The same we are going to demo/visualize inside this discussion. In your terminal, navigate to the environments' folder. Inside this we have 3 charts related to dev, prod and qa environments. First we need to decide under which environment we need to deploy our ms's. Lets assume we want to deploy our ms's into production. For this, you need to run a command which is helm install getttrightbank prod-env.  In this command, getttrightbank is the release name and prod-env is the chart name that we want to consider as we are trying to deploy our ms's using the production profile. This command will install all the prod-env ms's into the k8s cluster. Like this we are trying to deploy our ms's using the production profile. As soon as you execute this command, you will get an output that the deployment is completed/initiated. On visiting the k8s dashboard, you can navigate to the pods and you will notice that all of our ms's will try to start at the same time and because of this you will notice that they are going to fail multiple times because configserver will not have been readily available, same as eurekaserver. For that reason the pods will fail to start and whenever this kind of scenario occurs inside my K8s, my k8s is smart enough to do multiple restarts till the pod is successfully started or it reaches a maximum number of attempts. And whenever it is time to restart a particular pod, k8s will make sure that it is taking some time between multiple restarts . First time it may take 2 seconds, Next time it may take 4 or 5 seconds. Like this, it is going it is going to delay the next restart based upon exponential delay. But what we can do here right now is, we can go to the logs of each of the services and make sure that all of them started successfully. So the first service we need to make sure to start is configserver - For the same we can click on this configserver pod, you should see an option to View Logs. To automatically refresh the logs on that console, on the 3 dots just left of the console you can select the Auto-refresh(Every 5 s.) option. This will automatically refresh the logs into your k8s dashboard under that specific pod logs you are viewing after every 5 seconds. Somewhere in the logs you should see a statement saying that the configserver successfully started. This may take some good time inside our local system because in our scenario we just have a 16GB RAM and out of this 16 GB RAM, we only assigned 12 GB RAM to the docker desktop and K8S and inside that 12 GB, we are trying to set up so many components and that's why your installation is going to be super slow. You can wait for 5 to 10 minutes and in between you can keep checking the logs of all the services and make sure that all of them are started. When you see log statement i.e., Started Service abc in xyz seconds (Process running running for xyx) i.e., 'Started configserver in 120.534 seconds (Process running for 203.54'. This should confirm that my configserver started successfully. You can go back to the pods and open the Eurekaserver pod. Because EurekaServer has a dependency on configserver, under the Resources information section, you can clearly see under the field/column 'Restarts', it tried to start multiple times i.e., 3 times in my case. All this is because configserver was not yet available and hence in the initial attempts, the eureka server couldn't start successfully hence you can see 3 restart attempts happened by the K8s cluster. You can also check the logs and you will see finally after some time you will visualize that our eurekaserver also started successfully by locating a log statement like, 'Started EurekaserverApplication in 62.13 seconds (Process running ofr 97.284)'. My local system is super slow now, haha, the fan inside my local system is even rotating at a full speed. Going back to the pods, same drill, I can also try to check the logs of the individual ms's i.e., Accounts ms. Accounts ms have 6 restart attempts. Here if you check the logs, since accounts ms started successfully and it connected with my eurekaserver as well as the datasource you will see, 'Started AccountsApplication in ...'. Same drill, you can go ahead and conform with cards, loans, as well as message ms's. As a next step, you can check with gatewayserver. Next, you should check messages ms - this one should have started successfully because it has no dependency on configserver, eurekaserver and that's why you will see its number of restarts is way less. In my case 1. If you check its logs, you will see that it started successfully, and it also connected to the kafka broker that we provided as can be see from a log statement i.e., bootstrap.servers = [kafka-controller-0.kafka-controller-headless.default.svc.cluster.local:9092]. With this, all our ms's started successfully, as a next step, we will try test a few scenarios as discussed below.
 * First, in our postman under the gateway_security folder we will test GET APIs i.e, Accounts_GET_PermitAll. You should get a successful response. The properties inside the response are related to the prod profile as can be seen. This conforms that our application started with the production profile. Next, Card_GET_PermitAll. Here we are trying to get the Java Version an API which is available inside the cards ms. Hurrey! got a successful output. Next, Loans_GET_PermitAll, here I am trying to invoke the build info api which is under loans which is also working fine. As a next step, I will try to create a new account by invoking the request i.e., Accounts_POST_ClientCredentials. For the same, first I need to get an access token by mentioning the correct secret value. Apart from the secret value, we should make sure we have mentioned the correct KeyCloak url port which is 80. Now, get an access token and use the same to fire the request - this should be able to create a new account behind the scenes. As a next step, invoke the cards API request i.e., CARDS_POST_ClientCredentials, you should get a successful response. Same drill, invoke the LOANS_POST_ClientCredentials request. This will send a request to the loans API to create a new loan based upon a given mobile number. You should be able to get a successful response. As final step, we can try to invoke this fetch customer details API i.e., Accounts_GET_PermitAll for the given mobile number. With that, we should get all the loans cards and accounts information. In situations where you find out tha some dat is coming as null i.,e lets assume loans information came null, means that maybe a timeout issues may have occurred inside my local system - this we already discussed in details earlier. The solution is to retry making the request again and you should be able to get all the data happily.
 * With this the complete set-up of our m's inside k8s cluster is working perfectly even with the help of helm charts. And this time, we also set up other required components like kafka, grafana, prometheus, etc. As a next step, lets try to validate a few things inside the grafana, for the same we need to make sure that we have exposed the grafana network at the port 3000 by running the kubectl port-forward svc/grafana 3000:3000 command inside our terminal. Now, in the grafana page, you can navigate to the explore option and here 1st we will look for the logs information - for the same, select Loki from the dropdown.Next, select one of the label filters i.e., container and post that I will select the container name as 'gatewayserver' under the select value dropdown. This will give you all the logs present inside my gatewayserver. Now, if I try to click on any of the logs, I should be able to see/have a link to the Tempo for a particular TraceID. If you try to click on it, you should be able to get the complete distributed tracing details which is a confirmation to you that Loki and Tempo are working perfectly and that the integration between Loki and Tempo is working fine. As a final step, we can confirm Prometheus as well. For the same you can go to 'explore' and select Prometheus. Under the 'metric' input dropdown, you will see lot many metrics but let me look/search for and select the metric with the value, 'up'. Post that, under the Label filter input, I will select the label 'container' or 'instance' and run that respective query. This should show you the graph for the last 1 hour. I changed that to the last 15 minutes and finally changed that graph style to display as stacked lines. Like this you should be able to see a very beautiful graph explaining about my uptime metric for all the running containers/instances inside my K8S. This confirms that my prometheus integration with grafana is working perfectly. So far, you should be super happy with the progress made as we are able to successfully set up our ms's alongside supporting components inside k8s cluster with the help of helm charts. Hurreey! You are one of the few developers who know about all this process. You should be proud of yourself as you have learned so much hand-on experience by taking a k8s cluster and setting up all the ms's + supporting components inside it with the help of helm charts. In the coming sessions, we will also discuss on how to set up all these in a proper cloud environment like Google.
 *
 * Demo of Helm upgrade command
 * ------------------------------
 * Sometimes once you install al your custom ms's with the help of helm chart you may want to roll out new changes, or you may want to update your ms's deployments/installations with new values. For example, you could be wanting to increase the replicas from 1 to 2 or 2 to 5, or 3 to n based upon your requirements, or you may even want to deploy a new docker image into your k8s cluster. Previously, we visualized on how to roll out and roll back changes with the help of with the help of k8s and kubectl command in details. Now, we will be visualizing on how to do the same with the help of helm commands as well. For the same, inside my helm folder location, navigate to the environments' folder then navigate to the prod env folder because right now we installed the chart of production environment only. Here, I am going to make a small change and this is inside the getttrightbank-services/gatewayserver. So go to the gatewayserver and open the values.yaml file. As of now, the tag that we have deployed for gatewayserver is V5, I will change this to v4 so that no authentication and authorization information will be required by my gatewayserver. I should be able to access any API with the help of my gatewayserver. Save that change and post that you need to recompile your production environment helm chart because my prod-env helm chart has a dependency on the gatewayserver helm chart. So, in the folder prod-env, run the command helm dependencies build. Once the build is completed, we can run a command which is 'helm upgrade <release-name-that you had provided> <then-provide-the-helm-chart-details>' i.e., helm upgrade getttrightbank prod-env'. Make sure to run this command from the parent folder where your helm chart is - in my case 'environment'. So, if I try to execute this command, my helm is smart enough to identify what are the changes and deploy the same changes into the k8s cluster and as can be seen from the output, it is going to give the revision number as 2. Now, if you go to the K8s dashboard  and visit the pod which has a name i.e., gatewyaserver. If you open the logs you will see that it is starting. You can wait for few more minutes so that my gatewayserver deployment will complete. After sometime it should start successfully. To validate if my changes are really deployed or not I can try to invoke one of the POST APIs without any security. So, inside our postman, we have an API with the name 'Accounts_POST_NoAuth' If I invoke this API, I should not get a 401 error - that's the expectation. Now, I faced a challange with the gatewayserver starting due to the wrong open telemetry-javaagent as seen below in its logs:
 Picked up JAVA_TOOL_OPTIONS: -javaagent:/app/libs/opentelemetry-javaagent-2.17.0.jar
 Error opening zip file or JAR manifest missing : /app/libs/opentelemetry-javaagent-2.17.0.jar
 Error occurred during initialization of VM
 agent library failed to init: instrument
 *
 * This implies that what is javaagent that is downloaded into the location /app/libs/ is not the one we are specifying in our global helm configurations. In our global configurations we are specifying 2.17.0 but the gatewayserver image with tag V4 has the one which is 2.11.0. So a temporary solution of not disturbing the other ms's which are using the golabl configuration that matches what those ms's are downloading into the location app/libs, I decided to just disable the otel_enabled configuration at gatewayserver helm chart values.yaml to 'false'. So a next step, due to this change, we need to compile the charts that are dependent to this gatewayserver chart. Since we are working with prod-env chart and it has a dependency on gatewayserver chart where we have disabled otel, we have to navigate to this prod-env chart and run the helm dependencies build command. After that, navigate to the parent folder where this prod-env chart is present and run the helm upgrade gettrightbank prod-env command. Like this k8s is smart enought to pick up any changes we made to the gatewayserver, and it will try to redeploy it as a new revision.  Hurreey! after a few seconds, our gatewayserver started happily. Now if we invoke the endpoint 'Accounts_POST_NoAuth' this time around we should not get a 401 error just as expected even though we have not sent any authentication information in the request. This is a confirmation that the helm upgrade command is working fine as expected. This way we can make any number of changes inside our helm charts, do a build/compilation in the dependent charts and finally run a hel upgrade command and your helm is going to take care of deploying the changes related to only the new changes that you have done. Similarly, if you want, you can change the replica count from 1 to n, etc. So its up to you on what change you want to do. I am not doing any change related to replica count because inside my local system, my laptop is already cursing me a lot of saying that, 'How much weight I need to bear' haha! It is even eagerly waiting for me to uninstall all these components from k8s cluster so that it will get free from all the burden it is right now carrying. Jokes a part, so, as of now we have visualized on how to roll out new changes with the help of helm upgrade command, very similarly, we need to know how to roll back the changes if needed with the help of helm command. This we will explore next.
 *
 * Demo of helm history and rollback commands
 * --------------------------------------------
 * In this session, we will visualize on how to roll back to a desired previous working version with the help of helm. The beauty of helm roll back is, it is going to roll back your entire k8s cluster components to the previous desired state whereas with kubectl, if you need to roll back, means you have to do so component by component. As of now, I made a change only inside the gatewayserver, what if you have made changes inside multiple ms's? Regardless of how many changes you have done inside various ms's, with the help of helm the rollback command is also going to be a single command. To get started around this rollback command first, you should be aware of the helm history command. This command's output will show you the history of upgrades or installations or deployments that happened for a particular helm chart. The full command is, helm history <release-name-we-have-given-for-our-ms's> i.e., helm history getttrightbank. If I try to run this command I got an output like below:
 PS C:\Users\ctemoi\Desktop\K8s\helm\environments> helm history getttrightbank
 REVISION        UPDATED                         STATUS          CHART           APP VERSION     DESCRIPTION
 1               Fri Jan 23 12:52:16 2026        superseded      prod-env-0.1.0  1.0.0           Install complete
 2               Sun Jan 25 10:31:30 2026        superseded      prod-env-0.1.0  1.0.0           Upgrade complete
 3               Mon Jan 26 06:11:50 2026        deployed        prod-env-0.1.0  1.0.0           Upgrade complete
 *
 * This is showing me all the installation/deployments/upgrades that happened for the release getttrightbank. First, under the Revision 1 installation was completed as can be seen from the Description column/field. Post that, in revision 2 we had deployed a gatewayserver image tag name v4, but it could not initialize due to non-matching otel javaagent library in the app/libs folder as discussed earlier in details. In revision 3 we made one more temporary change by disabling otel in the gatewayserver and did one more upgrade as can be seen from the Description field/column in the output. Under the Status column/field in the output, right now Revision 3 has a status as deployed - this simply means that this is the revision right now deployed inside my k8s cluster. Now, think like we want to roll back to the previous working version which is revision 1 where my gatewayserver is going to be secured properly. For this, the command is going to be super simple. The command that we need to run to roll back to a previous revision is helm rollback <release-name> <What is the revision number to which you want to roll back>. In my case, helm rollback getttrightbank 1. With just that, using a single command the rollback was a success. I can confirm that by going into the k8s dashboard >> pods >> open the new pod that is right now getting created i.e., gatewayserver. If you scroll to the section 'Events' you should be able to see a new event where you will see a new docker image tagged V5 is being utilized this time around to spin this gatewayserver. So yea, that is the docker image right now being deployed into my k8s cluster. If you visit the logs, after some few seconds my gatewayserver should start happily! You can then proceed to validating the changes inside postman. This time if I try to invoke the api Accounts_POST_NoAuth, I should get a 401 error. Hurrey! the rollback is also completed with a single helm command. Now if I try to run the helm history getttrightbank command, this time you should be able to see an output like below:
 PS C:\Users\ctemoi\Desktop\K8s\helm\environments> helm history getttrightbank
 REVISION        UPDATED                         STATUS          CHART           APP VERSION     DESCRIPTION
 1               Fri Jan 23 12:52:16 2026        superseded      prod-env-0.1.0  1.0.0           Install complete
 2               Sun Jan 25 10:31:30 2026        superseded      prod-env-0.1.0  1.0.0           Upgrade complete
 3               Mon Jan 26 06:11:50 2026        superseded      prod-env-0.1.0  1.0.0           Upgrade complete
 4               Mon Jan 26 06:53:10 2026        deployed        prod-env-0.1.0  1.0.0           Rollback to 1
 *
 * This time you should be able to see a revision number 4 with the description Rollback to 1. This is a very crisp clear information to a person whoever tries to roll back in future or if you want to understand the history of the deployments/installations, upgrades and even rollbacks that happened for a particular release which is getttrightbank in our scenario. This should be crisp clear. Now, back to our values.yaml where we did the changes, revert back the  image tag to v5 and otel_enabled to true as we need to check in these files into the GitHub repo.
 *
 * Demo of helm uninstall command
 * ----------------------------------
 * Just like how we can install an entire ms set up with a single helm command, very similarly we can also uninstall all our ms's with a single helm command. Using helm uninstall command we can uninstall all our ms's  from our k8s cluster. This command is going to be super helpful whenever you want to shut down your entire k8s cluster. This is going to be helpful especially in the lower environments. Don't run it in production environment hahaha! Before I try to run the helm uninstall command first let visualize what are all the installations that we have done with the help of helm. For the same run the command helm ls. This is going to show you all the releases that we have installed with the help of helm as shown in the output below:
 PS C:\Users\ctemoi\Desktop\K8s\helm\environments> helm ls
 NAME            NAMESPACE       REVISION        UPDATED                                 STATUS          CHART                   APP VERSION
 alloy           default         1               2026-01-23 12:38:51.1724553 +0300 EAT   deployed        grafana-alloy-1.0.0     v1.5.1
 getttrightbank  default         4               2026-01-26 06:53:10.9873364 +0300 EAT   deployed        prod-env-0.1.0          1.0.0
 grafana         default         1               2026-01-23 12:50:13.3280961 +0300 EAT   deployed        grafana-1.0.0           11.4.0
 kafka           default         1               2025-12-09 08:21:15.4633353 +0300 EAT   deployed        kafka-1.0.0             4.1.0
 keycloak        default         1               2025-12-05 11:19:56.2538402 +0300 EAT   deployed        keycloak-1.0.0          26.4.0
 loki            default         1               2026-01-23 12:36:57.3696632 +0300 EAT   deployed        grafana-loki-1.0.0      3.1.2
 prometheus      default         1               2025-12-11 08:34:09.150339 +0300 EAT    deployed        kube-prometheus-1.0.0   v3.1.0
 tempo           default         1               2026-01-23 12:48:33.9944677 +0300 EAT   deployed        grafana-tempo-1.0.0     2.6.1
 *
 * First, we will try to uninstall getttrightbank and with that all the ms's that we have deployed as part of this release will get deleted. So the command here is helm uninstall <name of your helm release> i.e., helm uninstall getttrightbank. As soon as I fire this command the uninstallation process will get started behind the scenes. Very similarly, uninstall the remaining releases i.e., grafana, tempo, alloy, loki, prometheus, Kafka, and finally keycloak in that order. With this, you should have uninstalled all the releases gracefully. Now if you try to run the command helm ls as can be visualized, I got an empty result. We can also validate this inside the k8s dashboard. If you click on workloads, you will see a message saying that there is nothing to display here. Which simply means that all my pods, deployments, services, config Maps, secrets, .., everything got deleted except Persistent Volume Claims - please make sure you are deleting all these persistent volume claims otherwise next time when you try to install Keycloak, Kafka, ..., and other helm charts you may run into some issues. Not sure if this is a bug on the helm or they are doing this intentionally but my instructor said that he faced so many issues due to these PVC's not being deleted whenever we try to do an installation of helm charts To delete these PVC's, on the three dots on each record, you should be able to see a delete link. With this my k8s cluster is going to be empty and my laptop is going to have a sigh of relief because, I reduced all the load that is bearing from the helm installation that we had done.
 * With all we have been discussing, it is assumed that you are super clear as far as helm is concerned - its purpose, features it provides, etc. You maybe asking yourself a question like, is helm the only option that you have? No, of course there are other alternative products also. One famous helm competitor is kustomize.io. This is also going to solve all the problems that helm is solving. It also has a different approach and lot many people use it because it is very easy to learn - it has a very less learning curve compared to helm. But the reason we chose helm as our stepping stone is that helm has a very good ecosystem. Like with helm we can solve our own deployment challenges and apart from that, there are very many bitnami charts or other community charts available for us to install many components/products inside our k8s cluster whereas this community is missing for the kustomize. That's why many projects who started with the kustomize, later on they will end up realizing the benefits of the helm community and they will end up using both kustomize and helm charts. My instructors recommendation is to go with the helm charts because it is a more complex product and has many capabilities compared to the kustomize. That was my instructors personal opinion but for some reason if you have a feeling that kustomize is good enough for your projects then feel free to use the same. You can read more details about this customize in the official documentation - https://kustomize.io/.
 *
 * Quick revision of important helm commands
 * ------------------------------------------
 * # Check slide for this.
 *
 * Next, I also tried to deploy the other environments along with the supporting components as discussed below
 * dev-env deployment
 * ----------------------
 * Deployment order => keycloak, kafka, prometheus, loki,alloy, tempo, grafana, and finally getttrightbank. Wait 1-2 minutes between each deployment. To undeploy - do so in the reverse order.
 *  qa-env deployment
 *  ------------------
 *  Same as what we have done in dev-env deployment above.
 * * * */
@SpringBootApplication
public class GatewayserverApplication {

	public static void main(String[] args) {
		SpringApplication.run(GatewayserverApplication.class, args);
	}

	/* This method is going to create a bean of type RouteLocator and return it.
	* Inside this RouteLocator only, we are going to send all our custom routing related configurations based on our requirements.
	**/
	@Bean
	public RouteLocator eazyBankRouteConfig(RouteLocatorBuilder routeLocatorBuilder) {
		return routeLocatorBuilder.routes()
				.route(p -> p.path("/eazybank/accounts/**")
						.filters(f -> f.rewritePath("/eazybank/accounts/(?<segment>.*)", "/${segment}")
								.addResponseHeader("X-Response-Time", LocalDateTime.now().toString())
								.circuitBreaker(config -> config.setName("accountsCircuitBreaker")
										.setFallbackUri("forward:/contactSupport")))
						.uri("lb://ACCOUNTS"))

				.route(p -> p.path("/eazybank/loans/**")
						.filters(f -> f.rewritePath("/eazybank/loans/(?<segment>.*)", "/${segment}")
								.addResponseHeader("X-Response-Time",LocalDateTime.now().toString())
								.retry(retryConfig -> retryConfig.setRetries(3)
										.setMethods(HttpMethod.GET)
										.setBackoff(Duration.ofMillis(100),Duration.ofMillis(1000),2,true))
						)
						.uri("lb://LOANS"))
				.route(p -> p.path("/eazybank/cards/**")
						.filters(f -> f.rewritePath("/eazybank/cards/(?<segment>.*)", "/${segment}")
								.addResponseHeader("X-Response-Time",LocalDateTime.now().toString())
								.requestRateLimiter(config -> config.setRateLimiter(redisRateLimiter())
										.setKeyResolver(userKeyResolver())))
						.uri("lb://CARDS")).build();
	}

	@Bean
	public Customizer<ReactiveResilience4JCircuitBreakerFactory> defaultCustomizer() {
		return factory -> factory.configureDefault(id -> new Resilience4JConfigBuilder(id)
				.circuitBreakerConfig(CircuitBreakerConfig.ofDefaults())
				.timeLimiterConfig(TimeLimiterConfig.custom().timeoutDuration(Duration.ofSeconds(4)).build()).build());
	}

	@Bean
	public RedisRateLimiter redisRateLimiter() {
		return new RedisRateLimiter(1, 1, 1);
	}

	@Bean
	KeyResolver userKeyResolver() {
		return exchange -> Mono.justOrEmpty(exchange.getRequest().getHeaders().getFirst("user"))
				.defaultIfEmpty("anonymous");
	}


}
