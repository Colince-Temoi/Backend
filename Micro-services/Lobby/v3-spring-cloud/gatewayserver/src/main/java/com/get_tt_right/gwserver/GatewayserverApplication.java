package com.get_tt_right.gwserver;

import io.github.resilience4j.circuitbreaker.CircuitBreakerConfig;
import io.github.resilience4j.timelimiter.TimeLimiterConfig;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
import org.springframework.cloud.circuitbreaker.resilience4j.ReactiveResilience4JCircuitBreakerFactory;
import org.springframework.cloud.circuitbreaker.resilience4j.Resilience4JConfigBuilder;
import org.springframework.cloud.client.circuitbreaker.Customizer;
import org.springframework.cloud.client.discovery.EnableDiscoveryClient;
import org.springframework.cloud.gateway.filter.ratelimit.KeyResolver;
import org.springframework.cloud.gateway.filter.ratelimit.RedisRateLimiter;
import org.springframework.cloud.gateway.route.RouteLocator;
import org.springframework.cloud.gateway.route.builder.RouteLocatorBuilder;
import org.springframework.context.annotation.Bean;
import org.springframework.http.HttpMethod;
import reactor.core.publisher.Mono;

import java.time.Duration;
import java.time.LocalDateTime;


/** Update as of 3/2/2026
 * Making K8S Discovery Client Changes in Ms's
 * ---------------------------------------------
 * As a next step, we need to make some code changes inside our ms's to remove the Eureka related changes and on top of that we should also make some changes inside our ms's to leverage the discovery server exposed by the k8s. To get started around those changes, I am going to copy all the ms's code that we have inside the v2-spring-cloud-config workspace as It's where the latest and greatest ms's code changes are present. So, from this workspace select, accounst, cards, loans, configserver, gatewayserver and message ms's. We don't have to copy the eurekaserver because we are rtying to get rid of it. We will also not copy the docker-compose folder as we will be deploying all our ms's into k8s cluster and docker compose files is of no use right now. The folder copied paste them into our new workspace which is v3-spring-cloud.
 * We will try to make all the required changes inside our ms's to get rid of eureka server and use the Discovery server available inside the k8s cluster. For the same, first we will get started with the accounts ms. Inside it's pom.xml, we will update the docker image tag name to next version i.e., v12. Post that locate the eureka dependency i.e., spring-cloud-starter-netflix-eureka-client. Instead of this dependency, we are going to use a dependency with the name spring-cloud-starter-kubernetes-discoveryclient. Do a maven refresh, the do a build. Once the build is completed you may end up getting some errors inside some of your Java files. For example, my instructor ran into a CE on the import i.e., import org.apache.http.HttpStatus; inside the CustomerController. Since whenever we deleted the eureka server related dependency, it is internally going to delete the interrelated dependencies and that's why he ran into that CE. As a solution he removed that and internally Spring boot will try to use one of the https available in some other package in his case he has an import i.e., org.apache.hc.core5.http.HttpStatus. Build again and make sure you have no compilation errors. In my case I didn't run into any CE's.
 * Now, inside the Spring boot main class of my accounts ms i.e., AccountsApplication. Here, we need to mention a class level annotation i.e., @EnableDiscoveryClient. Now, inside the application.yaml file we also need to make some changes as we have mentioned many configurations related to eureka. Delete all those eureka related configurations. Just search eureka and you should see all the properties related to eureka, select all of them and delete them. Post that, we need to add a property/configuration related to the Discovery Server of k8s. This, we need to mention under the Spring.cloud i.e., Spring.cloud.kubernetes.dicovery.all-namespaces as true. The purpose of this property is that, as of now we are deploying all our ms's into the default namespace of k8s but sometimes some projects they may deploy different different applications in different different namespaces. In such scenarios, to make sure that our application is able to discover the services present inside other namespaces as well we need to make sure we are mentioning this all-namespaces property definition as true. Now, as a next step, lets go to the location where we are using feign client to connect with the other ms's. So, inside my accounts ms under the service package, open the client folder. Here we have the interfaces with the names CardsFeignClient and LoansFeignClient.
 * In the CardsFeignClient, whatever configurations we have defined as part of the class level annotation i.e., @FeignClient(name = "cards",fallback = CardsFallback.class), they will work perfectly with the Eureka server. Reason: Inside the Eureka server, all my applications are going to be registered with the application name as 'cards'. Using this same application name, my accounts ms can fetch the details of all the instances of cards ms from the eureka server. We also provided the eureka server connection details inside the application.yaml as we had earlier on visualized. With this there was a clear integration between your accounts ms and eureka server, And using the same integration it is going to use this same application name i.e., 'cards' which we have defined here in the @FeignClient annotation to get the details of cards ms instances. But in the scenario of Discovery server we are not providing any integration details between our accounts ms and Discovery Server as no where I have mentioned discovery server url details - that's why my feign clients cannot really understand what is the service name or the host name or the dns name or even the port number where it has to forward the requests. To resolve this drawback we need to provide details with the parameter/input which is url in the @FeignClient annotation. So apart from the name parameter i.e., 'cards', we also need to mention the url i.e., 'http://cards:9000' at which my cards ms is available. It is important to give keen attention to the service name we gave to cards ms service - which is 'cards' as can be seen from the url as this is what we configured in our respective cards ms helm chart of you can crisply recall. If you had given some other name then, make sure to specify that in your url parameter as the host/IP/service name. Like this, using this service name as can be seen in the url, the communication between accounts and cards ms is going to happen within the k8s cluster. And where are all my cards ms containers going to be available? At the port 9000 as we also configured in the respective helm chart. Yea, that's the only additional detail/parameter/input that we need to provide to the feign client. And here, the feign client is not going to perform any load balancing, it is simply going to forward the request to the url mentioned inside the k8s cluster. If the same is happening outside the k8s cluster then it is not going to work because outside your k8s cluster, there is no hostname or DNS name with the name cards.
 * That's why whenever you are trying to do some local testing without k8s cluster try to give your localhost url in this url parameter or @FeignClient annotation. Very similarly, mention this url parameter of @FeignClient annotation inside the LoansFeignClient as well i.e., url = "http://loans:8090". As can be seen, here the service name is going to be 'loans' and the port is going to be '8090'. So far, you should be crisp clear with all these changes we have done in the accounts ms as we should be good from accounts ms perspective. As a next step, I will go to the cards ms and the first change will be inside the pom.xml - in the place of eureka dependency add a new dependency which is spring-cloud-starter-kubernetes-discoveryclient. After this, update the tag name from v9 to v10. and once this is complete load the maven changes and then do a build to verify that there are no CE's. The next change inside this cards ms we will don in its main class - here we need to mention a class level annotation which is @EnableDiscoveryClient. The next change will be inside the application.yaml file of cards ms, search for eureka related configurations and delete all of them. Post that, search for 'cloud' - okay we have no cloud related configurations, and so what we can do is , just after the parent element 'spring', create a new child element i.e., 'cloud' and under this cloud, we are going to define a property related to the k8s discovery server same like we discussed in account ms i.e., spring.cloud.kubernetes.discovery.all-namespaces=true.. With this, we should be good as far as cards ms changes are concerned, we don't have to make any feign client related changes because my cards ms does not have any feign client related business logic.  In the loans ms also perform similar changes as to what we have done with cards ms. As a next step, lets go to the configserver and message ms's - here we are no where connecting/integrating to the eurekaserver and therefore there is no need of any changes. Here if you can see/visualize, we have done all the required changes in all the ms's except the gatewayserver project. So, open the pom.xml of gatewayserver  search for eureka and in its place add the dependency which is spring-cloud-starter-kubernetes-discoveryclient and update the tag name from v5 to v6 - load the maven changes and build the project to make sure there are no CE's. Once the build is completed, you can go to the gatewayserver main class and mention the class level annotation which is @EnableDiscoveryClient.
 * After making these changes, inside the same class if you try to scroll down, as of now you can see that the routing configurations that we have done here is going to have uri configurations i.e.,   uri("lb://ACCOUNTS"), uri("lb://LOANS") and uri("lb://CARDS"). What does this 'lb' here indicate - Load Balancer. With this, we are telling to the gatewayserver that, please leverage the Spring Cloud Load Balancer and do the load balancing at the gatewayserver itself. But we don't want that to happen because we are no more using the eureka server. So, to communicate to the gatewayserver not to perform any client side load balancing, we need to update this uri configuarion inputs with the actual url details of your individual ms's i.e.,   uri("http://accounts:8080"), uri("http://loans:8090") and uri("http://cards:9000"). The beauty here also is that we are not hardcoding the hostname/domain name. As long as you start your respective services with the service names i.e., 'accounts', 'cards', and 'loans' then your gatewayserver and feign client integration should work without any issues. With the changes we have made, we should be good from this GatewayServerApplication main class. As a next step, in the application.yaml file we need to delete the properties related to the eureka server i.e., dicovery.locator related configurationes. i.e., dicovery.locator.enabled and dicovery.locator.lowerCaseServiceId. Post deleting these eureka related specific properties, just at that same location as the gateway property, define a new property i.e., spring.cloud.kubernetes.discovery.enabled as true and spring.cloud.kubernetes.discovery.all-namespaces as true. With spring.cloud.kubernetes.discovery.enabled as true , we are telling to the gatewayserver that please leverage the discovery server available inside the cluster to forward the requests to other ms's like accounts, cards and loans. So, that discover.enabled property is the extra property that we need to define inside our gatewayserver because our gatewayserver is an edge server, and it is going to be responsible for handling all the incoming traffic. If you try to define only that property and try to start your gatewayserver, you are going to get an error according to my instructor. To avoid that error, you need to define one more property under the spring.cloud which is discovery.client.health-indicator.enabled as false. By default, there is a health indicator bean which is going to be created internally and whenever this is happening, behind the scenes my library which we have added related to the discovery client of kubernetes i.e., spring-cloud-starter-kubernetes-discoveryclient. It is going to create a problem becuase it is not going to create a bean that is required for this library. My instructor looked into the official github issues for this project/library and there is an issue raised inside the GitHub repo and the Spring Cloud Kubernetes team also confirmed and for now to overcome that problem we should define the property which is discovery.client.health-indicator.enabled as false. In the future, when this issue is resolved then you can safely remove that property. You can always be confirming with your instructors repo, if you notice that that property is removed from the application.yaml file of GatewayServer then that is an indication that that issue is resolved. Otherwise we should continue to define that property.
 * So, those are the required changes inside my gatewayserver application. As a next step, we are going to generate the docker images for the ms's where we have done changes i.e., gatewayserver, cards, loans and accounts ms. At the same time, set up the k8s cluster with all the required components like i.e., Keycloak, Kafka, Prometheus, Loki, Alloy, Tempo, Grafana in this order waiting for 1 - 2 minites between each installation. Next, we will update the helm charts of getttrightbank with the latest tag names and post that, using the same helm charts we are going to set up all our ms's into the k8s cluster.
 *
 * Updating Helm Charts for K8s discovery server changes
 * -------------------------------------------------------
 * Now, we need to update the helm charts of getttrightbank and post that we can deploy our own ms's into the k8s cluster based upon the latest helm charts. Inside the v3-spring-cloud>>helm>>getttrightbank-services folder, first delete the eurekaserver chart. Post that, open the values.yaml file of accounts, loans, cards and gatewayserver and update the tag names to the newest versions. Apart from updating this image tag name, for accounts ms I am going to create 2 replicas since we want to visualize the demo of load balancing at the server side. So, inside this accounts ms related helm chart values.yaml file, I am going to mention the replicaCount as 2.
 * As a next step I am going to open the v3-spring-cloud>>helm>>environments>>dev-env. Here, inside the Chart.yaml, we have defined all the dependent chart details. Inside this dependencies list, we need to delete the dependency related to the eurekaserver. Do the same for prod-env and qa-env as well. Once we have done these changes, we need to recompile our environment specific helm charts so that all the latest values that we have defined will be compiled and copied into the charts folder of our environment specific helm chart. For the same inside our terminal, navigate into v3-spring-cloud>>helm>>environments. From here first navigate into dev-env and here run the command which is helm dependencies build. With this the compilation should happen but like we discussed before, sometimes the Chart.Lock will create some issues as can be seen from the output below:
 tmi@DESKTOP-CR6ONMF MINGW64 /d/Software Downloads/Spring Boot/Spring/SPRING Programming Tutorial  by Nagoor Babu Sir On 15-02-2019/VCS-Backend/Backend/Micro-s
 ervices/Lobby/v3-spring-cloud/helm/environments/dev-env (main)
 $ helm dependencies build
 Error: the lock file (Chart.lock) is out of sync with the dependencies file (Chart.yaml). Please update the dependencies

 * The reason is that we deleted some dependencies inside the Chart.yaml. To fix this issue, we need to delete the Chart.lock file inside the respective environment folders i.e., dev-env, qa-env and prod-env. Now, try to run the helm dependencies build command. This time the compilation should be successful. Post that navigate into the prod-env and qa-env and run this helm dependencies build command as well. You should see an output like below:
 PS C:\Users\ctemoi\Desktop\K8s\helm - Discovery Server Changes\environments\prod-env> helm dependencies build
 Hang tight while we grab the latest from your chart repositories...
 ...Unable to get an update from the "kubernetes-dashboard" chart repository (https://kubernetes.github.io/dashboard/):
 failed to fetch https://kubernetes.github.io/dashboard/index.yaml : 404 Not Found
 ...Successfully got an update from the "grafana" chart repository
 ...Successfully got an update from the "bitnami" chart repository
 Update Complete. ⎈Happy Helming!⎈
 Saving 7 charts
 Deleting outdated charts
 *
 * Now as a next step, we can try to install all our ms's with the help of helm install command. So inside the environments location, in the terminal execute the command helm install <release-name> <chat name that we are trying to consider> i.e., helm install getttrightbank qa-env. On executing this command the installation should start behind the scenes. As a next step, you can go inside the k8s dashboard and confirm the same. Now, inside the k8s dashboard, I will go to the pods, for accounts you should be able to see 2 pods. If you search the string 'accounts' you should be able to see two accounts pod entries. This is becuase we defined the replica count as 2. You can check the logs of configserver and verify that it started successfully, post that only is when my remaining ms's are going to start. This may take some few minutes time because inside my local system we have installed too many components and due to that my laptop is going to perform very slowly. So you can wait for these deployments to complete behind the scenes for some time. My instructor waited for more than 5 minutes but unfortunately gatewayserver, loans, cards and accounts ms's were not able to start with an error saying, 'org.springframework.beans.factory.UnsatisfiedDependencyException: Error creating bean with name 'compositeDiscoveryClient' defined in class path resource [org/springframework/cloud/client/discovery/composite/CompositeDiscoveryClientAutoConfiguration.class]: Unsatisfied dependency expressed through method 'compositeDiscoveryClient' parameter 0: Error creating bean with name 'kubernetesDiscoveryClient' defined in class path resource'. [org/springframework/cloud/kubernetes/discovery/KubernetesDiscoveryClientBlockingAutoConfiguration.class]: Failed to instantiate [org.springframework.cloud.kubernetes.discovery.KubernetesDiscoveryClient]: Factory method 'kubernetesDiscoveryClient' threw exception with message: 'spring.cloud.kubernetes.discovery.discovery-server-url' must be specified and be a valid URL.' This error is very clear as it says that we didn't provide the url details of Discovery Server. If you see this error it is clearly highlighting that we missed mentioning the property, ''spring.cloud.kubernetes.discovery.discovery-server-url' using which we need to provide a valid url of Discovery Server. Without I mentioning the Discovery Server url, how my gatewayserver, loans, accounts and cards will know? That's why to fix this issue we need to define that property. Inside the blog -'spring.cloud.kubernetes.discovery.discovery-server-url'- also they have clearly highlighted this that whoever is using this dependency, they need to make sure they are setting this property by providing the url of the discovery server deployed on k8s. But it is my bad that I missed mentioning this change inside the helm chart. So, lets go to the helm chart and try updating the same so that we can try re-deploying our applications one more time. But before that, let me run the helm uninstall command for the getttrightbank release. i.e., helm uninstall getttrightbank. This will uninstall all my ms's.
 * As a next step I will go to the helm chart and inside this, 1st I need to go to the getttrightbank-common helm chart and here I am going to open the configmap template because we are going to create a new property using which we are going to provide the discovery server url. So here, in the place of 'EUREKA_CLIENT_SERVICEURL_DEFAULTZONE: {{ .Values.global.eurekaServerURL }}' we are going to create a new property i.e.,  SPRING.CLOUD.KUBERNETES.DISCOVERY.DISCOVERY-SERVER-URL: {{ .Values.global.discoveryServerURL }}. As can be seen, the variable name I have also changed from eurekaServerUrl to discoveryServerURL. So, this is one of the change that we need to do. The next change that we need to do is inside the deployment template file. Here we need to write the if condition and based upon the boolean value we need to set this property. So inside the deployment.yaml file, look for eurekaserver url if condition by searching the string 'eureka'. Here, in the place of eureka_enabled, replace that with discovery_enabled, post that, change the environment variable 'name' and the key also we are going to change both to 'SPRING.CLOUD.KUBERNETES.DISCOVERY.DISCOVERY-SERVER-URL'. With this, we should be good from the deployment.yaml file perspective. As next step, I need to go to the getttrightbank-services and here open the open the accounts ms helm chart values.yaml file and here change the variable name from eureka_enabled to discovery_enabled. This same change I need to do inside the values.yaml files of all the remaining ms's i.e., cards, configserver, gatewayserver,loans as well as message. As a next step, we can go to the environments folder and inside the values.yaml file of dev-env, prod-env and qa-env, we are trying to define a variable name which is eurekaServerURl, we need to replace this with discoveryServerUrl and once we ar done correcting the variable name we should also correct the url details and the url of the discovery server is going to be "http://spring-cloud-kubernetes-discoveryserver:80/". Like this you can see the port is 80. How did I know this service name? haha i.e, 'spring-cloud-kubernetes-discoveryserver'. If you go and check the k8s manifest file tha we have used to deploy the discovery server, here under the metadata you can see we have the name/service type mentioned as 'spring-cloud-kubernetes-discoveryserver' So, the same name we need to give inside the values.yaml. So, make this similar change inside other environments as well. Now, we have made all the required changes. So, recompile all the helm charts. First we will start with getttrightbank-services folder and in the terminal, navigate to the accounts helm chart and run the command 'helm dependencies build' so that the dependencies are recompiled. Same drill, do the same to the other helm charts inside this getttrightbank-services folder i.e., cards, configserver, gatewayserver, loans as well as message. Next, recompile all the environments as well i.e., dev-env, qa-env and prod-env. So, before I try to install all my ms's into the k8s cluster lets make sure that our helm charts are properly compiled or not by running the helm template . command in one of the environments. In my case I am choosing qa-env. I am getting a successful response which means that there are no compilation issues. From the response you can quickly validate for one of the ms's like loans and for it you can see that we are injecting an environment variable i.e., SPRING.CLOUD.KUBERNETES.DISCOVERY.DISCOVERY-SERVER-URL that is related to the docovery server url. So, this confirms that all our changes are recompiled properly. As a next step I will navigate back to the parent folder and here I am going to run the helm install command to install the qa-env by providing a release name i.e., getttrightbank.
 * This will start the installation of my ms's behind the scenes and they should spin up successfully!
 *
 * Demo of Server-Side Service Discovery and Load Balancing.
 * ------------------------------------------------------------
 * Finally, all my ms's started successfully inside my k8s cluster. Now, it is the time to test the changes we have done related to the k8s discovery server. To get started with the testing, behind the scenes I have created a client inside the KeyCloak with the name 'get_tt_right-bank-callcenter-cc' and for the same I also assigned the required number of roles. So, inside the postman, for the request 'Accounts_POST_ClientCredentials', first try to get the access token. For this request you can clearly see that we are trying to invoke the url, 'http://localhost:8072/eazybank/accounts/api/create' which is available inside the accounts ms. So, this is going to create a new account inside one of the instances of accounts ms pod - hope you remember that we created 2 replicas of accounts ms and both of them are going to have their own in-memory h2 DB - In my case I am using a persistent single DB for both. So since my instructor is using an in-memory H2 DB, the plan is, first he is going to create an account by invoking the request 'Accounts_POST_ClientCredentials' Post that, we can try to fetch the account details using the fetch api i.e., 'Accounts_GET_PermitAll' and during the same testing we can easily see how the load balancing is going to happen behind the scenes. For the same let me try to create an account by invoking the request 'http://localhost:8072/eazybank/accounts/api/create', this is going to take a few seconds since this is the first request, and finally you should get an output saying that account created successfully. Now, I will try to invoke the fetch API which is available in the collection gatewayserver i.e., gatewayserver_4. This has a correct url that we can use to fetch the accounts information, 'http://localhost:8072/eazybank/accounts/api/fetch?mobileNumber=4354437687' - Make sure to replace the mobile number input with what you used while creating the account. For my instructor he got an output saying that so-and-so account is created but for my case I got a 500 error both while invoking the request Accounts_POST_ClientCredentials and gatewayserver_4, both with the error message as shown below:
 {
 "apiPath": "uri=/api/create",
 "errorCode": "INTERNAL_SERVER_ERROR",
 "errorMessage": "JDBC exception executing SQL [select a1_0.account_number,a1_0.account_type,a1_0.branch_address,a1_0.communication_sw,a1_0.created_at,a1_0.created_by,a1_0.customer_id,a1_0.updated_at,a1_0.updated_by from accounts a1_0 where a1_0.account_number=?] [Unknown column 'a1_0.communication_sw' in 'field list'] [n/a]; SQL [n/a]",
 "errorTime": "2026-02-09T05:00:52.67734712"

 {
 "apiPath": "uri=/api/fetch",
 "errorCode": "INTERNAL_SERVER_ERROR",
 "errorMessage": "JDBC exception executing SQL [select a1_0.account_number,a1_0.account_type,a1_0.branch_address,a1_0.communication_sw,a1_0.created_at,a1_0.created_by,a1_0.customer_id,a1_0.updated_at,a1_0.updated_by from accounts a1_0 where a1_0.customer_id=?] [Unknown column 'a1_0.communication_sw' in 'field list'] [n/a]; SQL [n/a]",
 "errorTime": "2026-02-09T04:54:45.7154255"
 }

 * Below is how this problem was addressed with even more insights from the instructor:
 * It says that the column communication_sw is not present inside the DB table. Please check the table inside DB and alter it to add this new column.
 *
 * is there anyway to automatically reflect the schema.sql to DB ? So that I do not have to alter the DB manually
 *
 * You can use the property spring.jpa.hibernate.ddl-autowith the following valid values.
 *
 * create: Creates the database schema every time the application starts. All existing data will be erased. Recommended For: Local Development
 *
 * create-drop: Creates the schema when the application starts and drops it when the session ends. Recommended For: Unit Testing
 *
 * update: Updates the existing schema without deleting data. It modifies tables only when necessary. Recommended For: Development and Testing Environments
 *
 * validate: Verifies the schema against the entity mappings but does not make any changes to the database schema. Recommended For: Staging and Production Environments
 *
 * none: Does not perform any schema generation or validation. Recommended For: Production Environments with Migration Tools (e.g., Flyway or Liquibase)
 *
 *  With the above, I was able to resolve the issue and was able to successfully create an account as well as retrieve it. But couldn't visualize the Load balancing aspect like my instructor was because I was using a single persistent DB. But for my instructor as could be visualized, on sending the request 'gatewayserver_4' to get account information based on the mobile number we had used earlier to create that account information, he got a response saying that so-and-so account was created. Now, he kept on invoking this same API multiple times and at some point of time my K8S which is responsible for the load balancing is going to forward the request to one of the accounts instance where the account details are not available inside the internal h2 DB and this should be a confirmation that the load balancing is working fine without any issues even after making the Discovery Server changes. So, when he kept on sending the request multiple times at some point I could see he got a Not Found error. He went ahead and said that if you are not getting this not found error and using h2 db, there is a good chance that your k8s cluster is maintaining a sticky session. What is a sticky session? Whenever a request is coming from the same client using the same IP address from the same server or from the same system, it is always going to forward the request to the same pod which initially processed the request. So if you keep invoking the send request now that we got a not found error we will keep getting this same not found error because my k8s is trying to remember the details fo the client who is invoking this API. This is one of the great advantages that you will have with the Discovery Server. Maybe what you can do, if you want to see the response from another pod, then please take some time i.e., 1 - 2 minutes, post that you can try to invoke the GET request i.e., 'gatewayserver_4' by using the browser in incognito mode. My instructor did the same by trying to invoke the request 'http://localhost:8072/eazybank/accounts/api/fetch?mobileNumber=4354437687' dirictly from the browser and as soon as he hit the enter button he got the expected response this time.
 *  This implied that my K8S server is playing a smart role here so that always the requests from the same client will be forwarded to the same pod as long as the client is making requests very frequently. That's why when he kept refreshing that browser page he kept on getting the response from the same pod. He again went to the postman and tried sending the same request again and this time he got the response from the other pod that is not a 404 error. This confirms that the Load balancing and the k8s discovery server is working perfectly fine. Hope everything was crisp clear. There are some good advantages with k8s discovery server - with this you are going to give lot of free time to the developers where they don't have to maintain the eureka server and your ms's also they don't have to register themselves during the startup and they don't have to send the heartbeats regularly. So, it is going to be the responsibility of the Discovery server to monitor and track all the running instances inside the k8s cluster. But please note that with this approach you will not be having any control on the load balancing. If you need control on the load balancing then you need to go for C-S load balancing with the help of Spring Cloud Load balancer and Eureka Server. With all we have discussed, you should be super clear!!.
 *
 * Deploying Ms's into Cloud K8S cluster
 * --------------------------------------
 * Kubernetes Support by Cloud Providers
 * --------------------------------------
 * # Check slides for this discussion.
 *
 * Set up of Google Cloud Account and install Google Cloud SDK
 * --------------------------------------------------------------
 * We have decided that we will create a K8s cluster in the cloud with the help of GCP. To get started on the same first we need to visit cloud.google.com. On this website, you can log in with your gmail account. If login in to this website very first time, it is going to offer you 300 dollar free credit that you can use within 90 days. If you have already used a Gmail account previously inside this website, just create a new gmail account and login again so that you will be eligible to get a free 300 dollar free credit - you should be able to see that section/link with this offer on the website page either top or middle or somewhere in that website. With this credit, you can explore 20+ free products as can be seen on the information in the website. So, if you click on that banner/link you will be taken to a website where you can see all the details about your free credit. If you can scroll down on this page under the Free tier products section, you should be able to see a product with the name Google Kubernetes Engine. This is the same product that we want to use to create a K8S cluster inside the Google cloud. And since this is eligible under the free tier, I can use that 300$ free credit that I am going to get whenever I create a new account. So this is the main/primary reason we chose GCP compared to AWS and Azure. Now you can click on the console button. If you are landing to the page very first time, it is going to offer you 300$ free trial that you need to use within 90 days. You can click on the try for free button and with that it will ask you all your personal details. You can also see they are assuring you that no auto-charge once trial ends. Next, they are going to ask about your card details because after the free trial/credit if you start using other products like creating a DB or creating a Server or creating a K8S cluster they will charge you - All the cloud providers btw. That's why make sure you are not creating unnecessary products inside the cloud and always be cautious on what you create. Whenever you are done exploring about a product - please make sure you are deleting the product so that you are not spending your 300$ credit within 1 or 2 days. For some reason if you are not comfortable providing your credit card details, then don't provide them as you can always visualize the steps your instructor is following to understand the steps that we need to follow to deploy our ms's into the cloud k8s cluster. Afte sometime you will be landed on the google cloud console home page, if not, you can visit the console.cloud.google.com. By the time you logged in to this page, the Google cloud by default is going to create a project with the name My First Project and you should also be able to see the ID of your project. Using this project, you can explore all the products with the help of the 300$ free credits. To get started around this, now I can create a k8s cluster inside the Google cloud and before that first we need to make sure that we have first installed the Cloud SDK inside our local system.
 * Why should I install the Google Cloud SDK? Because when we install this SDK inside our local system, from our local system itself we can connect with the K8S cluster that is created inside the Google cloud and from our local system itself we can give some commands or we can also install our helm charts from our local system. So, to communicate with the products available inside the Google Cloud from our local system, we need to make sure we  install the Google Cloud SDK.  To do so, visit the website cloud.google.com/sdk the click on the Install Google Cloud CLI button, here you will find all the steps you need to follow to install the Google Cloud CLI.  And as can be seen in the section 'Before you begin' you need to make sure you completed the 2 steps i.e., 'Creating a Google Cloud Project' which we already created previously because as of now I am able to see a project i.e., 'My First Project' which is created for me by default.  After that we need to make sure that the billing is enabled for our Google Cloud Project - This we did in the previous steps by providing credit card details. If you can scroll down on this page, under the section 'Install gcloud CLI version 556.0.0', based upon your OS you will be having the list of steps that you need to follow to install the Google Cloud CLI. So, please make sure you are following these steps based upon your OS to install the Google Cloud CLI inside your local system. So, to confirm if your installation is successful or not, you can run a command i.e., gcloud --version. If the installation is successful you should be able to get an output something like below:
 PS C:\Users\pc> gcloud --version
 Google Cloud SDK 556.0.0
 bq 2.1.28
 core 2026.02.09
 gcloud-crc32c 1.0.0
 gsutil 5.35
 *
 * Once you validate the installation, you need to run a command i.e., gcloud init. This you can see under the section 'Initialize and authorize the gcloud CLI'. Using this, we are going to log in into our Google Cloud account from our local system with the help of Google Cloud CLI. This should give some options to you as shown below:
 PS C:\Users\pc> gcloud init
 Welcome! This command will take you through the configuration of gcloud.

 Your current configuration has been set to: [default]

 You can skip diagnostics next time by using the following flag:
 gcloud init --skip-diagnostics

 Network diagnostic detects and fixes local network connection issues.
 Checking network connection...done.
 Reachability Check passed.
 Network diagnostic passed (1/1 checks passed).

 You must sign in to continue. Would you like to sign in (selecting "Y" will open your browser to the sign-in page where you complete authentication) (Y/n)?  Y

 Your browser has been opened to visit:

 https://accounts.google.com/o/oauth2/auth?response_type=code&client_id=32555940559.apps.googleusercontent.com&redirect_uri=http%3A%2F%2Flocalhost%3A8085%2F&scope=openid+https%3A%2F%2Fwww.googleapis.com%2Fauth%2Fuserinfo.email+https%3A%2F%2Fwww.googleapis.com%2Fauth%2Fcloud-platform+https%3A%2F%2Fwww.googleapis.com%2Fauth%2Fappengine.admin+https%3A%2F%2Fwww.googleapis.com%2Fauth%2Fsqlservice.login+https%3A%2F%2Fwww.googleapis.com%2Fauth%2Fcompute+https%3A%2F%2Fwww.googleapis.com%2Fauth%2Faccounts.reauth&state=jQIFqqtN4ctOUxLuAPJIkgxcmRMsfo&access_type=offline&code_challenge=4oRuD9U0FKQY9OaZM2_DVrlThVJtu3KsNhHJcHZQPKY&code_challenge_method=S256

 You are signed in as: [colincetemoi190@gmail.com].

 Pick cloud project to use:
 [1] project-0835d5ec-6ff8-4ffe-a2b
 [2] Enter a project ID
 [3] Create a new project
 Please enter numeric choice or text value (must exactly match list item):
 *
 * I choose option 1 and below is what I saw:
 Your current project has been set to: [project-0835d5ec-6ff8-4ffe-a2b].

 Not setting default zone/region (this feature makes it easier to use
 [gcloud compute] by setting an appropriate default value for the
 --zone and --region flag).
 See https://cloud.google.com/compute/docs/gcloud-compute section on how to set
 default compute region and zone manually. If you would like [gcloud init] to be
 able to do this for you the next time you run it, make sure the
 Compute Engine API is enabled for your project on the
 https://console.developers.google.com/apis page.

 Error creating a default .boto configuration file. Please run [gsutil config -n] if you would like to create this file.
 The Google Cloud CLI is configured and ready to use!

 * Commands that require authentication will use colincetemoi190@gmail.com by default
 * Commands will reference project `project-0835d5ec-6ff8-4ffe-a2b` by default
 Run `gcloud help config` to learn how to change individual settings

 This gcloud configuration is called [default]. You can create additional configurations if you work with multiple accounts and/or projects.
 Run `gcloud topic configurations` to learn more.

 Some things to try next:

 * Run `gcloud --help` to see the Cloud Platform services you can interact with. And run `gcloud help COMMAND` to get help on any gcloud command.
 * Run `gcloud topic --help` to learn about advanced features of the CLI like arg files and output formatting
 * Run `gcloud cheat-sheet` to see a roster of go-to `gcloud` commands.
 PS C:\Users\pc>
 *
 * Like this , The Google Cloud CLI is configured and ready to use! It will also show you the email account and the project that you are connected to with the help of Google Cloud SDK. With this, you should be clear on how to set up a free account inside the Google Cloud and How to Connect to the same with the help of Google Cloud CLI. As a next step, we can start creating a K8S cluster inside the Google Cloud.
 *
 * Create K8S cluster inside Google Cloud
 * 	-------------------------------------
 * 	We will see the steps we need to follow. Actually, it is a very simple task to create a K8S cluster inside the Google Cloud or any of the cloud environments. To create a k8s cluster you can either click on the option - create a GKE cluster or you can search for kubernetes in the Search box, and you will have an option which is - Kubernetes engine. Since you are trying to access it very first time inside your account, you should be able to see it is going to ask you to enable this Kubernetes engine API. As soon as you enable this K8S engine API, it is going to ask you to enable billing which you need to do.
 * 	As soon as this is done, you will be landed to the K8S Engine page. On the LHS nav, if you click on 'Clusters' you will see an option to create a k8S cluster and by default you will see theu are saying that you can create an autopilot cluster which is nothing but a f of K8S cluster provided by the GCP to make our lives easy. So, based upon our traffic the cluster capacity will automatically be adjusted by the GCP. But If we go with this autopilot option, we will not be able to explore much about the K8S cluster becuase the heavy lifting is going to be done by the google cloud and that is the easiest option that any organization they can follow to set up the K8S cluster. But since in this session we want to explore more about the K8S cluster inside the GCP we need to make sure we click on the option, 'Switch to standard cluster' - It should ask you for your confirmation one more time. Next, you need to give a name to your cluster - I am fine with the default name input i.e., 'cluster-1' and the other default values to the input fields. Just scroll a bit and click on the create button. As soon as you click on the create button, the K8S cluster creation will get started and may take some time. With this you should be clear on the steps taken to create a K8S cluster inside GCP. Next, we will explore more details about this K8S cluster and how to deploy our ms's into this K8S cluster.
 *
 * Installing all our ms's and supporting components in GCP K8S
 * --------------------------------------------------------------
 * Once the k8s cluster is successfully created you should see the status field with a green tick which means that the K8S cluster is successfully created. To know more about our K8S cluster you can click on the name field hyperlink where you can see additioonal information aboutour K8S cluster - like where the K8S cluster is created, ...so all those details you can see here under the 'details' tab. Under the 'Nodes' tab, you should be able to see the list of nodes available under your K8S cluster. As of now, you can see that my K8S cluster is created with 3 nodes. Under this nodes table only you should be able to see all the CPU and memory related information that is assigned to your nodes. To understand more details about a particular node, you can click on the 'Name' hyperlink of that node and that should take you to another page where you can see the cpu utilization, memory utilization, disk utilization of your node. And if you scroll down on this pag you should be able to see the list of pods that are executed inside your particular node. As of now, you should be able to see that there are a good amount of pods that are installed by the google cloud for maintaining my K8S cluster behind the scenes. Like you can see here, we are able to see the CPU information, memory information as well as disk information. How we are able to see this - my google cloud behind the scenes it installed a pod which is responsible to collect all the metrics i.e., gke-metrics-agent - this you can clearly see under the pods table. Similarly under this pods table all the pods required to maintain and manage you k8s cluster are going to be installed by default.
 * Once you validated everything on this page you can navigate back to the previous page and for some reason if you want to add more number of nodes to your k8s cluster you can use the option 'Add node pool'. So once you validated all this information you can go back to the main page where you can see the cluster that is created inside the GCP. Here as a next step, we need to connect to this GCP K8S cluster from our local system - for the same you can click on the 3 dots at the furthest end of this cluster record and there you should be able to see a 'Connect' option. POst that, you will get a command which you need to copy and execute the same inside your local terminal with the help of Google Cloud CLI. On executing - this should establish a connection with the K8S cluster that we have inside the google cloud. And as can be seen in the output, we have something saying that, 'kubeconfig entry generated for cluster-1.' inside my local system. So, to validate if the connection is really working or not, I can run the command which is 'kubectl get nodes' and his should output as shown below:
 PS C:\Windows\system32>   gcloud components install gke-gcloud-auth-plugin

 Restarting command:
 $ gcloud components install gke-gcloud-auth-plugin

 PS C:\Windows\system32> gke-gcloud-auth-plugin --version
 Kubernetes v1.34.2+17253df9df7f1ef8629397908f6b9e70ce0783b7
 PS C:\Windows\system32> gcloud container clusters get-credentials cluster-1 --region southamerica-east1 --project project-0835d5ec-6ff8-4ffe-a2b
 Fetching cluster endpoint and auth data.
 kubeconfig entry generated for cluster-1.
 PS C:\Windows\system32> kubectl get pods
 No resources found in default namespace.
 PS C:\Windows\system32> kubectl get nodes
 NAME                                       STATUS   ROLES    AGE   VERSION
 gke-cluster-1-default-pool-209ff425-6mn8   Ready    <none>   26m   v1.34.3-gke.1245000
 gke-cluster-1-default-pool-4f3185f1-00qx   Ready    <none>   26m   v1.34.3-gke.1245000
 gke-cluster-1-default-pool-b88eb0de-0swh   Ready    <none>   26m   v1.34.3-gke.1245000
 *
 * Since we have 3 nodes, yes you should be able to see 3 nodes in the output. And here, you may have a question like since we previously connected to the local K8S cluster and now all over sudden we are able to connect to the K8S cluster present inside my google cloud?! How is this possible? What happened to the previous connection to my local K8S cluster? Because we didn't even delete my local K8S cluster. For the same if you right click on you docker desktop icon you should be able to see an option, 'kubernetes context'. Since right now I connected to a new cluster, that is marked as the default context. If I change the context to docker-desktop and run the same command which is kubectl get nodes, I will only get one node because inside my local k8s cluster I have only a single node. So, using the context inside docker desktop you can connect to any number of K8S clusters and whenever you want to interact with a particular K8S cluster, you need to make sure you are switching the context to the corresponding K8S cluster.
 PS C:\Windows\system32> kubectl get nodes
 NAME             STATUS   ROLES           AGE    VERSION
 docker-desktop   Ready    control-plane   142d   v1.30.2

 * As a next step, we need to start applying our helm charts and kubernetes manifest files on top of the k8s cluster that we have created in the Google cloud.
 * Same drill as we did on the local K8S cluster. First deploy the K8S Discovery server i.e., kubectl apply -f kubernetes-discoveryserver.yml. As a next step if you run the command helm ls, you will notice that as of now we don't have don't have any installations done on our remote K8S cluster with the help of helm. Now you can install the Helm charts in the following order, waiting 1–2 minutes between each deployment: Keycloak, Kafka, Prometheus, Loki, Alloy, Tempo, Grafana, GetttRightBank. Make sure you are in the right parent folders while installing these respective helm charts. As can be seen nothing is different, its just that your cloud/production K8S cluster is going to have more capacity compared to your local K8S cluster. You can wait for 5-10 minutes for the deployments to complete(Before you try to assume that something is not working) and post that you can go to your GCP K8S cluster to validate if all the installations are successful or not. To understand if the installations of our helm charts and applications are completed or not, you can click on your cluster hyperlink and inside the page that comes up, under the Nodes tab you should be able to see all the list of nodes - its 3 for us. If you click on the hyperlink of any of the nodes you should be able to see all the running pods inside that particular node. This way, we can understand under which node a particular pod is running. If you are able to see the green tick marks for all your pods under the 'Status' field, means that all your containers are deployed into the respective pods without any issues. To make sure that everything is going to work as we expect, we should make sure gatewayserver container is deployed successfully because without this we cannot test any of the ms's.
 * So to understand under which node my gatewayserver is installed, you can manually go into the 3 nodes looking for this gatewayserver pod or you can try to run few command inside the terminal in order to tell under which node your gatewayserver pod is present. The first command is kubectl get pods - this will give the list of all the pods that got created inside our k8s cluster. So, you need to look for the gatewayserver related pod and copy the entire pod name. Post that, you need to run the command kubectl describe pod <pod_name>. This will give more details about your pod. Under this output you should be able to see information about the node where the pod is created. So can capture/note this node name/id - the last few characters i.e., Node: gke-cluster-1-default-pool-27ece9f1-bxrt/10.128.0.58 - here you can take note of 'bxrt'. Inside our google K8S cluster you can go to the page where you can see all the nodes. Here, look for the node ending with those last characters you noted and if you navigate to that node you should be able to see the gatewayserver related pod. You can click on this pod related hyperlink and you should be able to see under the details tab the status of this pod. Inside this pod only the gatewayserver related container got deployed. Under the 'Events' tab, you can be able to understand the list of events that happened. Here you should see clear information on which image was used to set up a conatiner inside this pod. Also under the logs tab, you should be able to see all the logs related to the gatewayserver container. With this you should have confidence that your gatewayserver got installed successfully. As a next step, on the LHS nav you can click on the WorkLoads hyperlink and in the page that comes up you should be able to see all your deployments. WorkLoads mean nothing but your deployments.
 * Here, you should be able to see all the Deployment K8S objects that happened based upon your instructions. Using these Deployment K8S objects only, the replicas and the pods and the containers will be deployed into the K8S cluster. Once we confirm that all these K8S Deployment objects are successful - you can check with the status field, if all are green ticks means ok/successful, you can locate the nav link 'Services and Ingress' on the LHS nav. Under the Services tab, you should be able to see all the K8S Service objects that have been created based upon our deployment instructions. If you scroll through these K8S Services, you will notice that most of them we exposed as Type 'ClusterIP', except KyeCloak and Gateway Server because we want to create some credentials with the help of KeyCloak Admin Server and post that also we want to send all the traffic to the gatewayserver and that's why we need to expose the gatewyaserver to the outside of the K8S cluster. Btw, whenever we use a Service type as LoadBalancer, in the cloud environment, it is going to create a public IP using you can access you ms. You can see this under the endpoints field for the Services with Service type as 'External Load Balancer' - If needed your K8S admins they can map this public IP to a domain name or to a DNS name. So all my Service objects are also deployed successfully as can be seen from the green tick and ok symbol under the status field. You can also check if all the 'Secrets and Config Maps' are created by clicking on that respective hyperlink on the LHS nav.
 * As a next step, you can go back to the 'Services' and here first I will try to access the Keycloak server. Under the 'endpoints' field, you can copy that url - post that inside an in cognito tab you can access the same and you should be able to be landed on the KeyCloak Admin console. Here enter the admin credentials i.e., 'user' and 'password' - based upon our configurations/settings inside the helm chart of KeyCloak. As soon as I click on sign in, I should be able to log into my administration console. Same drill create a client. Under the credentials tab, copy the secret and mention the same inside our postman. And here inside my postman, I should also update the access token url, as of now we are have the localhost but instead of the localhost, we need to mention the public IP of KeyCloak. After creating a client we need to create the Realm Roles and assign them to this client - same drill vas we have been doing. As a next step, go the postman and test the API's same drill as we did - but remember to mention the public IP of the gatewayserver while firing the requests. Actually I testes everything and it worked! This confirms that our set up is working as expected in the production/cloud as well. This should be clear to you and you should be happy that all the things are working like we expected even inside the cloud also. I also validated the grafana components as well - same drill and it worked.
 *
 * Validate Grafana components inside Google K8s Cluster
 * ---------------------------------------------------------
 * As of now, our Grafana is deployed as a ClusterIP service. You can confirm the same under the Services page inside the GCP console. Against the grafana you will see we have the type as 'ClusterIP' and under the endpoints there is no public IP for us to access the Grafana - it is a private IP. In this kind of scenario, you K8S admin is going to have 2 kind of options, either he can update the helm chart to have the type as LoadBalancer and run the helm upgrade command and with that the developer should be able to access grafana. The other option that your K8S admin may recommend is - from your local system you need to connect to the K8S cluster with your own K8S credentials. As of now, we connected as a K8S admin, so regardless of your credentials type, you need to make sure that you are connected to your K8S cluster from your local system with the help of GCloud CLI, post that you need to run a kubectl port forward command i.e., kubectl port-forward svc/grafana 3000:3000. With this the grafana is going to be accessible inside our local system at the port 3000.  But before we try to run this command you need to know what the grafana admin credentials - these you can get from the grafana helm chart values.yaml. As using these credentials you are going to login into the grafana service from your local system. This way a developer can access any production service which is exposed as a ClusterIP. So, whoever has access to the K8S cluster they can easily access the cluster Ip services as well. Inside the explore hyperlink of the grafana dashboard, you can open a dropdown to confirm that the integration between grafana, Loki, Prometheus and Tempo is working. This you will be able to tell if you see all these four in the dropdown. Yea, the integration is working and that's why we are able to see these 4 options in the dropdown. First you can select the Loki option, then select the label as 'container' and the value as 'gatewayserver'. Post that click on the 'run query' which will give me all the logs of gatewayserver.
 * If you click on any of the logs, post that after the trace id, you should be able to see a button to navigate to the Tempo using which you can see the distributed tracing details and which you can validate whenever you have some performance problems. This confirms that Loki and Tempo are working as expected. This we already discussed in details before. As a next step you can select Prometheus and inside this you can select a metric i.e., 'up', post that under the label filters, you can select 'container' and after that you can select the duration as last 15 minutes and run the query. With this you should be able to see a graph - this confirms that prometheus is also working fine. We also discussed this earlier on in details. Hurreey! you should be super happy that everything is working like we expected in the Cloud K8S cluster as well.
 *
 * Deleting Google Cloud K8S cluster
 * ------------------------------------
 * We deployed all our ms's into the K8S  cluster and we validated all of them including the supporting components and everything si working as expected. If you want to validate the event driven scenarios that we have build with the help of Kafka, then you can go and check the logs of accounts and message ms's - same drill like we had done before. Nothing new as the event driven scenario should work/is working fine. As a last step, we need to make sure we are uninstalling all the components from our K8S cluster - using the helm uninstall <release_name> command and post that we should also delete the K8S custer inside the Google cloud. Same drill of uninstalling the components - nothing new. Also don't forget to uninstall the Discovery server btw - to delete/uninstall the discovery server: Go to the location where this file is >> Run the command kubectl delete -f <file_name>.  If you don't do all these you may end up getting a suprise credit card bill. So, please follow all the instructions and make sure you are deleting your cluster at the end of it all. Before deleting the cluster, you can confirm that all the releases/deployments are uninstalled by running the command - helm ls. You should not see any release records which confirms that all th releases are uninstalled. Finally uninstall the Discovery Server installation by going to the parent location where the k8s manifest file of discovery server is present and then run the command  -  kubectl delete -f kubernetes-discoveryserver.yml. As soon as I run this command the uninstallation of Discovery Server will also be completed. To validate if all the uninstallations are completed in the GCP console, under the workload nav, you should see it is empty. Same thing under services you should see no records, also under Secrets and Config Maps, everything should be empty.
 * Now my cluster is empty, as a next and final step delete your cluster from your cloud environment. Under the Cluster nav, you should not see any cluster record and this should confirm that our cluster is successfully deleted. With this our K8S cluster will be permanently deleted.
 * With all we have been discussing you should be super clear on how to deploy your ms's into any cloud provided K8S cluster. The process between you local K8S cluster and the cloud provided K8S cluster is going to be the same, also the commands are going to be the same. With this, you can consider yourself in the developer perspective having all the required stepping stone knowledge to work with any K8S cluster you will be provided with.
 *
 * 	Introduction to K8s Ingress, Service Mesh(Istio) and mTLS
 * 	-----------------------------------------------------------
 * 	In this section, we are going to focus on few advanced concepts/Topics of DevOps which are usually practiced by many organisations in the real production ms's environment. As a developer, you don't need to learn in details about these advanced concepts but knowing them on a high level will give you an advantage/edge inside your ms's interviews or in your day to day ms's work as well. For eaxmple think like you are attending a ms interview, if there is a question about a particular topic, usually you will try to tell the answer based upon the developer perspective because you are a ms's developer. On top of that, if you add more details on what usually happens inside the production environment by using various DevOps concepts then definitely the person whoever is interviewing you will be impressed by looking at your knowledge even at the DevOps advanced concepts. One such advanaced concept that we will be discussing in this session is K8s Ingress. As of now we learnt about how to expose our ms's with the help of K8s Service object. If you go to the official documentation of Service object inside K8s - https://kubernetes.io/docs/concepts/services-networking/service/. If you scroll down, you will be able to see how to do the pod definition, how to map an application, ...etc. All this information we already discussed. But the new information that I want you to know here is, if you can scroll down under the Service Type, you will see we have ClusterIP, NodePort, and LoadBalancer - We discussed these 3 already. Apart from these 3, we have one more Service Type which is called ExternalName.
 * 	If you try to click on that ExternalName hyperlink - you will be navigated to its detailed doc. So, under which scenarios we need to leverage this ExternalName. Whenever you want to map your Service to one of the DNS names or domain name that your organisation owns, then in such scenarios we are going to use the Service Type as ExternalName. This will give flexibility to your K8s admins to map your services to a specific domain name that your organisation owns. This way, any client application who wants to consume your ms's , they can simply forward their requests to the domain name that you have given to them and behind the scenes this ExternalName Service Type available inside the K8S cluster is going to take care of forwarding the request to the corresponding pod or container inside your K8s cluster. This was one of the new information I needed you know about the Service Types inside K8s. As a next step on the LHS nav, click on the Ingress hyperlink that is just below the Service hyperlink. Using Ingress also, we can expose all our ms's to the outside of the cluster. But here you may have a question like, how Loadbalancer is different from the Ingress? As you can clearly see that Ingress is not available inside the Service doc, it is something else. As in it is a separate topic/concept inside K8S. So, there has to be a difference between Service Type LoadBalancer and Ingress even though they are doing the same Job which is exposing the ms's to the outside of the cluster. We will focus more and more on Ingress next.
 *
 * 	Deep Dive on K8s Ingress and Ingress Controller
 * 	------------------------------------------------
 * 	# Check slides for more details.
 * ContD from slide discussion...The second point is, sometimes Ingress may not be the go to answer for all your problems. The developer may want to write some business logic which cannot be achieved by the K8s Ingress. In such scenarios obviously the option is to go with the Spring Cloud Gateway because Spring Cloud Gateway approach has a lot of flexibility provided where the developer can write the custom business logic with the Java programming language. So, it all depends on the scenario you are into and what kind of organization you are going to work at. With all we have discussed you should be crisp clear on the difference between K8s Ingress and Spring Cloud Gateway approaches. Now lets try to understand more details about the K8s Ingress.
 * So, how are the DevOps team members going to define the routing requirements with the help of Ingress? For the same, in the slide you can see a sample snippet provided. In that, the very first important information that you need to observer/visualize is 'Kind' element whose value we have mentioned as 'Ingress'. With this is clearly an indication to the K8s cluster that this manifest/specification is related to the Ingress. You already know of the purpose of the metadata.name definition. Moving on to the specification definition i.e., 'spec', under it we have an element that we need to write which is 'rules'. Inside these rules we can provide what is the base 'host' name of our organisation. under this host element, we can go ahead and provide various path configuration - like if someone is trying to access so-and-so path what is the backend service inside the k8s cluster you want to forward the request. So all those details the DevOps team members they can configure using this kind of K8s Ingress configurations. In the slides you can see that sample configuration/manifest file for your reference but like we said, you don't need to learn or remember all these things because these are all responsibilities of DevOps team members or a person who is responsible to set-up K8s components inside your organisation. So, with the help of Ingress we are only going to define the configurations or our own requirements. At the end of the day there should be a component which is responsible to implement all these Ingress requirements that we have defined. For the same we have a component called Ingress Controller.
 * So, by default Ingress resource can't do anything based upon our configurations. We need to have an Ingress Controller installed or configured inside your K8s cluster to make Ingress resources functional. There are very many popular Ingress Controllers available inside the market. Few such famous Ingress controllers are - NGINX Ingress, Traffic and HAproxy Ingress. The job of these controllers is - they should watch for the Ingress resources and configure the underlying network components accordingly inside the K8s cluster. If you want to understand what is the full list of Ingress Controllers available - https://kubernetes.io/docs/concepts/services-networking/ingress-controllers/.  Like this K8S officially published in their documentation that these are all the supported Ingress Controllers - More than 30, actually. Organisations use them based on their own custom requirements. In that page if you search for ' NGINX Ingress Controller for Kubernetes ' you should be able to see it - it's actually the most commonly used Ingress controller because it is an Open-Sourced controller. This is build and maintained by the NGINX team. Similarly there are many more other Ingress Controllers which we don't need to deep dive into them and understand them because it is the job of K8S admin to set up the Ingress Controller and make it functional inside the K8S cluster. As a next step, in the slide you should be able to see/visualize a sample representation  and discussion of how traffic is going to flow inside K8S cluster whenever we are using K8S Ingress. # Check slide for that.
 *
 * Benefits of K8S Ingress and the kind of traffic it Handles
 * --------------------------------------------------------------
 * # Check slide for this discussion.
 *
 * Ingress Controller Vs Service Type LoadBalancer
 * ----------------------------------------------------
 * # Check slide for this discussion.
 *
 * Introduction to Service Mesh and Its Capabilities
 * -----------------------------------------------------
 * # Check slide for this discussion.
 *
 * Introduction to Service Mesh Components
 * ---------------------------------------
 * # Check slide for this discussion.
 *
 * We just visualized a quick introduction about all the advanced concepts inside ms's environment like Ingress, Service Mesh, what are sidecar containers, etc. So, you should have a clue about all these advanced concepts as a ms developer then only it is going to make your life easy as many at times you may get pulled into some discussions around these advanced topics and in some interviews, people may also ask you like are you using any Service Mesh inside your production environment? - You don't have to tell them how you are using or how you are setting it up but at least you should be aware about what is Service Mesh and what its capabilities are. With this I am assuming you are super clear about what Service Mesh is. As a next step, we will try to understand how the Service Mesh is going to secure all the internal traffic happening between ms's.
 * In the slides under 'Who handles Service to Service Traffic?' section, you can see the last point around security, it is highlighted that Service Mesh can secure internal service-service communication in a cluster with a concept called Mutual TLS /mTLS. As a developer you should know about this concept i.e., mTLS becuase there might be some questions to you inside an interview asking that, you are securing your edge server with the help of OAuth2, Spring Security, etc. - then why are you not securing all your internal ms's inside your K8S cluster? - The answer is mTLS. So, we will try to understand more details around this in the next sessions.
 *
 * * * */
@SpringBootApplication
@EnableDiscoveryClient
public class GatewayserverApplication {

	public static void main(String[] args) {
		SpringApplication.run(GatewayserverApplication.class, args);
	}

	/* This method is going to create a bean of type RouteLocator and return it.
	* Inside this RouteLocator only, we are going to send all our custom routing related configurations based on our requirements.
	**/
	@Bean
	public RouteLocator eazyBankRouteConfig(RouteLocatorBuilder routeLocatorBuilder) {
		return routeLocatorBuilder.routes()
				.route(p -> p.path("/eazybank/accounts/**")
						.filters(f -> f.rewritePath("/eazybank/accounts/(?<segment>.*)", "/${segment}")
								.addResponseHeader("X-Response-Time", LocalDateTime.now().toString())
								.circuitBreaker(config -> config.setName("accountsCircuitBreaker")
										.setFallbackUri("forward:/contactSupport")))
						.uri("http://accounts:8080"))

				.route(p -> p.path("/eazybank/loans/**")
						.filters(f -> f.rewritePath("/eazybank/loans/(?<segment>.*)", "/${segment}")
								.addResponseHeader("X-Response-Time",LocalDateTime.now().toString())
								.retry(retryConfig -> retryConfig.setRetries(3)
										.setMethods(HttpMethod.GET)
										.setBackoff(Duration.ofMillis(100),Duration.ofMillis(1000),2,true))
						)
						.uri("http://loans:8090"))
				.route(p -> p.path("/eazybank/cards/**")
						.filters(f -> f.rewritePath("/eazybank/cards/(?<segment>.*)", "/${segment}")
								.addResponseHeader("X-Response-Time",LocalDateTime.now().toString())
								.requestRateLimiter(config -> config.setRateLimiter(redisRateLimiter())
										.setKeyResolver(userKeyResolver())))
						.uri("http://cards:9000")).build();
	}

	@Bean
	public Customizer<ReactiveResilience4JCircuitBreakerFactory> defaultCustomizer() {
		return factory -> factory.configureDefault(id -> new Resilience4JConfigBuilder(id)
				.circuitBreakerConfig(CircuitBreakerConfig.ofDefaults())
				.timeLimiterConfig(TimeLimiterConfig.custom().timeoutDuration(Duration.ofSeconds(4)).build()).build());
	}

	@Bean
	public RedisRateLimiter redisRateLimiter() {
		return new RedisRateLimiter(1, 1, 1);
	}

	@Bean
	KeyResolver userKeyResolver() {
		return exchange -> Mono.justOrEmpty(exchange.getRequest().getHeaders().getFirst("user"))
				.defaultIfEmpty("anonymous");
	}


}
