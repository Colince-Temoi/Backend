package com.get_tt_right.gwserver;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
import org.springframework.cloud.gateway.route.RouteLocator;
import org.springframework.cloud.gateway.route.builder.RouteLocatorBuilder;
import org.springframework.context.annotation.Bean;

import java.time.LocalDateTime;

/** Update as of 03/6/2026
 * -------------------------
 * Axon framework IntelliJ plugin.
 * -------------------------------
 * Go to plugins and install it. Using this plugin you can improve your productivity while you are trying to build ms's with the help of Axon framework. I know you may have installed lot many other plugins and your IntelliJ IDE maybe crying like, "How many more plugins 😒 I need to accept". Sometimes we can't avoid plugins because they make our lives so easy! Once the installation is completed and you have restarted your IDE, you can go to the classes where we have implemented Axon framework components like commands, aggregates, handlers, etc. For example, inside customer ms, inside the command>>controller package, there is a class with the name CustomerCommandController. If you can open this, inside al these APIs we are trying to create a command for example, in the /create API we are trying to create the object of CreateCustomerCommand, in the /update API we are trying to create the object of UpdateCustomerCommand, etc. If you closely observer, In the line of code where we are trying to build the command object, there is a LHS arrow with orange color.
 * If you click on that arrow, it is going to take you to the method where a particular respective command object is being handled. Since the CreateCustomerCommand object is being handled in the CustomerAggregate constructor, we will be landed on that constructor. Similarly, If you want to know from where this command is being triggered/fired, you can click on the Orange arrow from the CustomerAggregate constructor, and you will be landed on the CustomerCommandController class /create API. From the command handler method i.e., CustomerAggregate constructor, we are going to execute some business logic followed by we are going to create an Event object i.e., CustomerCreatedEvent and the same we are going to publish. If you try to click on the Orange arrow on the LHS of the line of code where we are creating a CustomerCreatedEvent object, you will be able to see that this time you have 2 options where you can navigate to. The plugin is showing that this event object is being handled at 2 different places - The very 1st one being inside the CustomerProjection class, followed by the Second one is inside the CustomerAggregate class with an annotation @EventSourcingHandler.
 * So, let me select the EventSourcingHandler option, and like this, it is going to take me to the 'on' method where we are handling this respective event object i.e., CustomerCreatedEvent with the help of the @EventSourcingHandler annotation. Here, we can also click on the Orange arrow which will take us back to the CustomerAggregate class constructor where we are trying to create this respective event object i.e., CustomerCreatedEvent. Now, you can click on the orange arrow one more time and this time, I am going to select the CustomerProjection option and as you can see, we will be landed onto the method where we are trying to handle this event object i.e., CustomerCreatedEvent in the CustomerProjection class. This way, you can always leverage this plugin to navigate from commands to events, events to commands. Usually inside real applications, there will be too many commands and events and instead of opening these methods and classes manually, we can leverage these orange arrows which are going to improve our productivity. You should be happy with this plugin - but unfortunately this plugin as of now available only inside IntelliJ IDE marketplace but not in other IDE marketplace like Eclipse, VS Code, etc. You can always check out in case Axon framework team is going to publish this plugin in other IDE marketplaces in the future, that is if you may prefer using other IDEs. But for now if you are using IntelliJ in your day-to-day development, you can happily use this plugin.
 *
 * Perform Validation using MessageDispatcherInterceptor
 * -----------------------------------------------------
 * Whe a Command object is dispatched from the Controller class, it will be handled inside the Aggregate class. As part of this Command object handling logic, we can perform some validations i.e., for the CreateCustomerCommand object the validations that we are first performing inside the Aggregate class is - we are checking if the Customer is already registered with the given mobile number or not, of course we can don more other n number of validations. Once we are satisfied with these validations, then only we can proceed with the next set of logic i.e., ..., Creating an Event object and finally publishing it. Check the CustomerAggregate class  constructor docstring for more details.
 * We have our Interceptor class ready but if you try to test this application/ms, the interceptor logic is not going to be invoked. To make this work, we need to make one more small configuration. In the Spring boot main class i.e., CustomersApplication, I am going to create a method registerCustomerCommandInterceptor - Check the docstring for more details.
 * Save the changes we have done >> Do a build and test our application/ms. For my case I had stopped all supporting components and everything because my machine is starving of resources. Otherwise, I would have not done that - I would just proceed to save the changes we have done >> do a build and restart only the Customer ms where we have do the changes. But now I will have to start up everything as the documented step by step states. Once everything is up and running properly, we can go to the CustomerCommandInterceptor and we can try to keep a breakpoint in the first line in the body of our lambda expression. Now from our postman, we will try to invoke some operations - first what we will try to do is, we will try to create a customer by using the same mobile number, and we should get a validation error as we already created a customer with the same mobile number. Now, if you click on the send button, the break point should stop inside our CustomerCommandInterceptor. Let's try to debug this line by line. As of now, if you check inside the 'command' object, you will see it is an object of "GenericCommandMessage" which is an implementation of "CommandMessage" interface. If you click on "view" on the RHS of the "GenericCommandMessage', you will see inside we have the entire payload that we are trying to send.
 * So, If I try to step over/debug - F8 key, on this entire code, you will see that the inner if condition i.e., if (optionalCustomer.isPresent()) is not satisfied. Seems something is not working - so release the breakpoint/resume the execution and go to the postman to see the output. Interestingly the Customer is created. Why? 😂 remember we had soft deleted the previous customer - yes that's why. Now if you fire the request again and this time if your try to debug this interceptor the if (optionalCustomer.isPresent()) condition ind the interceptor during the CreateCustomerCommand should be satisfied and we should get an expected Exception which is "Customer already registered with given mobileNumber 3254537623"
 * Now, lets test/debug the update customer request. If you fire the request and this time provide a given random mobile number, since their won't be any record for this mobile number you should get a Validation Error. This time the CreateCustomerCommand if condition will not be satisfied and the request/control will go to the else if i.e., UpdateCustomerCommand which will be satisfied. Now, if you resume the execution/release the break point you are going to receive a Customer not found exception in your postman response which is a Proof that everything is working as expected.
 * Similarly, test/debug the delete customer request. If you fire the request and this time provide a given random customer Id, since there won't be any record for this Customer Id you should get a validation Error. This time the CreateCustomerCommand and UpdateCustomerCommand conditional blocks will not be satisfied and the request/control will go to the conditional block of DeleteCustomerCommand. Now, if you resume the execution/release the break point you are going to receive an error i.e., Customer not found with the given input data customerId. With all we have discussed, you should be clear on how we have implemented this interceptor and how we registered it with the Axon framework. So, please always leverage this kind of implementation if you want to perform some business logic that needs to be executed just before the Command handling logic in the Aggregator. With this, we also have an advantage which is all your command related validations you are going to maintain in a single place as we have seen. Nothing but it is going to be easy to maintainance as well.
 *
 * Reading Data from Event Store
 * -----------------------------
 * Inside CustomerCommandInterceptor class, we are trying we are trying to perform the validations and while we are trying to do this, we are actually loading the data from the Reader DB. As can be visualized, with the help of CustomerRepository dependency, we are trying to load the data which we stored on the Read side. This may work in this scenario but in real projects you may get ome unique scenarios or peculiar scenarios where you may want to read the data from the event store / Write DB to perform some validations. That's why in this session we will learn on how to read the data from the event store. To demo the same what we are going to do is, we are going to perform the validations by loading the data from the event store.
 * For that, we will not disturb the existing validations code that we have written inside the CustomerCommandInterceptor class. Instead, we will go to the CustomerAggregate class and here inside the method where we are trying to handle the UpdateCustomerCommand, we are going to write some logic. Check out the CustomerAggregate class UpdateCustomerCommand handler method for more details.
 *
 * Event Processors in Axon Framework
 * -----------------------------------
 * We will be discussing an Exception flow that may arise in real projects when we are trying to implement the CQRS and ES kind of design pattern inside your ms. To explain us about this - lets imagine a scenario where one of the client send an Update/Write/Delete request, since it is a command the write APIs will be invoked >> A command will be dispatched >> The aggregate is going to handle the command object >> The data will be stored inside the Event Store/Write DB >> Once the data is stored inside the EventStore, an Event object is going to be published into the Event Bus >> Now, on the projection side, think like one of the Event Object Handler method is handling the given Event Object and while storing the data inside the Read DB or while processing the event object on the Projection side, let's imagine we got a RTE. At this point in time what do you think is going to happen? 😂 Do you think that whatever we stored inside the Event Sourcing DB is also going to be rolled back? The answer to this question depends on the Event Processor that you are trying to use inside your ms. This said, we need to understand what are the different types of Event Processors that are available inside the Axon Framework and how they are going to affect the behavior whenever we are facing some RTEs while processing the Command objects and Events. We will discuss about Event Processors in detail but before that - let's visualize a small demo so that you will have crisp clarity on what we are trying to covey.
 * Imagine we received a request to the /update REST API >> In our logic, 1st we are trying to create the UpdateCustomerCommand object which will be dispatched from the Controller layer to the Aggregate layer, nothing but the respective Command handler that we have defined >> Inside this Command Handler, our business logic is going to be executed and eventually we will populate an Event object which is going to be published to the Event Sourcing Handler in the Projection Class. Now, if you navigate to the Projection class of this flow inside the respective Event Handler method, we are trying to simply update the data inside the read DB. Check that Event Handler method for more details/docstring about this discussion.
 * To understand the Event Processors, we can go to the https://www.axoniq.io/ website >> Developers >> Documentation. Inside the documentation website you can click on the link - https://docs.axoniq.io/axon-framework-reference/5.1/ which is related to the Axon framework reference guide just under the "Reference" Section. Here, as can be seen on the LHS nav, you will be having all the good documentation around all the concepts present inside the Axon Framework. For the information around the Events click on the Events nav link dropdown on the LHS nav to expand it >> then I will click on the Event Processors nav link dropdown to expand it. In the middle section of the page, you should be able to see some good information on how the events are going to be handled inside the Axon framework. You can use the documentation of Axon Framework version 4.10 which your instructor was also using - You can switch to that from the far RHS somewhere on a dropdown up on the page.
 * You will see an illustration there which we will revisit later. But before that you can see that it is highlighted the inside the Axon framework, "Event Processors come in roughly two forms: Subscribing and Streaming." The very first one is Subscribing and the second one is Streaming. Let's understand the difference between Subscribing and Streaming Event processors so that we can choose one that best suites our customer ms. Inside the Subscribing Event Processor, everything is going to be carried out with a single thread - when we say everything, we imply right from command dispatching >> Event handling >> Event Storing >> ...so, everything is going to be handled using a single thread. To leverage this Subscribing Event processor, you should make sure that both your command side and query side, they are build under a single JVM. If you look at the technical flow illustration that we have in our slides, as of now inside our customer ms, we build both the command and query side under a single ms / Under a single JVM. When both these Command and Query sides are under a single JVM, means it is perfectly possible to execute the entire flow in a single thread because they are deployed inside a same JVM. But few projects, they are going to build this command and query sides as 2 different ms's where they are going to deploy them in separate JVM's or into separate docker containers. In such scenarios, the entire flow cannot be executed in a single thread because they are deployed in completely 2 different JVMs. So, based on how you build your CQRS and ES pattern, you need to choose one among the event processors available.
 * In our scenario since we deployed everything into a single JVM, it is going to make sense to use the Subscribing Event processor because it is also going to give some flexibility on how we are handling the Exceptions. When we execute the entire flow in a single thread, we can easily perform the rollbacks on the command side also whenever there is an Exception on the Reader side or Event Object handling side - We will explore on how to implement this in a few. But before that, let's try to understand how the Streaming Event Processor is going to work. Inside the Streaming Event Processing, every operation is going to be executed using multiple threads, like there will be thread1 which is going to take care of command dispatch, and there will be thread2 which is going to take care of event handling. This event processor is well suited for the applications where they have implemented command sid and query side in different ms's or in different JVMs. Apart from the thread perspective differences, these event processors they also have another important difference - Subscribing Event processing is going to follow sequential event processing, so, when you have multiple events published  - all of those events are going to be processed sequentially when we are leveraging the subscribing event processor. Whereas under the streaming event processor, all the events that are published from a command, they can be processed parallely. So as of now inside our ms's , a single command is publishing a single event but later on, we will have a visual explanation and hand-on on how to dispatch or publish multiple events from a single command. Overall, my instructor is recommending to use the subscribing event processor when you have built CQRS pattern in a single JVM. Whereas, if you have built Command side and Query side on 2 different JVMs, then in such scenarios using the subscribing Event processor is not going to make any sense. You need to use the Streaming event processor.
 * This streaming event processor is also going to help you to process a good amount of events compared to the subscribing event processor because inside the streaming event processor, all the events are going to be processed parallely instead of sequentially. We will see an explanation on how to select one of these event processors in the coming sessions. For now, inside the next session lets try to focus on the diagram in the official documentation and try to visualize what is happening there. As we were discussing, multiple events can be published from a single command. First, lets try to understand how to publish multiple events/event objects. On the LHS nav, if you click on the "Events" nav link and click on the "Dispatching" nav link, you will be able to see a nice doc on how to dispatch events from an aggregate. As of now, what we have implemented in our code, we are using the apply method available inside the AggregateLifecycle class that is going to take care of dispatching the respective event/event object. So, with the help of this apply method we are only able to dispatch a single event/event object - this is a story and implementation that we know of already. If scroll down a lil bit to understand more advanced information, you can see something like, "The apply method returns an implementation of the ApplyMore interface and that interface provides the following method .." So, by using the return type of this apply method, you can invoke any of the 4 methods listed which are .andThenApply(Supplier<?>), .andThen(Runnable), .andThenApplyIf(Supplier<Boolean>, Supplier<?>) and .andThenIf(Supplier<Boolean>, Runnable runnable). All these 4 methods, they are going to allow you to apply or dispatch more number of events/event objects. So that's how we can publish/dispatch multiple events. You should now have gotten some clarity. If needed, we can also dispatch the events from a non aggregate class as well as documented in the official doc - if you scroll dow a lil.
 * If you are inside a non-aggregate class, means you can't use this AggregateLifecycle#apply. In such scenarios you can use EventGateway#publish to publish the event. You just have to inject this EventGateway bean as a dependency to your normal non-aggregate class and with the help of its publish method, you should be able to publish any number of events. There is a good chance that as part of a single Command we may end up into situations where we want to trigger or publish or dispatch multiple events. Now, with this understanding, lets go back to our diagram under the "Event Processors" nav link. Think like, one of the command triggered 3 different events and these 3 different events are going to be handled by 3 different Event Handler methods because at the end of the day a single event object can be handled by a single event handler method. So as shown in the diagram, assume the 1s 3 orange boxes are E1, E2, and E3. When you have these kind of multiple events, it is recommended to group all related events into a processing group as shown in the grey box just below the 3 Events. So, what is the advantage that we are going to get when we group multiple events under a processing group - with the help of processing group, we can provide or implement non-functional configurations like on how we want error handling or how we want to process these events - like whether we want to process them sequentially or concurrently. So, all such configurations we can make with the help of processing group. So, as can be visualized in the image, you can define any number of processing groups inside your application - but please note: A single Event Handler can also be assigned to a single processing group. So, once this processing group layer is established, we can assign how a particular processing group we want to execute - like whether we want to use subscribing style or whether we want to use Streaming style of Event processing. That's why you will be able to see at the end of the illustration, we have Event Processor.
 * So what is an Event Processor inside Axon framework - we have only 2 options, one is Streaming and the other one is Subscribing. So, based upon your requirements you need to assign one of the event processor implementation. So, with the event processor implementation, we will be able to control threading mode - whether we need the execution in a single thread or in multiple threads. And we can also implement Error handling logic as well. In the case of Streaming Event processor, you are going to get one advantage which is - if you have multiple events being published by a single command all of them are going to be processed parrallely. We also have one disadvantage with the streaming processing - if a RTE happens inside one of the Event handlers, it is not going to stop the execution of inside the other event handlers/ it is not going to roll back the TXNs that are committed by the other event handlers. If you are looking to execute all your events and commands inside a single thread then subscribing event processing is the correct event processor that you need to choose. Because inside the subscribing event processor, you will have flexibility to execute all your events/event objects in a single thread. Since it is a single thread under a single JVM, you are going to get the flexibility to roll back the transactions and to control the error handling behavior. But the disadvantage with the Subscribing event processor is - All your events are going to be processed sequentially one by one. For our case where we have implemented the entire CQRS pattern under a single ms, it is going to make sense to leverage the Subscribing Event Processor - but to leverage the Event processor as we have seen in the illustration, first we need to to assign our events to a processing group, followed by to this processing group we need to assign the event processor. We will understand how to achieve this inside the next session.
 * By default, inside the Axon framework, the Streaming Event processor is going to be considered. That's why when we are having a RTE on the Query side as had been simulated in the demo, the transaction that happened on the command side is not getting rolled back. Reason: The command is run with a different thread compared EventHandler method where the Event object is being handled. So, to override this default behavior and leverage the Subscribing Event processor so that we can roll back on the command side as well. This should be easily achievable because right now we have implemented both command and query side as part of a single JVM. If you have implemented these 2 sides of the CQRS  pattern across multiple JVMs, then it is not going to be a super easy job 😂 You will need and be forced to follow the Saga design pattern. For now, lets not worry about the distributed transactions. So, as a very first step - I need to assign my event handlers to a processing group. So, what I am going to is, all the events related to the customer I can assign to a processing group. To create and assign the events to a processing group, we just have to use an annotation which is @ProcessingGroup. To this annotation, we can give/mention a name to the processing group as a string input to this annotation i.e., @ProcessingGroup("customer-group"). - Check out the CustomerProjection class for more details.
 * Now, you can save the changes, do a build and try to invoke the update operation again. As of now, inside the CustomerProjection class, you will be able to see there is a simulated "throw new RuntimeException("It is a bad day");". Now, from the postman if you try to invoke the update customer operation, this time you should be able to see we are getting an Exception which is "It is a bad day" and with the status as 500 Internal Server Error. That particular response you are seeing in your Postman we have coded it inside the GlobalExceptionHandler - here we have a method that handles all the exceptions of type "Exception.class" and so whatever logic we have written there, the same is being executed and that's why we are receiving the Internal Server Error response. This time if you visit the Axon Server dashboard, if you try to refresh the Search 🔍 you will see that no new record is being written in the ES store which is a clear indication and verification that the transaction on the command side is also never committed - it is only going to be committed once all the events are processed without any exceptions. In the GlobalExceptionHandler class we can do one more enhancement - As of now, whenever a RTE happens on the Event Side or on the Command Side we are going to execute the method#handleGlobalException annotated with @ExceptionHandler(Exception.class) because that is a global exception handling that handles the Exception.class. Any Exception inside the Java Ecosystem is a child of Exception.class and that's why the method#handleGlobalException handles all kind of exceptions. But if you are looking to write a separate Exception logic for the RTEs that happen due to the Axon framework code, you can do that very easily. First, what I am going to do is try keeping a breakpoint in the first line of code in the method#handleGlobalException and try again to fire the update operation. Once you click on the send button you will see that as of now the execution is stopping at the breakpoint we have put inside the method#handleGlobalException where we are trying to handle the exception of type Exception.class. You can now release/resume the execution.
 * But like we have discussed, we can write a separate logic for all the exception that are arising from the Axon framework code. So, copy and paste the same method#handleGlobalException code + its method level annotation(s). Now, instead of the value "Exception.class" in the annotation @ExceptionHandler(-), we are going to handle/mention the CommandExecutionException.class. So, what Axon framework is going to do is - whenever you are trying to execute a Command, as a part of command you are going to dispatch the command >> Process it >> Store the Command data to the Write DB >> Dispatch the Event >> Process the Event >> Store the Event data to the Reader DB. Since command execution is the very first step of the CQRS flow, any RTE that is going to happen inside the entire CQRS flow as discussed and implemented, it is going to be wrapped as a CommandExecutionException by the Axon framework. Now this time instead of the method#handleGlobalException annotated with @ExceptionHandler(Exception.class) the lower method#handleGlobalException which is annotated with @ExceptionHandler(CommandExecutionException.class) is going to be executed. This we can also prove in the response we are going to receive in the postman - in its logic instead of just sending exception.getMessage() in the ErrorResponseDto, what we can do is, we will try to append a String prefix which is "CommandExecutionException occurred due to : "+exception.getMessage(). Save the changes >> Do the build and this time keep the break point on the first line of code  also in the method#handleGlobalException which is annotated with @ExceptionHandler(CommandExecutionException.class) and lets see at which method the execution is going to stop. Now, if you click on the send button of the update customer request in postman, this time you should be able to visualize that the breakpoint stopped inside the method#handleGlobalException which is annotated with @ExceptionHandler(CommandExecutionException.class) where we are trying to handle the command Execution Exception. Like this we can clearly see that the framework is doing a good job here in wrapping the RTEs that are happening inside the Axon framework into a separate lower Exception i.e., CommandExecutionException. Now, if you release the breakpoints and resume the execution you should be able to verify/validate  what we have just discussed.
 * With all we have discussed, by now you should have gotten some clarity around the Event Processors available inside the axon framework and more importantly how we solved the problem at hand that we experienced with the help of Subscribing Event Processor. More details around the distributed transactions we are going to discuss in the coming sessions. Before we move on the next discussions, make sure you go to the CustomerProjection class and comment out the throw new RTE line of code that we were using for our simulations and tests and uncomment the actual business logic line of code i.e., iCustomerService.updateCustomer(customerUpdatedEvent);
 *
 * Implementing CQRS and ES patterns in Accounts, Cards and Loans ms's
 * ---------------------------------------------------------------------
 * As of now we have implemented the CQRS and ES patterns inside our customer ms. The same steps you need to follow for the Accounts ms. This time you should be able to do it very quickly as you already know what changes need to be done. To even make your life easier you instructor has documented all the steps as well that you need to follow in a read me file for each of the ms's - check that out.
 * All those are simple Java code - since you are an advanced ms's developer you should be able to simply understand all that is happening in all the code snippets.
 * Once you have made all the changes >> Do a build >> and start your accounts ms. But before that delete all the existing data so that we can get started from scratch. First >> Delete you axon server container >> Followed by inside my axonserver volumes folder that we created in the desktop, under the data I am going to delete all it's contents/existing data such that it is empty. Similarly under the events folder I am also going to delete all it's contents so that it is empty. But don't try to delete the content under the config folder as that is mandatory. Next, similarly under my user folder ind Disk C , we have some files related to the h2 db for the respective ms's - delete them. Now, 1st we can start our axon server container >> Next, in your browser, you can try to refresh the path - http://localhost:8024/ and since it is a brand-new container set up, we need to make sure we are clicking on the Complete button that we see on the landing page under "Nodes And  Applications" to complete the axon server set up/installation with the initialization action as "Start standalone Node" then only the microservices will be able to connect with this axonserver. Next, first we are going to start the Eurekaserver followed by we are going to start the accounts application. If needed you can start customers application but since we already completed the testing of customers ms we are not going to start that service.  At last we need to start the gatewayserver application as well. Once your services are started successfully, you can go to the postman and under the Event Driven Microservices >> accounts you then open the create accounts request/API and by using that mobile number you can try to fire the request and as can be seen, I got a successful response >> Next under the fetch account request/API, invoke it and it should provovide you with the account details - Copy the response/the account details response body and paste inside the update request body and their we will try to update the branch address to 1234 and click on the send button. Next fire another update request with the branch address as 1235 - and yes 2 times our update operation is successful. Now, if we try to read the account details with the help of fetch API  you should be able to see that under the branch address we have the 1235 which we is a confirmation that the read DB is updated with the most current value. But if you are looking for the history of the events that are triggered against to a specific account, then we need to come to this axonserver dashboard and here you need to click on the "Search" option present on the LHS nav and as you can see on the window that comes up, according to the filter "query time window" which has a default value of 1hr - the events displayed are those for the past 1 hour when you click on the 🔍icon on the RHS of the "query" text box with empty input.
 * As can be seen, in the past 1hr there are 3 events triggered for the aggregate identifier which is nothing but the account number. First, we initiated the AccountCreatedEvent, followed by AccountUpdatedEvent twice. On the LHS nav, if you click on the "Commands" option, ypu can also be able to see the metrics. Also under the "Queries" option on the LHS nav you can also be able to see metrics related to the Query side - You should be able to see that for example we have fired the FindAccountQuery multiple times - this you should be able to see as well. With what we have discussed so far, you should now be clear on how we set up CQRS and ES patterns inside our accounts ms. Same drill - we can do the same for the loans and cards ms as well and do the end-to-end testing to verify the same just as we have been doing for the customer and accounts ms - Easy! as you have everything documented and in addition you have a step-by-step documentation in your instructors readMe files + source code for the respective ms's which you can always use a refresher. With all that we have been discussing, you should now be completely comfortable with the CQRS and ES patterns that we have implemented with the help of Axon Server. In the next session, you can start all the 4 ms's - customers, account, loans and cards ms's and try to test all the 4 ms's end-to-end to make sure one more time that everything is working perfectly around the CQRS and ES patterns. Since we have been successful in implementing CQRS nad ES patterns inside cards and loans ms's - Nothing but if you go under the cards ms >> Under the src folder you should be able to see there is a command sub-package containing all the related command side logic. We also have a query sub-package containing all the query side logic. The same applies for the loans ms as well. Now, start all the 4 ms's and now if you go to the axonserver dashboard and click on the option "Overview" on the LHS nav, under the "Nodes and applications" section, you should be able to see/visualize all these 4 ms's are registered under the axonserver - as can be seen the orange icons representation/illustration. Finally, you can try to restart/start your gatewayserver application to fetch the latest information from the eurekaserver. Once the gatewayserver application restart is completed we need to create some data under the customer, accounts, cards and loans ms's. From my postman, I am going to create a new customer with the same mobile number that we have used for accounts ms testing. In short, make sure you are using the same mobile number across all the ms's while doing you tests because one we will have created the data, we will need to check if the data is stored inside all the 4 ms's properly by using the API that we have created inside the gatewayserver. If you recall, long back we have implemented an API with the help of API composition pattern using which we are going to fetch all the data related to a customer using a given mobile number.
 * So, first create the customer by firing the respective create request in you postman >> Once the customer is created I can try to invoke the fetch API. Copy the response details and paste them under the update API request body and here I am going to update the name to "Colince Linus Temoi" and click on the send button. Now If I go and click on fetch cutomer information request the name is going to have the latest updated value. Same drill create Account, Loans and Cards details using the same mobile number. For Loans update operation we will try to update the ammountPaid information to 10k and with that the outstandingAmmount information is going to be 90k from the total loan of 100k. Now, if you try to fetch the loan details again you should be able to see the latest information. Next, you can go to the creation of card and try to create a new card with the same mobile number. Once the card is created I am going to fetch the card details and those card details I am going to paste under the update card API request - Here I am going to mention the amount used as 10k followed by available amount to 90k. Now if you fire the send button, our card details will be updated. Now, if you once again invoke the fetch API, you should be able to see/get the latest information. With this we have populated the data in all the ms's. Now, if you go the gateway server and fire the fetch customer summery request - an API which we have implemented with the help of API composition pattern. This API is going to read the data from all the ms's by using the given mobile number. Now, if you click on the send button- with that you should be able to see we are getting the customer information, account information, loan and card informations. This gives a confirmation to us that even though we have migrated our into CQRS and ES architectural patterns, the reading operation is not affected becuase we made changes in such a way that whenever someone invokes a query operation, it is going to read the data from the Read DB. Now, let's go and chacek what is their inside the axon server dashboard. On the LHS nav, if you click on the "Search" button, you should be able to see that in the last 1hr, the many events displayed are triggered. The aggregate identifier will be card number in the case of card ms, it will be a loan number in the case of loan ms, customer id in the case of customer ms and account number in the case of account ms. Anytime like we said, if you are looking for a specific information you can always use the "aggregateIdentifier", I mean inside the query text box, you can try to type aggregateIdentifier="the_actual_aggregate_identifier_value" i.e., account number, loan number, card number or even customer id. Now, if you clcik on the search button, you are going to get the events that are handled/that are dispatched in the same order/aggregate sequence with the very first sequence number i.e., 0 at the bottom of the table. Since right now we are catching each and every action inside this event store/Write DB, this is going to open lot many scenarios. If your analytics team they want to perform some analytics on the users behavior or on the users actions, based on the history of events they can try to take some marketing decisions.
 * Similarly, if the developer team are trying to analyze a prod issue, using this, they can try to understand what are the sequence of events that happened behind the scenes, and accordingly they can try to initiate the same kind of actions on another new account and this is going to give great clues to the developers in reproducing the issue that is being faced inside the production. There are many use cases that are supported by the ES pattern. That's why many of the big organizations like the e-commerce applications, OTT applications, Social applications, etc. are all going to leverage ES because if they store all the events that are being triggered by the end-user, they can take this data and on top if it perform analysis and take informed decisions. Incase if a UI application is looking for the most current data, then they are always welcomed to query the read db because that information is readily available on the read DB. Apart from this, there are many advantages that you are going to get since you have 2 separate DBs on th write and read side - now you have complete freedom on choosing the DBs based upon your use cases, you can do the independent scaling and when the write DB is down, there won't be any impact onto the read side. Like this there are many advantages like we discussed previously. So please make sure you are crisp clear about this CQRS and ES patterns. If you are attending a ms's interview, there is a very good chance that you may be asked about the CQRS, ES and Saga patterns. And now that you have the stepping stone knowledge, you should be good to go.
 *
 * Different approaches to Implement CQRS
 * --------------------------------------
 * When someone is trying to implement CQRS pattern, the will end up choosing various flavours based upon their business needs. We will discuss and see all the possible flavours available for the developers to implement the CQRS pattern. Check slides for more details.
 *
 * Why we need to Consider Materialized View Pattern
 * ----------------------------------------------------
 * Before we see more details about this pattern, 1st we will see some information regarding under which scenario we need to use this pattern. RecalL the discussion we had in details about Cross-Service queries? Since each of the ms is going to have its own DB - whenever we have a scenario where we need the data from multiple services. Such scenarios can be called as Cross-Service Queries because we are going to execute the queries across multiple ms's and you are going to aggregate/collet the data before sending to the client application. We also visualized the demo on how to overcome the Cross-Service Queries challange by using the API composition pattern with the help of API Gateway. If you recall the illustration - we have our Spring Cloud Gateway application in which we xeposed an API to the client application , so whenever our client applications makes a call to this API - Internally my gateway is going to invoke all the 4 respective services(Loans, cards,accounts, customer) to fetch the data from their respective DBs and the colleting of the data is going to happen at the gateway level. And the response will eventually reach to the client application. But, there is a drawback with this approach - Even though from client perspective a single call is made, internally the gateway application is going to make the individual calls to the respective ms's. Since you have a scenario to fetch the data from 4 different ms's, the gateway is going to make 4 different API calls to the respective ms's. If each of the ms is going to take 200m's to process and send the data then obviously it is going to take 800ms to lead the data from all the 4 ms's if the invokation is done sequentially. Considering the scenario wher the gateway application invoked all these 4 ms's sequentially one by one, in this kind of scenario the total time is going to be 800ms and here in the gateway for the aggregation of the data  considering it is going to take +200ms more time, this means in total the client has to wait for 1s. This clearly tells us that as the number of ms's increases, the waiting period or the performance time to load the response to the client is going to also increase. Of course, we can try to overcome this challenge to some extent by invoking the APIs parallelly from the Spring Cloud Gateway - but still there will be some network latency which will add to the overall response time to the client application. So, performance is the very first problem we will experience in this 1st approach. The second problem is these individual ms's are going to store the data in their respective DBs based upon their business logic. But for some reason if the client is expecting the data in a different format, then the formatting/transformation logic we need to write inside this gateway application - this means that in this gateway application we will end up writing some good amount of complex business logic which is going to teke some time to execute and return the response to the client application. The 3rd drawback is - Whenever the client application is trying to load the data using this Spring Cloud gateway application, this simply mean the gateway application has dependency aon all the 4 ms's(Accounts, cards, customer and loans) - if one of the ms's is down or it is responding very slowly, in such scenarios obviously the client application they are going they are going to get the null information or the empty information from the respective services. So, these are all the challanges w ehave with this Gateway aggregator or API composition pattern. This pattern can be useful for a smaller application who has some limited number of ms's or limited number of scenarios around the Cross-Service queries.
 * But we have a better approach - Materialized view pattern. Check slide for discussions about what this pattern is.
 * Based upon your business requirements, you need to choose between the API composition pattern and Materialized View Pattern. API Composition pattern - you can use this for a smaller application where they have very less traffic. But if your application is expecting a huge amount of traffic then going with the API composition pattern may not be a feasible option because everytime an end-user is trying to perform an action - if you want to read the data from multiple ms's  and onec the reading operation is complete you want to transform the data and send it back to the client application. So doing all these operations in real-time is going to bring some performance issues and it is also going to make your ms's super busy always. They will be already busy with the write operations but for the read operation if you are making them also busy then you can imagine what is going to happen. That's why for heavy loads traffic go with this Materialized View Pattern. One more beauty of this pattern is - whenever the read operation is happening, their is no dependency on the actual ms's. Even if one of the actual ms is down, or it is performing slowly, still the read operations will have Zero impact. In the case of this eazybank scenario, obviously bank will have thousands and millions of customers. As soon as these customers login, if this dashboard needs to be displayed then it is recommended to maintain a separate Materialized View focusing on this scenario. Check slide for the sequence flow discussion that is going to happen whenever this Materialized View Pattern is implemented.
 *
 * Implementation of Materialized View Pattern
 * --------------------------------------------
 * As of now, we have 4 different ms's namely customer - which holds the customer information, followed by accounts, loans and cards ms's. Let imagine we have a UI page which is responsible to display the data that is spread across all these 4 ms's. We are going to build this new API with the help of this Materialized View Pattern. We aready saw an example with the help of gateway aggregator pattern, but this time we are going to follow this better approach - MV pattern. To make things clear around what we are going to build, think like we have a customer r/ted table with the fields:  cus_id | mob_num | name | email | active_sw | audit_columns | ... This table is present inside the customer DB that is going to be used by the customer ms. Similarly, we have account tables with fields like : mob_num | account_num | acct_type | branch_address | active_sw | audit_columns | ... we also have a cards table with fields like: mob_num | card_num | card_type | total_limit | amount_used |  available_amount | active_sw | audit_columns | ... and finally we have a loan table with fields like: mob_num | loan_num | loan_type | total_loan | amount_paid | outstanding_amount | active_sw | audit_columns | ... We know that all these tables are being used by the respective ms's. For example inside the customer table which is going to be maintained by the customer ms, it is going to store the information related to the customer. Same thing for the other ms's as well. Inside the 4 table, you may have observed that there is a common column which is mob_num. Initially when the customer registered with the bank application, an entry is going to be made inside the customer table. Using the same mob_num, the end user can create a new account, card as well as loan. Now, what we want to do is, we want to build a new API inside a new ms - imagine this ms name is profile. What this profile ms is going to do is It is going to maintain a MV with the table name as profile. Inside this profile table we are going to  mainatin the fields like : mob_num which is a common column for all the ms's, followed by name which we are going to get from the customer table, followed by  account_num which is going to come from the accounts table, card_num from cards table, loan_num from loans table,we also have active_sw which will indicate whether a given profile record is active or not. At last it is also going to have some audit columns. I am trying to keep thes table simple just to make our lives easy but in real applications your table may have 100's of columns or your profile ms may have multiple tables inside it - all this is going to depend on your business requirements. For now lets keep things simple and let's assume the profile table/MV has the fields we have mentioned and that's what we want to maintain under the profile ms. Initially this profile table/MV is going to be empty - as soon as a customer is created, an event is going to be published by the customer ms for the profile ms. As part of the event processing, a new record is going to be inserted into this profile table/MV for a given mobile number along with the name details and all the other fields inside this MV are going to be null value because we are yet to create the account, card and loan details for this mobile number that belong to the given user/customer. And, there is also no guarantee that the end-user will have the account, card or loan details created for them. Few of the customers will only have account details i.e., account_num and even fewer customers are going to have account and card details i.e., account_num, card_num, ...etc. Since it is optional for the end-user initially, the fields acct_num, card_num and loan_num in the profile table/MV can be nullable fields. During the very first insertion operation, we are going to keep those 3 columns as null.
 * Later on if an end-user ends up creating an account or card or loan, respective events are going to be published and as part of the event processing the individual null fields in the profile table/MV will be updated based upon the mobile number. Once we have this MV pattern, what we can do is, we are going to invoke a /profile API which is exposed by this profile ms itself via postman. For now, we are going to mimic this API invocation using postman. But in real applications you can assume this invocation will come from UI applications or from the websites. As soon as someone invokes this profile API, based upon the mobile number that we provide, all the information in the profile table/MV will be loaded from the DB and the same we are going to send back to the postman or client application or UI as a response. With that what we are going to achieve, we are going to overcome all the challenges that we have with the API Gateway aggregator pattern. Whenever we are looking to read the profile information, we are never going to call these individual ms's, we are always going to call this /profile ms which is going to read the data by using this MV table that we are going to maintain behind the scenes. With what we have discussed, you should now be crisp clear about what we are going to build with the help of MV pattern. Next, we will be making the code changes inside our ms's to adopt this pattern. To impl the MV pattern inside our ms's, we need to create a new ms with the name 'profile'.  What we are going to do is - in the work-space 00_start, just copy one of the ms i.e., 'customer' and paste it here inside the same 00_start workspace and rename it to 'profile'. Go into this profile folder that we have created and open the pom.xml file. Here, rename the artifactId and name from customer to profile - nothing but just replace anywhere we have "customer" in the pom.xml to "profile" . Under the description I will keep "Microservice for reading profile info". Since we got nothing else to change, save the pom.xml. Now, go to the intelliJ and try to load this new maven profile ms that we have created. As of now, profile ms has code related to the customer ms. Like this we need to make some good amount of changes. 1st inside the resources/application.yaml file update the port number to 8084. Followed by update the name of the application to "profile" and h2 file I will also tyr to give as "profile". Next update the info.app.name to "profile". In short replace everywhere we have "customer" to "profile" in this application.yaml file. Next, under the axon related properties - get rid of the eventhandling attribute and related child attributes. Reason: Inside this profile ms I am not going to build any logic around the commands. We are only going to focus on the query side and that's why delete those properties and only maintain the axon.axonserver.servers property. Using this only, my profile ms is going to connect with the axon server. Why my profile ms has to connect with the axon server? Because we are going to make other ms's to publish the events whenever we have any data changes in them and the published events we are going to handle inside the profile ms. Since we are going to leverage the axon server as our event bus instead of Kafka and RabbitMQ, we need to make sure our profile ms is connected to the axon server. So from the application.yaml perspective w e are good to go. Next, under the same resources folder, delete the schema.sql file. Reason: We are going to create a profile related table with the help of jpa entity class itself. Next, under the java folder, rename the package to "com.get_tt_right.profile". Make sure to select "All in selected/current module" then finally click on "Refactor". Under the audit>>AuditAwareImpl you need to mention the current auditor name as PROFILE-MS followed by delete the entire command package and its sub-packages because we are not going to build anything around the command side as we are only going to focus on the query side.
 * Followed by I am going to rename the CustomerConstants to "ProfileConstants". This should also rename the constructor that we have defined explicitly. Under the dto package, rename CustomerDto to ProfileDto. Next, under the entity package we are going to rename "Customer" to Profile. We will get back to these files and change the code inside them - for now we are taking care of renaming all the files. Next, under the exception package we don't have to make any changes. Under the mapper package rename the CustomerMapper to ProfileMapper and get rid of all the code/methods present inside them. For now lets keep this file as empty as we will come back to it later. Under the query package we have the sub-packages i.e., controller, handler and projection. We also have a file/clas i.e., FindCustomerQuery. Rename this FindCustomerQuery to FindProfileQuery. Coming to the member it contains i.e., private final String mobileNumber; we should be fine with it because we want to query the profile ms with the h elp of the mobile number itself. In the controller package, rename the file CustomerQueryController to ProfileQueryController. Inside this ProfileQueryController instead of CustomerDto, we are going to return ProfileDto and instead of "fetchCustomerDetails" we can mention "fetchProfileDetails" as a method name. Request param is going to remain as mobile number and the api instead of /fetch, we can mention as /profile. So, anyone who wants to invoke this API they will need to mention the path as /api/profile. Next, I want to create the object of FindProfileQuery and using the same we are going to invoke the query method of QueryGateway and to it, we are going to pass the findProfileQuery reference and the response type is going to be ProfileDto and the same we are going to return as a response from this method. Like this we are good from the ProfileQueryController class perspective. Now, we can go to the handler>>CustomerQueryHandler and rename this to ProfileQueryHandler. Here, instead of the method name "findCustomer" rename that to "findProfile" and this method is going to accept the object of "FindProfileQuery" and using the same we are going to invoke the service method. Now, here, instead of the secondary type member i.e., "ICustomerService" we can try to modify it to "IProfileService" - now what we can do is we will go to the service package and there instead of "ICustomerService" we can rename it to "IProfileService", next get rid of all the other specifications inside this class and retain only the fetch specification i.e., ProfileDto fetchCustomer(String mobileNumber); - rename it to ProfileDto fetchProfile(String mobileNumber); and as you can see the return type from this specification is going to be ProfileDto. Also rename the specification comments from "Account..." to "Profile...". Next, if you go back to the ProfileQueryHandler - this time you should not have any CE's. Next, under the service>>impl>>CustomerServiceImpl - rename that to ProfileServiceImpl followed by you need to get rid of the implementations to the non-existing specs. To resolve the existing CE's inside this ProfileServiceImpl class we need to update the secondary member i.e., private CustomerRepository customerRepository; to private ProfileRepository profileRepository;. This you can simple achieve by going to the repository package and rename CustomerRepository to "ProfileRepository". Make sure that the "Profile" entity is passed to the generic of JpaRepository. Next, delete the abstract method  Optional<Profile> findByCustomerIdAndActiveSw(String customerId, boolean active); and retain only Optional<Profile> findByMobileNumberAndActiveSw(String mobileNumber, boolean active); and cross-check to make sure that the return type from this method/spec is Optional of "Profile". With this, we should be good from the ProfileRepository perspective. Now, inside the ProfileServiceImpl, as you can see the ProfileRepository secondary member is injected into this class and using the profileRepository reference we are invoking the findByMobileNumberAndActiveSw spec and the return type from this method is going to be Profile.
 * If there is no Profile we are going to throw an ResourceNotFoundException with the resource name as "Profile" and with the mobile number that we have received. Towards the end, using the ProfileMapper, we need to invoke one of the methods - haha, but before we try to write a method inside the ProfileMapper class, we need to first define fields inside the entity class and inside the Dto class. First, inside the ProfileDto, delete the existing attributes definition and we will try to mention the field names i.e., private String name; - this we are going to get from the customer ms private String mobileNumber; - this we are going to get from the customer ms. private long accountNumber; - this we are going to get from the accounts ms. private long cardNumber; - this we are going to get from the card ms and private long loanNumber; - this we are going to get from the loans ms. Like this, we are good from the ProfileDto perspective. Next, we can go to the Profile entity class - get rid of the existing code and copy/paste the lines of code in your instructors repo - check that class for more details. Now we can go to the ProfileMapper and here you can paste the code to the new method i.e.,mapToProfileDto - check it for more details. Once this method is created you can use it in your ProfileServiceImpl class and to this method the source we can pass the Profile object as the 1st parameter followed by a new object of ProfileDto class. On the LHS of the mapper logic, we are receiving the result into a profileDto reference and that is what we are returning from the ProfileServiceImpl class fetchCustomer method. With this we should be good from the ProfileServiceImpl perspective. Next, open the ProfileQueryHandler and here using the iProfileService reference we are going to invoke the method fetchProfile and since this is going to return the profile dto, we need to update the same on the method "findProfile". Next, we can go to the projection>>CustomerProjection and rename this to ProfileProjection. And to this class I am going to inject the secondary type IProfileService - this will nothing but make available the IProfileService implementation class bean. And inside this projection class we are going to write business logic to handle various events that are going to be published by the individual ms's. For now you can delete the entire code related to the customer ms, and here we will write our own new event handling logic. Next, in the CustomersApplication class, rename that to ProfileApplication. In this class, we don't need the 2 methods - registerCustomerCommandInterceptor(InterceptorRegistration logic) and configure(EventProcessingConfigurer logic) because we are not going to have any Interceptor class and since we are also not implementing any command side logic we can get rid of these 2 methods respectively. As a last step, we can go to the test folder and try to rename the class CustomerApplicationTests and AccountApplicationTests to ProfileApplicationTests. With what we have discussed, you should be crisp clear about whatever changes we have done so far - we shall continue with this journey in the next sessions.
 * Profile ms is ready, as a next step, we can start publishing the events from the individual ms's whenever the data is being changed inside these services. To publish these events we need to create the Event classes. As of now we have created these event classes directly under the command package i.e., CreateCustomerCommand, DeleteCustomerCommand and UpdateCustomerCommand as can be seen in the customer ms. Reason: The commands are going to publish these Events using the AggregateLifeCycle. Very similarly, we have to create these event classes as can be seen in the event package but we should not create these event classes directly inside the individual ms's because the same event is going to be used by the profile ms as well. So, since the event classes are going to be used/shared by the multiple ms's we thus need to create these event classes under the getttright-bom>>common project/module. So, under the src folder of this common module, create a new package "event". Inside this event package the very first class that we are going to create is CustomerDataChangedEvent - this event is going to be published whenever the data on the customer side is changing. Similarly, I am creating few more classes i.e., AccountDataChangedEvent for the accounts ms, LoanDataChangedEvent for the loans ms and CardDataChangedEvent for the cards ms. Now, let's try to declare the required fields inside these event classes - check out these respective classes for the respective docstrings. Now we have created the required event classes under the common project, now these event classes can be leveraged by any of the ms's. As a next step we need to start dispatching or publishing the events from the individual ms's to the profile ms. 1st we will try to make changes inside the customer ms. Here, we have a class with the name CustomerAggregate - Inside this class we are handling the commands that manipulates the data of customer. We have a method handling the CreateCustomerCommand, similarly for UpdateCustomerCommand and finally for DeleteCustomerCommand. Here, what we can do is - 1st in the method that handles CreateCustomerCommand, just like how we are creating and populating the CustomerCreatedEvent object, what we are going to do is - Check CustomerAggregate class docstring for more details under the section "Materialized View Pattern Impl"
 * With these changes - my customer ms is going to publish the events whenever the data is being changed. The same kind of changes that we have done, we need to make inside the other 3 ms's i.e., loans, cards and accounts. Its the same drill - nothing different. As of now, we have only published/dispatched the CustomerDataChanged events from the customer ms. We need to publish the AccountDataChangedEvent from the accounts ms, LoanDataChangedEvent from the loans ms and CardDataChangedEvent from the cards ms. With what we have discussed so far you should now be crisp clear. Now, all our individual ms's they will publish or dispatch the events whenever data is being changed.
 * As a next step, inside profile ms we need to handle these events by writing some business logic. So check the ProfileProjection class docstring for more details.
 * Now, we have done all the required changes - the individual ms's are going to dispatch the events and my profile ms is going to listen to them or it is going to consume those events and update the profile DB. As a next step, we can try testing this scenario end to end. If you can recall - we already build a REST API inside the Profile ms. If you go to the ProfileQueryController - here, we have an API with the name /profile. From this API we are trying to dispatch the query FindProfileQuery which is going to be handled by the ProfileQueryHandler and there, we are simply trying to fetch the profile details from the DB/MV. But to invoke this API which is present inside the ProfileQueryController, we need to make some changes inside the gateway application. If you go to the GatewayServerApplication, as of now we have the routing related configuration ONLY for the customer, accounts, loans and cards ms's. Very similarly, we need to add the routing related configuration for the profile ms as well. Just copy and replicate one of the routing configurations and make the necessary changes to it -  which is just changing the name to profile where the previous ms name was present and being case sensitive as was. Simple!!!. Now, we should be good -do a build i.e, ctrl + f9 which should be successful to confirm there are no issues. Next we will be visualizing the demo!  by following the steps documented in "Testing your Event driven Ms's from scratch". Now all my ms's and supporting components are started successfully. 1st we will try to test the customers application by creating a brand new customer. As soon as we try to create a new customer, a CustomerDataChangedEvent should be dispatched to the profile ms. I can try to clean the console under the customers application and under the profile ms. Now, inside the postman, under the customer ms, I am going to fire the create new customer request with the given mobile number and other customer details. I received a successful message as expected! But if I check the Profile ms logs, there is a RTE due to which the given event is not processed as shown in the excerpt below.
 08:22:47.862 WARN  [EventProcessor[customer-group]-0] o.a.e.TrackingEventProcessor - Error occurred. Starting retry mode.
 com.thoughtworks.xstream.security.ForbiddenClassException: com.get_tt_right.customer.command.event.CustomerCreatedEvent
 at com.thoughtworks.xstream.security.NoTypePermission.allows(NoTypePermission.java:26)
 at com.thoughtworks.xstream.mapper.SecurityMapper.realClass(SecurityMapper.java:74)
 at com.thoughtworks.xstream.mapper.MapperWrapper.realClass(MapperWrapper.java:125)
 at com.thoughtworks.xstream.mapper.CachingMapper.realClass(CachingMapper.java:47)
 at org.axonframework.serialization.AbstractXStreamSerializer.classForType(AbstractXStreamSerializer.java:171)
 at org.axonframework.axonserver.connector.event.axon.GrpcMetaDataAwareSerializer.classForType(GrpcMetaDataAwareSerializer.java:75)
 at org.axonframework.serialization.LazyDeserializingObject.<init>(LazyDeserializingObject.java:83)
 at org.axonframework.eventhandling.EventUtils.lambda$upcastAndDeserializeTrackedEvents$1(EventUtils.java:107)
 at java.base/java.util.stream.ReferencePipeline$3$1.accept(ReferencePipeline.java:212)
 at java.base/java.util.stream.ReferencePipeline$3$1.accept(ReferencePipeline.java:212)
 at org.axonframework.axonserver.connector.event.axon.EventBuffer$SimpleSpliterator.tryAdvance(EventBuffer.java:216)
 at java.base/java.util.stream.StreamSpliterators$WrappingSpliterator.lambda$initPartialTraversalState$0(StreamSpliterators.java:292)
 at java.base/java.util.stream.StreamSpliterators$AbstractWrappingSpliterator.fillBuffer(StreamSpliterators.java:206)
 at java.base/java.util.stream.StreamSpliterators$AbstractWrappingSpliterator.doAdvance(StreamSpliterators.java:169)
 at java.base/java.util.stream.StreamSpliterators$WrappingSpliterator.tryAdvance(StreamSpliterators.java:298)
 at java.base/java.util.Spliterators$1Adapter.hasNext(Spliterators.java:681)
 at org.axonframework.axonserver.connector.event.axon.EventBuffer.peekNullable(EventBuffer.java:171)
 at org.axonframework.axonserver.connector.event.axon.EventBuffer.hasNextAvailable(EventBuffer.java:144)
 at org.axonframework.eventhandling.TrackingEventProcessor.processBatch(TrackingEventProcessor.java:419)
 at org.axonframework.eventhandling.TrackingEventProcessor.processingLoop(TrackingEventProcessor.java:316)
 at org.axonframework.eventhandling.TrackingEventProcessor$TrackingSegmentWorker.run(TrackingEventProcessor.java:1197)
 at org.axonframework.eventhandling.TrackingEventProcessor$WorkerLauncher.cleanUp(TrackingEventProcessor.java:1396)
 at org.axonframework.eventhandling.TrackingEventProcessor$WorkerLauncher.run(TrackingEventProcessor.java:1373)
 at java.base/java.lang.Thread.run(Thread.java:1570)

 * If you see these logs, for the Profile ms, someone is dispatching the CustomerDataChangedEvent - haha! we know who is that someone (Customer ms). But we are getting a security related Exception. Why this is happening - my instructor knows! we will visualize how to fix it. Bu before that, lets spend some time understanding this Exception. As of now, what is happening is - you are trying to dispatch an event from the customer ms to the profile ms. These 2 ms's are started under different JVMs or under different tomcat servers. So whenever you are trying to send an object from one application to the other application over the network, what is going to happen? Serialization of the object is going to happen. For Security reasons, by default the sharing of objects from one service to another service is not allowed whenever we are using the Axon framework. To fix this issue, we need to tell to the axon server that please allow serialization of my Event objects so that the multiple ms's they can they can dispatch and consume the event objects based upon the actions of the end-user. So, we can try to communicate the same to the axon server. To configure the same, we need to go to the common module/project and here I am going to create a new configuration class. Reason: Instead of defining this configuration class in all the ms's separately, what we can do is - we can try to create this under the common project/module and the same we can import inside all the ms's. So, create a new package "config' and under this new package create a class i.e., "AxonConfig" - Check this class out for more details.
 * Once we have @Import(AxonConfig.class) in all the ms's, do the build of all your services i.e. ctrl + f9 and see if the dev-tools is smart enough to detect these changes without having to restart your services over again. Now, tyr to clean the console of profile ms and try to create a new customer with a new mobile number - or you can just start over btw. This time under the profile ms, hurrey! we don't have any RTE which confirms that the data might have changed behind the scenes inside the profile MV/DB. We can also validate the same by going to application.yaml of profile ms - Here you can try to connect to that datasource by clicking on the DB icon on the LHS side of the datasource configurations - otherwise on the RHS Nav is a DB icon with which you can do the connection to this DB explicitly. Now, if you open the tables that you have under this profile DB, you should be able to see that we have a table with the name profile which if you query, you will be able to see that there is a record inside the DB with a given mobile number, the name is also populated successfully. The loan number, card number and account number are populated with the default value which are '0'.  This confirms that customer ms is dispatching the event successfully . We can also validate if the customer ms is dispatching the customer updates/modify as well i.e., update and delete operations. So, using the sme mobile number I will try to fire the fetch customer request - copy the response body and paste it inside the update customer request. Here, we will modify the name from "Colince Temoi" to "Colince Linus Temoi" and finally fire the send button in postman. Yes it was successful and this time if you refresh the profile MV/table this time you should be able to see the name field as "Colince Linus Temoi". So, this confirms that the customer ms is dispatching the customer updates/modify as well. Hope you have clearly visualized that every chage that happens on the source side, the same is being reflected in the MV as well. Now, lets try to create an account with the same mobile number, similarly, you can try to create a new card and a new loan as well - using the same mobile number. Now, if I try to invoke the fetch profile API under the profile ms in postman with the same mobile number you should be able to see I am getting the profile data i.e., name, mobile number, acct number, loan number and card number.
 * Now, lets try to delete the card and see if it is working fine or not. So, what you can do, you can try to fetch the existing card details using the same mobile number and then using the received card number in the response fire the delete operation. We ran into a RTE with the message "Name for argument of type [java.lang.Long] not specified, and parameter name information not available via reflection. Ensure that the compiler uses the '-parameters' flag." This we know how to fix - just make sure in your controller classes where you are using @RequestParam annotation and to it mention the respective param name you are sending as an input explicitly. Now, if you invoke the delete card API one more time, this time you should get a successful message. Now, if I try to invoke the fetchProfile again, this time you should see that the card number has a value 0 indicating that it has been deleted. Next, you can also try deleting the loan - 1st try getting the loan details and then using the loan number fire the delete operation. It was successful and now if you fire the fetchProfile request, you should be able to see that the loan number is 0. Same drill, test the same for accounts ms as well. With what we have visualized, you should now be crisp clear. You can do multiple rounds of tests with different mobile numbers to visualize more on how the sync between the source ms's and the MV are happening. Just like we have done - same drill! No rocket science here. For update card request their is nothing that we are going to update that will need to be synced to the MV because through my card ms I am only going to allow the updation of the amount used and available amount but these fields anyway we are not syncing to the MV. So, we are good to go here and hence no meaning to invoke the update card request. Same drill for loans and accounts ms as well. At last we can try to delete the customer as well. 1st fetch the customer using the same mobile number and then using the customer id fire the delete operation. Same drill! You should be able to see that the customer details have been deleted. If you now invoke the fetch profile request this time you can see a response i.e., "Profile not found with the given input data mobileNumber : '3254537623'". This is a proof that the data changes/actions at the source ms is/are synced to the MV accordingly. With the progress that we have made so far, you should be proud of yourself as you are now one of the very few ms developers who know how to implement the MV pattern.
 *
 * Use cases under which you can use this MV pattern happily
 * -------------------------------------------------------------
 * 1. Cross-Service Queries - As of now, above, we have seen/visualized one use case where we can use the MV pattern which is Cross-Service queries. Similarly, there are a good amount of scenarios where you can use the MV pattern which is not going to be possible for my instructor to spoon feed me all of them.
 * What my instructor will try to do is try to cover some of the most common scenarios where you can use the MV pattern. With this knowledge in future whenever you have some problem inside the ms's, you can always think for a second whether that problem at hand can be solved with the help of MV pattern. If yes! then you can happily adopt the same.
 * 2. Sharing/duplicating the data across ms's - To explain about this scenario, consider you have 2 different ms's i.e.,accounts and customer ms. Inside the customer ms, I have customer details, address details and many other details. Now, inside the accounts ms, there are some scenarios where they are going to look for the address details of the customer. For example, think like this accounts ms has an API which is /dispatchCB - CB means check book. To dispatch the check book definitely the accounts ms, they are going to need the address details of the customer. So at runtime, inside the accounts ms what they can do is - they can make a call to the customer ms to read the address details(This is just one basic approach). The other approach is - inside the accounts ms itself, we can maintain the copy of address details for all the customers - this way this accounts ms doesn't have to rely on the customer ms to fetch the address details. So, these address details copy of data in the accounts ms we are going to maintain with the help of MV pattern. Whenever a change around the address details is happening inside the customer ms, the customer ms is going to dispatch an event which is going to be handled/consumed on the accounts ms and update the respective address details MV table at the accounts ms. With this the accounts ms will never have to invoke the customer ms to fetch the address details.
 * 3. The 3rd most common scenario where this MV pattern can be used is during the Reporting or Analytics scenarios - lets imagine you have a requirement to generate some daily/weekely/monthly report based upon the data present inside the multiple ms's. There are 2 ways on how we can achieve this. The very first basic approach is - inside this analytics or reporting processing, they can invoke the actual ms's and the data can be transformed, aggregated - nothing but all the complex business logic you can try to execute in real-time but this is going to demand lot of computation power and you are also going to make your ms's busy because this reporting and analytics processors they are trying to invoke the APIs exposed by the ms's. So, that is a very basic approach which my instructor doesn't recommend me to follow. Instead what you can do is - you can tell all your individual ms's to publish the data that are interested by the reporting and analytics processors into a MV pattern table. So, just like how we have mainatined profile table, very similarly we can maintin a separate table for daily/weekly/monthly reporting and for the analytics purpose as well. This way we can add any number of Materilized View Tables which are going to have ready made data in the required format. Like this, when the data is readiliy avaible the reporting and analytics processors are going to be super quick with very less computation required. They can directly read the data from the MV tables and generate the reports.
 * So, the above discussed 3 are the most common scenarios my instructor could think of -  but like we discussed, MV pattern can be used in many places. With the knowledge that you have gained you should right now be in a better position to decide when to use this MV pattern inside your projects. Consider all you have learnt as stepping stone knowledge. You now have all it takes to use the MV pattern in your projects.
 *
 * * * */


@SpringBootApplication
public class GatewayserverApplication {

    public static void main(String[] args) {
        SpringApplication.run(GatewayserverApplication.class, args);
    }

    @Bean
    public RouteLocator eazyBankRouteConfig(RouteLocatorBuilder routeLocatorBuilder) {
        return routeLocatorBuilder.routes()
                .route(p -> p
                        .path("/eazybank/customer/**")
                        .filters(f -> f.rewritePath("/eazybank/customer/(?<segment>.*)", "/${segment}")
                                .addResponseHeader("X-Response-Time", LocalDateTime.now().toString()))
                        .uri("lb://CUSTOMER"))
                .route(p -> p
                        .path("/eazybank/accounts/**")
                        .filters(f -> f.rewritePath("/eazybank/accounts/(?<segment>.*)", "/${segment}")
                                .addResponseHeader("X-Response-Time", LocalDateTime.now().toString()))
                        .uri("lb://ACCOUNTS"))
                .route(p -> p
                        .path("/eazybank/loans/**")
                        .filters(f -> f.rewritePath("/eazybank/loans/(?<segment>.*)", "/${segment}")
                                .addResponseHeader("X-Response-Time", LocalDateTime.now().toString()))
                        .uri("lb://LOANS"))
                .route(p -> p
                        .path("/eazybank/cards/**")
                        .filters(f -> f.rewritePath("/eazybank/cards/(?<segment>.*)", "/${segment}")
                                .addResponseHeader("X-Response-Time", LocalDateTime.now().toString()))
                        .uri("lb://CARDS"))
                .route(p -> p
                        .path("/eazybank/profile/**")
                        .filters(f -> f.rewritePath("/eazybank/profile/(?<segment>.*)", "/${segment}")
                                .addResponseHeader("X-Response-Time", LocalDateTime.now().toString()))
                        .uri("lb://PROFILE")).build();


    }


}
