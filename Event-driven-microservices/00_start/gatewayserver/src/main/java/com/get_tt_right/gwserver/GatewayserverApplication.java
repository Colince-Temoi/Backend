package com.get_tt_right.gwserver;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
import org.springframework.cloud.gateway.route.RouteLocator;
import org.springframework.cloud.gateway.route.builder.RouteLocatorBuilder;
import org.springframework.context.annotation.Bean;

import java.time.LocalDateTime;

/** Update as of 05/4/2026
 * -------------------------
 * Data Consistency and Complex Transactions Challenge in ms's
 * -------------------------------------------------------------
 * We discussed in the slides on a high level about the 4 major challenges we are going to face is ms's environment i.e., Cross-Service Queries (This we have taken our time and discussed in detail on how to overcome it with hands-on implementation and visualization step by step), Data Consistency, Complex transactions and data duplication. These challenges arise because our ms's are going to adopt database per service pattern. We already discussed in detail about cross-service queries challenge and as part of the same discussion we understand that there are two approaches that we can use to overcome this challenge. The very first approach is by implementing API Composition pattern - we visualized the demo step by step on how to implement this API Composition pattern by using the Spring Cloud Gateway project. The next option that we have is with the help of CQRS pattern. By implementing CQRS pattern inside our ms's we should be able to solve this challenge of Cross-Service Queries.
 * Now inside this session we will try to understand the next 2 challenges that we may face inside ms's environment whenever we adopt database per service pattern. These 2 challenges are around Data Consistency and Complex Transactions. We will try to understand these 2 challenges by taking examples. Check slides for this discussion.
 * The last challenge that we need to discuss is data duplication - check slides for detailed discussion.
 * With all we have discussed, you should be crisp clear about all the 4 challenges that we have discussed in detail around the DB per service pattern. If you try to summarize all we have discussed to resolve all these 4 challenges then we need to adopt various design patterns i.e., CQRS pattern, Saga pattern, Event driven architecture. Apart from the 3 mentioned patterns we also have other patterns i.e., Event Sourcing pattern which we can use along with the CQRS. If you combine both CQRS patterns and Event Sourcing  you are going to get many other advantages which we are going to discuss in the coming sessions.
 * These patterns we have mentioned is a deadly combination that most of the enterprise organisations are using these days to handle the distributed transactions inside their ms's. We are going to discuss in detail about all these patterns, and it is recommended to implement all these patterns (CQRS + Event Sourcing and Saga patterns) by following the Event driven architecture.  It is not recommended or even possible to implement these patterns inside traditional ms's that use REST APIs and synchronous calls for communication. We should always implement these patterns by using event driven architecture. We discussed in detail about event driven architecture in our ms's sessions where with the help of Spring Cloud Functions, Spring Cloud Stream, Kafka and RabbitMQ we visualized the demo on how to build ms's that adopt event driven architecture - You can always go back to the docstring you generated while we were discussing this for a refresher. But also we are going to remind ourselves on a high level around the same in the coming sessions but not as in details as we documented earlier in our learning. We will be focusing more on the advanced design patterns like CQRS, Saga and Event Sourcing.
 *
 * Introduction to CQRS Pattern
 * -----------------------------
 * We are going to talk in detail about the CQRS design pattern and the Event Sourcing design pattern. These 2 design patterns are going to complement each other. We will in detail understand these 2 patterns from the theory perspective and once we are clear with these patterns we will later on implement them.
 * The full form of CQRS is - Command Query Responsibility Segregation. Anytime you want to build a business logic inisde your ms's or inside any web application, we usually write the logic to either update the data or read the data. When we talk of update data it can be insert or delete or modify/update the data. When we talk of read the data means we can only query the data without modifying it. So all the operations or business logic that we write inside ms's will either fall into the update operation or read operation. So what this CQRS  pattern recommends is - Separate the write APIs and read APIs into 2 different individual components. Apart from building 2 different individual components handling the update and read operations, this pattern also recommends to have separate DB's for write and read purposes. That's why we have this pattern name i.e., Command Query Responsibility Segregation whereby Command means - An operation that can Alter/Modify/Create/Delete the data. All such operations we can call them as Command. Whereas Query means we can only perform reading of the data or querying of the data from the DB. So the Command and Query Responsibility it is recommended to Segregate separately. In the Slide you can see an illustration of the command part and the query part. Anytime if someone invokes the API that alters the data then it will be handled by the Command component. Once the business logic is executed the data will be written into the Write DB or Command specific DB. On the other side if anyone wants to read the data they are going to invoke the APIs exposed by the read component or the query component and this Query component it is going to read the data from the Read DB.
 * Here, since we are trying to maintain 2 separate DB's - one for writing purpose and the other one for read purpose. Obviously the question will arise which is - How are we going to sync these 2 DBs? whenever the new data or updation of the data is happening. For the same purpose we are going to have a middle component which is event bus. This Event Bus can be any component like Kafka or RabbitMQ or any other product which is available inside the market. Whenever the data updation is happening inside the write DB, this Command Component is going to publish an event into this Event Bus. As soon as the event is published into the Event Bus, the Read component or the Query component is going to read the event from the Event Bus and accordingly it is going to update the read DB. In future if someone is trying to read this data through the read APIs, then definitely the data will be fetched from the Read DB and since both these DB's are in sync there will be no issues. Of course the sync between these 2 DBs is not instant - there will be some nanoseconds/milliseconds/seconds difference to sync these 2 DBs. Basically this kind of sync mechanism where the sync is not happening instantly we will call it as Eventual Consistency. Nothing but the data will be eventually updated on the other DB maybe withing nanoseconds or milliseconds or seconds. You're maybe having many questions around what advantages we have by separating Command and Query Responsibilities and having different DBs for each component. We are going to talk in detail about the advantages in the coming sessions. For now, we will see few of the most important advantages that we get by implementing this CQRS pattern. The very first advantage is if your organisation is going to perform a lot of read operations, then you can choose the read DB that is optimized for read operations and if needed you can also you can also scale this read DB to any extent based upon your read operations.
 * And you can also choose the right DB that is suitable for the minimum write operations. One more advantage that you get while implementing this pattern is, you can have your write DB as NoSQL DB or any Document based DB whereas the read DB can be a relational DB. Maybe, your application processes a lot of documents, a lot of JSONs - all these documents and Jsons you want to store inside write DB but for someone to read the data you don't want to send the data present inside this write DB in a Json format or document format. Eventually, by the time you store the data inside the read DB you can write some transformation logic where you are going to store all the data in a relational DB style. With this set up you are going to make read operations much simpler. The next advantage that we have with this CQRS pattern is - it is going to help you to handle the Cross Service Queries efficiently. Long back if you can recall, we resolved the Cross Service Queries challange by using the API Composition pattern. The same challange can be efficiently resolved by adpoting CQRS design pattern. Lets's imagine you have multiple ms's like customer, accounts, loans and cards all of which are storing their own specific data in their respective DBs. But now you have a UI page where for an end user or customer you want to show a sub-set of data from all those ms's - for example, from customer ms you want to display the profile information i.e., name, email, photo of the end-user, mobile number, address details. From the accounts service you want to display only the current balance inisde the account, from the loans you want to display loan summarry and from cards you want to display the card summarry. If you don't have CQRS pattern implemented then means you are going to follow the API composition pattern where the gateway is going to call all these services individually and it is going to aggregate the data and the aggregated data will be sent to the UI application. But this has some drawbacks like, performance issues, and you are going to increase the traffic inside your ms's network unnecessarily.
 * But the same problem can be solved very efficiently with the help of CQRS. All the mentioned ms's are going to have their own respective individual DBs storing their own specific respective data. During specific requests to these ms's once the processing and storage/updation/writting of data to the respective DBs of these ms's an event will be published to the Event Bus saying that so-and-so customer or account or card or loan data is written/updated. Now on the other side we have a query component which is responsible for the query operations. So, what this component is going to do is - it is going to read the events from the event bus and update the data received into a read DB. So this DB is going to act only as a Read DB where we have a dedicated table that is going to adhere to the requirements of Customer summary UI page. So, from this UI page the call/request will go the Fetch Customer Summary service API and this API instead of reaching out to all the individual ms's for the specific required data, what it is going to do is - it is going to simply perform the read operation from the customer summery view DB. With this we are simply trying to avoid many network calls at run-time from the UI/Client application. Here you may have a very valid question which is - where is this FetchCustomerSummaryService query component going to be deployed? It can be deployed anywhere - it can be deployed as a separate component, or it can be deployed as part of the customer or accounts or cards or loans ms. Basically, with this pattern what we are tyring to do is - we are separating the write and query operations. Anytime any client who wants to read the data they should reach out to the query component whereas for the command operations (Create/update/Delete), they can reach out to the individual ms's. With all we have discussed above you should now be crisp clear about the introduction on this CQRS pattern. This pattern is mandatory for enterprise applications where they are processing millions of transactions on day-to-day basis. If you try to have a same DB for both write and read then definitely we are going to get some performance issues and that's why it is always recommended to use CQRS pattern to separate command APIs and Query APIs.
 * When you combine CQRS and Event Sourcing pattern, then it is going to be even more powerful - we will be discussing what is Event Sourcing in the coming sessions. But right now you have gotten some clarity that this CQRS pattern can only be implemented with the help of Event driven architecture because there is a mediator between the Command and Query components which is Event Bus due to which the syncing between the 2 different DBs - Command specific DBs and Query DB - is always going to be eventually,"BUT NOT INSTANTLY!", happen. Of course there are some options where we can avoid this Event Bus and implement this CQRS without the Event driven architecture - but those options are not going to be super helpful and are going to have some challenges. All this we will discuss in details in the next sessions.
 *
 * Advantages and disadvantages of CQRS pattern
 * ---------------------------------------------
 * Check slides for this.
 *
 * Introduction to Event Sourcing Pattern
 * -----------------------------------------
 * Check slides for detailed theoretical discussions.
 *
 * Introduction to AxonIQ
 * -------------------------
 * We want to implement CQRS and Event Sourcing Patterns inside our ms's.  Building these patterns from scratch is a very complicated task but for us we are going to stay smart and take a much simpler approach as usual! haha! Reason - At the end of the day how you implement this pattern does not matter. It is the knowledge that you gain from these patterns that is going to matter inside your real projects. Different organisations may have different approaches in implementing these patterns and that's why for you what is important is to try to understand these patterns and what are the differences and advantages that these patterns are bringing to the table inside our ms's environment. Now, let's try to break down on how we are going to implement these patterns inside our ms's.
 * As of now, inside our ms's all types of operations we are trying to carry out with a single DB. But when we try to implement CQRS pattern, the very first task that we need to complete is, we need to separate our APIs into write APIs and Read APIs so that anyone who wants to alter the data they are going to invoke the write APIs and the change(s) will eventually go to the write DB. Whereas if someone wants to read the data they are going to invoke the read APIs and the data will be fetched from the read DB. When we want to write the data into the write DB, we are going to follow the Event Sourcing pattern. which means that as part of the step 2 we need to enable the Event Sourcing while we are trying to write the data into the write DB. All the transactions that we are going to perform on a single record, they are going to be stored in a historical style or in an audit style.
 * As part of the 3rd step, whenever a change happens on the write DB side, we need to make sure we are triggering an event to the event bus. There is an event bus which is going to sit in the middle - as soon as an event is published into this event bus, from the read side we need to consume the events. Once this event is consumed we need to update the data inside the relational DB and the same will be used by the read APIs whenever someone is trying to query the data. So, these are the most important steps that we need to address while we are implementing the CQRS and Event Sourcing patterns. It may somehow look complex but with the approach that we are going to take it is going to be super simple. Before we introduce the approach that we are going to take, first, let's discuss a real life analogy that we can always relate with the CQRS pattern. Ever went to a restaurant? For example, inside a pizza restaurant, we will be having 2 kind of people - Chef => The person responsible to prepare the pizza. Whereas on the other ide we are going to have another person who is going to take orders from the customers. If you closely look at this restaurant operation, the responsibilities are segregated into 2 different kind. One is making the pizzas and the other one is delivering the pizzas to the customers by taking their orders once they enter into the restaurant. Of course for a very small restaurant where they are going to get very limited number of customers, then definitely the same single person can play both roles of making the pizzas and delivering the pizzas based on taken orders from the customers. But for a bigger restaurant it is really impossible for a single person to manage bot these 2 roles.
 * The same applies for our enterprise ms's as well - sometimes we will be having scenarios where implementing traditional ms's may not be a solution. So, for such kind of scenarios we are going to follow the CQRS approach. Just like we have 22 kind of persons inside a restaurant, same way inside our ms's we are going to have command side and query side. Hope everything is crisp clear and always remember this analogy to remember CQRS forever. If someone asks you what is CQRS then you should immediately recall this analogy/scenario and accordingly you need to answer your question. Now, let's dive into how we are going to implement this pattern inside our ms's. We are going to take help from the AxonIQ framework/product which is available inside the market.By going to their site i.e., https://www.axoniq.io/ you are going to read more details about this product. We are going to implement CQRS,Event Sourcing and Saga patterns with the help of AxonIQ product. This is a very mature product available inside the market which is going to help you to implement event driven patterns like CQRS, Event Sourcing and Saga patterns very efficiently. You don't have to start from scratch - you can always leverage this product/framework which is going to help you to implement Event Driven Patterns very easily and efficiently. It is not a free product and if an organisation is very serious about their ms's, they need to buy this product to use it. For our local development and exploration of this product and learning about this product, it is going to be completely free. The intention here is not to force you to use this product but to explain to you how the event driven patterns are going to work in real ms's. And like we saw before, few organisations are going to implement these patterns from scratch which may work for them but other organisations are going to prefer to use the ready products like AxonIQ. Whenever you have some questions about this product you can always go to their official website and go to the documentation i.e., Developers >> Documentation - https://docs.axoniq.io/home/
 * Inside the documentation website you will have references around the Axon Framework, Axon Server, AxonIQ console - all of which you can navigate to under the references' dropdown. We are going to explore a lot about this Axon throughout our sessions. But on a high level if you ask me what does this product contain? This product contains 3 different important components. The very 1st one is Axon Server - this server is going to provide an out-of-the-box message routing and an event store for message driven applications. Basically, this server is going to provide the required infrastructure to implement event driven patterns like CQRS. The next component is the Axon Framework - This has all the libraries, source code that can be leveraged by the developers to implement the event driven patterns. At last, we have a component which is AxonIQ console - this is an optional component. We can use this console component to monitor our ms's and how the events are being processed. So all monitoring, managing, controlling and reporting stuff we can do with the help of this AxonIQ console. Anytime if you have any questions around this product you can navigate to the Developers dropdown >> Community. This will take you to the website https://discuss.axoniq.io/ this is like Stack-Overflow for Axon. Here you will find lot many questions and answers that are posted for many years. Anytime if you are faced with any blocker around this product then that is the very first place you should explore for a solution. With all we have discussed, you should be clear with the plan, and you are excited.
 *
 * Axon Server Setup locally using Docker
 * -------------------------------------------
 * As we were discussing previously, Axon Server is an important component inside the AxonIQ product which is going to provide the infrastructural capabilities to implement the Event driven patterns. So, using the docker we are going to setup this axon server component locally. The steps that we need to follow to set up this product are going to change very frequently and that's why what my instructor has done is - he has documented all the steps that we need to follow inside his GitHub repo. Since we are in Section 3 - Inside his GitHub repo, you can click on it and if you can scroll down you should be able to see the ReadMe document where he has mentioned all the steps that we need to follow to set up Axon server locally using docker. This was like 2024, and we are in  2026 - we will try to follow the same steps that my instructor documented in the readMe in 2024 - he goes ahead and says that in future we need to check that readMe document so that if there are any changes he is going to update it and by following that readMe document you need to set up the Axon Server locally inside your system as well.
 * So as the first step we need to create the Axon Server folder Structure. So, first we need to create a folder with the name 'axonserver' at any location - but my instructor prefers you create it on your desktop. Next, you need to create 3 different sub-folders namely, 'config', 'data' and 'events'. Next, inside the config folder you need to create a file with the name 'axonserver.properties' - Make sure to follow this naming convention STRICTLY because axon server is going to look for this exact file name. Next, open the file and paste the properties as documented by your instructor i.e.,
 server.port=8024 # With this, I am telling to the axon server to start at that mentioned port number. Actually, that is the default port number but we are trying to re-initiate/define the same explicitly one more time.
 axoniq.axonserver.name=GetttrightBank Axon Server # Next, we are trying to provide a name to our axon server
 axoniq.axonserver.hostname=localhost # This property is related to the hostname. Here we are mentioning 'localhost' because we are trying to set up the server inside our local system.
 axoniq.axonserver.devmode.enabled=true # Next, we are trying to enable the dev mode by mentioning true against to this definition. Since we are exploring this product inside our local system, we should always make sure we have enabled this. If you don't enable dev mode then it is going to be considered as a production environment and with that you may need to handle lot many production-ready set up standards. In real-applications also, this entire axon server related set up is also going to be taken care of by the infra people or DevOps people. As a developer you don't need to worry about how to set up this axon server. We are just trying to provide all these steps so that you at least can be able to set up this server inside your local system for your exploration purpose.
 *
 * As can be noted above, most of the properties related to the AxonIQ product  are going to start with the prefix which is 'axoniq' but server dot port is different because at the end of the day axon server is also built by using Spring and Spring boot and that's why we are trying to follow the same Spring Boot property here i.e., server.port. Other properties which are going to be leveraged by the AxonIQ , they are going to have a prefix which is 'axoniq'. My instructor also mentioned what is the purpose of each of these properties in the readMe document. If you have any question you can always check with that readMe doc. Next we need to run the docker command i.e.,
 docker run -d --name axonserver \
 -p 8024:8024 -p 8124:8124 \
 -v "/Users/pc/Desktop/axonserver/data":/axonserver/data \
 -v "/Users/pc/Desktop/axonserver/events":/axonserver/events \
 -v "/Users/pc/Desktop/axonserver/config":/axonserver/config \
 axoniq/axonserver

 * This will set up the axon server inside our local system. We already know that -d indicates we want to run/start this command in a detached mode and using the --name flag we are trying to give the value/name 'axonserver' as a name to our docker container. Using the -p flag we are trying to provide the port mapping. Axon server is going to start at 2 different ports - the very first one is 8024 which is going to be used for the Http ineterface, for example if you want to access the Axon Server dasgboard we are going to leverage this 8024 port. The other port is 8124 is a port used for the gRPC interface that Axon framework applications use to communicate with the within the server. That's why we need to provide the port mapping for both 8024 and 8124, but we are always going to use 8024 to access the dashboard of the Axon server.
 * Next, using the -v flag, we are trying to provide the volume configurations. Volume is a concept inside docker using which we can mount a local folder to our docker container. If you see the configurations, they are nothing but pointing to the sub-folders that we have created previously i.e., data, events and config. These local folders - on the LHS side of the ':' I want to mount them to the docker container that I am going to create - So, inside the docker conatiner that I am going to create it is going to have the folders like can be visualized on the RHS of the ':' in the volumes configurations. But whatever data that my container is going to write into these folders(As defined on the RHS) it is going to be written into our local defined folders (As defined on the LHS). Basically, with these volume configurations what we are trying to achieve is, even if we delete or restart our Axon server container, we don't want to lose all the data that we have stored indie our container. Since with the help of these volumes we are trying to store all the data inside our local system, in future whenever we set up a new container or whenever we are restarting our existing container, it is always going to read the data from our local folders that we have defined as can be visualized in the volumes configurations, nothing but '-v' configurations in the command above.
 * Under the data sub-folder, Axon server is going to store all its internal data. Under the events sub-folder, Axon server is going to store all the events that are going to be published as part of our business logic. config is a sub-folder under which we are going to provide all our configurations. Inside this config folder only we have defined a properties file with the name axonserver.properties. Using this properties file we are trying to convey our required configurations to the container. At last we need to mention what is the docker image name i.e., axoniq/axonserver. So, the above entire command that we have in detail discussed you can copy it and ry to execute it in your terminal. Throughout our sessions we are going to use one beautiful terminal called 'Warp' - If you type it on Google you can go to the website which is https://www.warp.dev/ - This is an intelligent terminal which is going to work by leveraging Artificial intelligence. Any time if you have questions around the commands, you can always ask those questions inside this terminal itself. Overall, it is a nice terminal! leverage it, otherwise you can use the normal terminals that you are used to that are part of your OS.
 docker run -d --name axonserver `
    -p 8024:8024 -p 8124:8124 `
    -v "C:/Users/pc/Desktop/axonserver/data:/axonserver/data" `
    -v "C:/Users/pc/Desktop/axonserver/events:/axonserver/events" `
    -v "C:/Users/pc/Desktop/axonserver/config:/axonserver/config" `
    axoniq/axonserver
 Unable to find image 'axoniq/axonserver:latest' locally
 latest: Pulling from axoniq/axonserver
 b40150c1c271: Already exists
 a2c74f5c2809: Already exists
 3b9a1e97487b: Already exists
 2ef33ef1b993: Already exists
 e9d29f6a4c60: Already exists
 01708da6b879: Already exists
 c5ec858e52de: Pull complete
 4f4fb700ef54: Pull complete
 Digest: sha256:d0b19f6b65af91835b45fbf95c00e2c27c14ceaddfecda88bf449fe86be832c4
 Status: Downloaded newer image for axoniq/axonserver:latest
 a4efa6ea6908e7ce0b393321fd7a218284f2e10a49daba179f1fd9380b93fe3e

 * As soon as I executed this command in the warp terminal, as can be seen the container got created. This you can also verify in your docker desktop. If you click on the container 'name' you will also be able to verify that our container started happily! Since Axon server is also build by using Spring boot you can be able to notice that it is trying to follow the similar style of logs. Also you can confirm that the Axon server started successfully by trying to access the url which is - http://localhost:8024/. You should be able to see the Axon Server dashboard. Since we set up this container very first time, if you sroll down on the landing page you should be able to see a 'Complete' button. Make sure to click it to complete the installation. Before clicking on it, you will notice a dropdown labelled 'Initialize action' with various options. Here you need to select the 'Start standalone node' option which means we are trying to start the axon server as a single node inside our local. We can also set up this axon server in a multi node cluster style as can be seen on the options which is an expectation for enterprise applications inside the production environment - we don't have to worry much about this as the DevOps team are going to take care of this in production environment. So, once you select the option, 'Start standalone node', proceed to click the 'Complete' button and with that, the set up of your axon server is going to complete.
 * As of now you can see on the dashboard that their is a context with the name '_admin'. So, yea, this is the context that is going to be used by the axon server itself. For my instructor he was seeing '_admin' and '_default' - not sure why I didn't see the 'default' context? But maybe it because of the warning message I am seeing on the dashboard i.e., 'Running in unregistered mode. Trial access is limited to 12 hours. Register at platform.axoniq.io to continue.' Once our ms's start registering with this axon server, we will be able to see our ms's as well here in the dashboard. Hurreey! we were successful in setting up this axon server inside our local by following all the discussed steps. Without setting this inside your local system you will not be able to practice all the demo that we are going to discuss throughout our sessions.
 *
 * Adding Axon framework dependencies inside ms's
 * ---------------------------------------------------
 * By setting up Axon server inside our local system means that now we have the required infrastructural component required to build event driven patterns. As a next step, we should also learn on how to leverage Axon framework to make the code changes. Inside this session we will try to set up the required libraries inside our ms's - as discussed by my instructor in his Section3 source code. Since we are looking to leverage the Axon framework libraries, we need to define the framework and it's respective version details inside our getttright-bom - if you open it's pom.xml, here we are trying to maintain all the versions of the libraries that we are trying to use. In the same pom.xml we need to define the Axon framework related details as well. 1. under the properties tag,I am creating one more property with the name 'axon.version' with a value 4.10.1. To tell the version series, it is very simple - just go to the Google and type 'axon maven dependency' and open the very first maven repository link/website and here you can look for the module name which is 'Spring Boot Starter Module For Axon Framework' and here you will be able to see that the latest version is 5.0.x series. Use whatvere version that your instructor has mentioned in the GitHub repo - don't try to use the very latest version that you see inside the maven repository because sometimes the latest versions may have some stability issues. That's why every few month your instructor will always try to understand what is the new version, what are the new changes that are introduced - once tested and satisfied with the new version then he is going to update the entire code inside the GitHub repo. So, be checking for any updates in your instructor's GitHub repo and align them in your implementations. This not only for the Axon version but also for all the other versions mentioned under the properties inside the getttright-bom pom.xml. Once we have created this axon.version property, we need to import the pom file of the axon framework - just like we have done for  'spring-boot-dependencies' dependency that is under the dependencyManagement>>dependencies element. So, towards the end under this element 'dependencyManagement>>dependencies' paste the new dependency i.e,
 <dependency>
    <groupId>org.axonframework</groupId>
    <artifactId>axon-bom</artifactId>
    <version>${axon.version}</version> <!- Coming to the version we are referring to the variable axon.version --></!->
    <type>pom</type>
    <scope>import</scope>
 </dependency>

 *If you navigate to the artfactId by ctrl + clicking on it, you will notice that inside that bom of axon you will be able to see that there are many dependency details defined here - our ms's are not going to need all these dependencies - whatever they require they are going to define such dependencies under their respective pom.xml. Now, we have successfully added the bom of axon framework inside our getttright-bom. The same steps my instructor has mentioned inside the Section3 readMe as well just after the Axon Server set up locally using docker steps. In the readMe, apart from what we have discussed and actioned already, he also mentions what changes we need to make under the common project. For now don't worry about those changes as we will in detail discuss and action them in the coming sessions. Now, as a next step, we need to define/mention the required dependencies inside our actual ms's. We have a total of 4 ms's i.e., accounts, cards, customer and loans. The other projects i.e., eurekaserver, gatewayserver and getttright-bom are nothing but supporting components. We will step by step visualize on how to implement CQRS pattern - those are the SAME steps you will also need to follow in your future implementations of CQRS pattern. By the end of all our discussions you should be confident and crisp clear enough to implement the CQRS and Event Sourcing Event driven patterns. If any doubts, you have your detailed step by step docstring + Official docs + your instructors GitHub repo + AI - haha! where you can always get any help to clear your doubts. Now, if you open the Customer ms/project that is present inside the section 3 in your instructors GitHub, you will see a readMe file as well where you can get all the steps that you need to follow to implement CQRS and Event Sourcing patterns inside your ms.
 * First we need to add the dependency i.e., axon-spring-boot-starter inside the customer ms pom.xml. This is the only dependency that is required by the customer ms. You also need to add the very same dependency in all the remaining ms's as well i.e., accounts, cards and loans. Also, add this dependency in the common module as well - as in the coming sessions, we are going to make some changes inside the common project as well which will be the common changes for all the ms's that we have. We don't have to add this dependency inside the eurekaserver and gatewayserver because we are not going to implement any CQRS pattern inside these supporting components. Now you can go to the Maven tab on the RHS nav and reload all the maven projects, also try doing a build to make sure it is successful as well and no CE's.  With all we have discussed you should now be crisp clear on what we have done to add the axon framework into our ms's. This tells us that now we have the infrastructure required in the form of axon server and we also have the required libraries set up inside our ms's. As a next step we should now start implementing our actual business logic as nothing is going to work on the fly magically! hhaha!
 *
 * Creating Command Classes inside Customer ms
 * -----------------------------------------------
 * We need to add a property inside application.yaml file of our ms's. In the application.yaml file of customer ms - towards the end, I am going to create a property axon.axonserver.servers and as a value, I am going to mention the local axon server url which is localhost:8124. With this property mention/definition what we are trying to tell is - We are giving a clue to the customer ms to go ahead and connect to the axon server that we have spin up previously using the docker container. In the scenarios where we have cluster of axon servers means we should be able to mention all the server details here by using comma as separator. But since in our local we only have a single axon server, we should be good with these details. Let's learn about some intelliJ Ultimate version tip - whenever you are using intellij ultimate version you don't have to type the properties in the yaml file entirely - as long as you remember and type the very first letter of each word, intellij should be able to support you i.e., a.a.s and you should be able to see the property which is axon.axonserver.servers and the press tab to accept it and finally assign a value to it. This is applicable to any kind of properties. This same property mention/defined in the application.yaml files other ms's as well i.e., accounts, cards and loans. Like this, whatvere ms you are planning to implement event driven architectural patterns in it like CQRS, Event Sourcing, Saga etc. In all such ms's you need to make sure you are mentioning this property. By now, you should be able to have the maturity that it is not mandatory to convert all your ms's into Event driven ms'sby implementing CQRS - this should go hand in hand with your business requirements. If you have a requirement to implement CQRS pattern only in a single ms, do that! but don't force the other ms's to convert into event driven ms's. So, it is critical to take the decissions based upon your business requirements. In our sessions, we are trying to convert all our ms's into event driven because we want to learn and visualize the demo of CQRS, Event Sourcing, Saga patterns in detail. That's why we are trying to make this changes many times so that you can be comfortable and crisp clear about all the changes that we will be discussing in our sessions.
 * With all we have discussed we should be good from application.yaml changes perspective in our respective ms's. Next, inside the customer ms, we want to get started with the business logic around the CQRS implementation. Like we were discussing previously whenever you want to implement CQRS you need to build 2 different components supporting Command and Query functionalities, that's why what we are going to do is we are going to create new sub-packages - one with the name command where we are going to build all the classes related to the command functionalities. Very similarly, I am going to create one more sub-package with the name query inside which we are going to build all the classes related to the query functionality. As a next step, under the command package - we are going to create few classes and before that let's understand how CQRS is going to work one more time. Whenever someone wants to invoke a command operation they are going to invoke the REST API that we are going to expose as part of the command component. From end-user perspective they are always going to send the input in a Json format and one we recive the input data we can perform the validations and once we are happy with the input validations we need to initiate a command and this command will be processed by the axon server and during the process of this command the axon server is going to store the data received inside the write DB. Once the write DB DML is completed the axon server is going to trigger the event which will be consumed by the query side and on the query side this event is going to be processed and the data is also going to be updated on th read DB as well. To make this entire flow work without any issues we need to let axon server and axon framework know on what commands need to be triggered or on what events need to be triggered when a specific API ins invoked inside our ms. To provide such details, we need to create some command classes and event classes inside our project - that's why what I ma going to do is - under the command package, we are going to create a new Java class 'CreateCustomerCommand'. We know that our Customer ms is going to support creation of the customer, Updation of the Customer, Deletion of the Customer and Reading of the customer. The reading of the customer details will happen on the query side. For now on the command side, we need to worry about 3 kind of operations which are create, update and delete operations. That's why I am going to create 3 different command classes i.e., 'CreateCustomerCommand', 'UpdateCustomerCommand' and 'DeleteCustomerCommand'.
 * If ou closely visualize the naming convention of these classes, they are following some pattern i.e. First we need to mention the VERB (What action we are trying to perform i.e., Create, Delete, Update etc.) Followed by we need to mention the NOUN (What entity/object we are trying to create, update, delete etc. i.e., Customer, Loan, Card, Account, etc). Followed by the suffix Command. For example - CreateCustomerCommand, UpdateCustomerCommand, DeleteCustomerCommand, CreateLoanCommand, UpdateLoanCommand, DeleteLoanCommand, etc. This is the usual recommended naming convention that you need to follow for the command classes. So, always make sure that your command class indicates the present tense and not past of future tense. Now we have 3 different classes ready indicating 3 different commands i.e., CreateCustomerCommand, UpdateCustomerCommand and DeleteCustomerCommand. Inside those classes we just have to create/define/mention/declare the fields which are related to the object i.e. Customer, Loan, Card, Account etc. Check out those respective classes for docstrings. With all we have discussed now, we have now successfully created 3 different command classes that are going to be used by the axon framework. If closely look at these command classes, each of them represents the command APIs that we are going to build i.e., CREATE, UPDATE AND DELETE. Next, we will try to create very similar kind of POJO class(s) on the query side which we can use during the query operation. Just like how we have created 3 different classes on the command side, very similarly, we need to create a class on the query side as well. On the query side in our customer ms scenario, we will have a single read operation. In case if you have multiple read operations, then you need to create multiple query related classes.For now, I am only going to create a single class since we only have a single read operation. The class name I am going to mention is 'FindCustomerQuery' - check it's docstring for more details.
 * Now, if you closely look under the command package, we have 3 different classes - CreateCustomerCommand, DeleteCustomerCommand and UpdateCustomerCommand. On the query package/side we have a single class which is FindCustomerQuery. Each fo these classes represent an API operation that an end-user can invoke. Whatver data that end-user is sending in the respective requests we are going to transform that data into these command or query classes. As a next step, we should also create the event classes - Like we discussed before whenever a command is processed by the axon server and the data is stored inside the write DB as a next step, the axon server is going to trigger an event for the query side to update the read DB. To support the same functionality what we have to do, we have to create the classes related to the event and that's why under the command package, I am going to create a new sub-package with the name event because the command side only is responsible to trigger these events whereas the query side they are only repsonsible to process those events. To my instructor it only makes sense to create these classes under the command >> event package ✅ instead of query >> event package❌. But feel free to create this event package wherever you want. Under the event class I am going to create a new Java class with the name 'CustomerCreatedEvent'. Check out the docstring of this class for more details. Very similarly, lets create the other classes which are CustomerUpdatedEvent and CustomerDeletedEvent. Check their respective docstring for more details. With what we have discussed, you should now be crisp clear on why we have created these event classes. With this, you can safely assume we have created all the required POJO classes both on the command side and on the query side. As a next step, we can start developing the write APIs on the command side.
 *
 * Building Command APIs in Customer ms.
 * ------------------------------------------
 * Now, we need to build REST APIs that support all command operations like Create, Update and Delete. So, under the command package, we are going to create a sub-package with the name 'controller'. Inside this controller package I am going to create a new Java class with the name 'CustomerCommandController'. Inside this new class we need to build the REST APIs - check out the docstring of this class for more details.
 *
 * Technical flows of CQRS and ES
 * --------------------------------
 * # Check slides for detailed visualized discussion.
 * Aggregate is a component inside Event Sourcing which is going to encapsulate the stateand the behavior of a given record. These aggregate is going to process the events(incoming requests) and is also going to produce the events for the query side. Similarly, we have projections which are nothing but the read only views or models built from the sequence of events that are often used to query the current state of the record. More clarity about these aggregate and projections you will get when we start implementing them inside our ms's.
 *
 * Creating aggregate class in customer ms.
 * -----------------------------------------
 * To create an aggregate class, first I need to create a sub-package with the name 'aggregate' under the command package. Inside this sub-package I am going to create a new Java class with the name 'CustomerAggregate'. Check out the docstring of this class for more details.
 *
 * Creating Event SourcingHandler method in customer ms.
 * -----------------------------------------------------
 *  Now, we want to define the logic on how the event object data need to be stored inside the ES/write DB.  Check the docstring of this inside the CustomerAggregate class just after the constructor annotated with @CommandHandler for more details.
 * With what we have discussed so far, we have successfully completed all the required components on the command side to process the CreateCustomerCommand. But we also have other command like UpdateCustometCommand and DeleteCustomerCommand. For them also, we need to write their respective logic inside the CustomerAggregator on how those specific command objects need to be handled, how their respective events need to be triggered, and how the respective Event objects need to be stored on the write/ES DB - that we will do in the next sessions.
 * If you got any question don't worry as when we see/visualize this entire flow through a demo then it is going to make more sense to you. For now since we are building piece b few of the things may not be clear for you, but don't worry as eventual everything will make sense once we visualize the demo that is going to clear most of any questions/doubts that you may be having.
 *
 * Building Update and Delete Logic in Customer ms.
 * --------------------------------------------------
 * We will now write logic inisde our CustomerAggregate class to handle the UpdateCustomerCommand and DeleteCustomerCommand. Check out the docstring of this class for more details.
 *
 * Building Projection class in customer ms.
 * -------------------------------------------
 * To create the projection class on the query side, first under the query package, I am going to create a new sub-package with the name 'projection' inside which I am going to create a new Java class with the name 'CustomerProjection'. Check out the docstring of this class for more details.
 * With all the changes we have discussed so far, we should be good from the projection side perspective. So, this Projection is going to take care of reading the data from the Event objects published in the Event Bus and saving those to the Reader DB as we have visualized and as also illustrated in the slides. As a next step, we need to build the read APIs, and from the Read APIs we need to create a Query object and the same Query object we need to publish to the Axon framework which is going to eventually look for the Query handler and inside this Query handler only we need to read the data from the Reader DB and the response we need to send back to the client application. So, those three components required on the read side, we will build in the next sessions.
 *
 * Building Query API in customer ms.
 * -----------------------------------
 * To build the read API on the query side, we are going to create a new sub-package under the query with the name 'controller'. Inside this 'controller' sub-package, we are going to create a new Java class with the name 'CustomerQueryController'. Check out the docstring of this class for more details.
 * With all we have discussed, we have implemented all the required components and we should be good to get started testing with the entire flow of the CQRS and ES inside the customer ms. You should be over the moon! haha!
 *
 * Demo of CQRS nad ES in customer ms.
 * -----------------------------------
 * First, you need to make sure that the Axon Server container is running behind the scenes. We had started this container long back and deleted it - that's why what we are going to do is, make sure there is no data inside the data and events folders. If there is any contents in these 2 folders then please clear out everything because we want to resume from scratch.
 * Next execute the docker command to start the axon server in my local system. Once this container is created, we can validate if the container started successfully or not by by accessing lh:8024. Here you need to make sure you are clicking on the "Complete" button by selecting the "Start standalone node" option otherwise your ms's will not be able to communicate to this server.Now, under the Nodes and Applications you will see some icons which will be a confirmation that the set up of your Axon server is completed. As a next step, you can go to your user root folder and delete any h2 related DB files that are present because we want to start from scratch. Once all this is in place we can try to start our ms locally in ide. First you need to start the Eureka Server because these are all the supporting components and thus we need to make sure we start them before. Next, we are going to start the customer ms alone. We will not worry abut the other ms's right now because we are yet to make the code changes in those ms's.
 * For some reason our customer ms didn't start successfully and based on the error on the console it seemed we had duplicate REST API i.e., '/fetch' inside the our customer ms - that is inside the CustomerController and the CustomerQueryController. So delete or comment out the endpoint inside the CustomerController as we will no longer be using it. Since we had these 2 similar endpoints, the Spring boot application is throwing a start up error - So what we can do is, we can delete this entire CustomerController class along with the controller package that is holding it. Do the build and again try to start the customer ms in debug mode to validate if it will be successful this time around. Hurrey! this time it started successfully. Next,I am going to start the Gatewayserver application and once it is started, we can try to invoke the write and read APIs that are exposed from the customer ms. But before that, if you go to the axon server dashboard and refresh it, you can see that this time the customer ms icon is also connected to the axon server. If more number of ms's are connected are connected to the axon server, more number of icon illustrations will also be visible on the dashboard under the "Nodes and applications" canvas.
 * On the LHS nav, you can go to the "Monitoring" page to see the monitoring details. But the intrsting part that I want us to visualize is the "Search" option on the LHS nav. As of now, if you try to click on the search 🔍 icon on the far RHS end of the  "query" input without any input of course you will notice that as of now there are no events published/There is no data stored here in an Event Sourcing style. But when we start invoking the APIs you will start to see/visualize some data being stored here. Now, on the LHS nav, if you click on the "Commands" option, you should also be able to see that there are no record commands invoked. Same thing if you go to the "Queries" option. Now, inside the postman, invoke the create customer request. This gave me a successful response. Using the same mobile number invoke the fetch customer request to fetch the customer details. Copy the entire response body and paste it into the update customer request body - Here, I will update the name from "Colince Temoi" to "Colince Linus Temoi" and click on the send button. Next, I am going to do a second update but this time on the email input i.e., from "tutor@getttright" to "tutor1@getttright.com" and click the send button again. This means tha we will have done 2 update operations. Other than name and email, I can't update any other fields based upon the business logic that we have written. As a next step, I can go ahead and invoke the delete customer request - this we will do towards the end.  Now, if you go back to the dashboard and click on the query 🔍 button/icon by selecting the Query Time Window as last one hour, you will be able to see that their are 3 events published all having a similar "aggregateIdentifier"  which is nothing but the customer id. If you can copy the customer id from the update request or fetch response and then Ctrl + F and search it in the "Search" page, you will see that all the values of records in the aggregateIdentifier column are same, nothing but all those event records so far are related to the same customer and the evry first event has the aggregate sequesnce number as 0 followed by 1 and 2. All those event as can be seen in the column named "aggregate type", are handled by the same aggregate type which is CustomerAggregate that we have written behind the scenes.
 * Coming to the "Payload type" field/column the very first event that we have initiated is related to the "CustomerCreatedEvent" followed by "CustomerUpdatedEvent" - 2 times we have invoked. All this you can clearly visualize in the table on the "Search" page of Axon dashboard. All these records of events as we have discussed are stored in a sequence style. All these records of events as can be seen is nothing but our write DB where it is leveraging the Event Sourcing pattern. Here if you can click on the dropdown labelled "field columns", you can also select this "Payload Data" check box and with that you can also see a column in our records indicating/showing us what payload data that we have received as part of the API invocation from the end-user. Bruh! This is so beautiful and powerful as with this kind of data we can do a lot of great work and wonders inside our real production applications. Anytime if you have any production issues or any bugs, you can leverage all this data to reproduce and fix the issue. Bruh! you like have a time machine with you! how powerful. Now, back to the dropdown "field columns" you can now uncheck the "Payload Data" and yea, you can see all the other default selected columns/fields data. Each event record as can be seen is going to have an unique event identifier as generated by the Axon framework itself. With all this detailed discussion on the Event records fields as displayed on the "Search" page, you should be crisp clear on how the event data is being stored in the write DB using the Event Sourcing pattern. As of now, all those record events and data that we have seen being displayed, they are being inside the events and data folders respectively as we had configured earlier. But like we said, if required, your DevOps team members they can configure to store this data and events inside a MongoDb or any other NoSQL DBs. Next, on the LHS nav, if you go to the "Commands" section/page, you will be able to see the details on how many commands are triggered and processed by this Axon Server. So, total we have triggered 3 different commands, CreateCustomerCommand 1 time and UpdateCustomerCommand 2 times as can be visualized on this page. That's why the same 3 commands are being displayed here - so in total 3 commands are have been "dispatched"/fired, all these 3 commands have been "Handled" and under the "Distribution" tab on the same page,also you can see that all these 3 commands are distributed as events to the Query side as well.
 * Next, we can go the "Events" option on the LHS nav, and as of now you can see that there are no event records available because all those events are processed. Next, we can go the "Queries" option on the LHS nav. Since we have fired the fetch customer API, behind the scenes the find customer query will be triggered and the same are being monitored by the Axon. So, in summery you can be able to see that a total of 2 queries are executed and Errors are 0. So, this is one of the great advantage if you follow the technical flow that is recommended by the Axon framework as we discussed in details in our slides and docstrings and also implemented the same. Of course, you maybe tempted to read the data from the read DB directly from your read REST API itself without following the Axon framework recommended flow on the read side nothing but you don't wan to create the Query Object component, you don't want to create the Query handlers - you may want to read the data directly from the read APIs but that is not recommended like we have said because the Axon framework is going to provide lot many features out of the box if your follow what they recommend that you can easily leverage inside your real production applications. One such feature is around the monitoring capabilities that you are going to get as you can see here in your Axon dashboard pages. As a last step, we can try to invoke the delete customer API, and to invoke the same we need to customer Id that was returned during the fetch customer response. Copy the same and paste it as a request param in the delete customer request. Now if you click on the send button, you should be able to see a successful response. Now, if you try to invoke the fetch customer request one more time, you will notice that on the read DB the activeSw flag may have been update to false and that's why you will get an error saying that customer not found with the given input data mobile number. This is an indication that the syncing between the write DB and the Read DB is happening perfectly with the help of Axon Server provided event bus. Now, if you go back to the Axon server dashboard amd click on the Search option page and the click on the 🔍 icon/button, this time you will be able to see that there are 4 events and the last event that happened on this customer is related to the CustomerDeletedEvent. As we start creating more and more of data, you will be able to see lots of data under this Search page of the Axon dashboard. Axon server also supports querying of the data present inside this Event Sourcing DB/Search Page in the axon dashboard. As can bee seen, there is a "query" input where for example if you are looking to search for the records related to specific customer, what you can do is, you can try to type, "aggregateIdentifier" (Please make sure no spaces and you are following the same case sensitivity) followed by = then inside the double quotes mention the customerId/aggregateIdentifier value i.e., aggregateIdentifier="4e6b0b1a-bb2e-40a3-a654-af9334e032c7" and now if you click on the 🔍 button/icon this time you can see that in total there are 4 event that are happened matching to this aggregateIdentifier/customerId.
 * This way, you can always try to search specific customer records or specific account records/details buy sung this aggregateIdentifier or any other columns that you can visualize in the Search page/Write DB event records. Next, lets go to "Commands" option and this time you should be able to see that new command details are added and this us related to the DeleteCustomerCommand. Same thing under the "Queries" option, you can see that the find customer query is executed in total 3 times. With the demo that we have visualized end to end, you should now be crisp clear and I am also assuming that right now you are also crisp clear with the technical flow to implement CQRS amd ES patterns with the help of Axon project/products that we have implemented as illustrated in the slide. Throughout our sessions we are using Axon framework but if needed a company can build their own product or framework that is capable of implementing CQRS and ES patterns.
 *
 * Axon framework IntelliJ plugin.
 * -------------------------------
 *
 *
 *
 *  * * */


@SpringBootApplication
public class GatewayserverApplication {

    public static void main(String[] args) {
        SpringApplication.run(GatewayserverApplication.class, args);
    }

    @Bean
    public RouteLocator eazyBankRouteConfig(RouteLocatorBuilder routeLocatorBuilder) {
        return routeLocatorBuilder.routes()
                .route(p -> p
                        .path("/eazybank/customer/**")
                        .filters(f -> f.rewritePath("/eazybank/customer/(?<segment>.*)", "/${segment}")
                                .addResponseHeader("X-Response-Time", LocalDateTime.now().toString()))
                        .uri("lb://CUSTOMER"))
                .route(p -> p
                        .path("/eazybank/accounts/**")
                        .filters(f -> f.rewritePath("/eazybank/accounts/(?<segment>.*)", "/${segment}")
                                .addResponseHeader("X-Response-Time", LocalDateTime.now().toString()))
                        .uri("lb://ACCOUNTS"))
                .route(p -> p
                        .path("/eazybank/loans/**")
                        .filters(f -> f.rewritePath("/eazybank/loans/(?<segment>.*)", "/${segment}")
                                .addResponseHeader("X-Response-Time", LocalDateTime.now().toString()))
                        .uri("lb://LOANS"))
                .route(p -> p
                        .path("/eazybank/cards/**")
                        .filters(f -> f.rewritePath("/eazybank/cards/(?<segment>.*)", "/${segment}")
                                .addResponseHeader("X-Response-Time", LocalDateTime.now().toString()))
                        .uri("lb://CARDS")).build();


    }


}
