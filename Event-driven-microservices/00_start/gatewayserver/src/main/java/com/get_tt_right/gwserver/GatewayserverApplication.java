package com.get_tt_right.gwserver;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;
import org.springframework.cloud.gateway.route.RouteLocator;
import org.springframework.cloud.gateway.route.builder.RouteLocatorBuilder;
import org.springframework.context.annotation.Bean;

import java.time.LocalDateTime;

/** Update as of 01/7/2026
 * -------------------------
 * Transactional Outbox Pattern
 * -----------------------------
 * Check slide for details.
 *
 * Saga Pattern
 * ---------------
 * Check slide for details.
 *
 * Choreography Saga Pattern
 * -------------------------
 * Check slide for details.
 * Check the 00_end folder for the code and docstring on this pattern because inside the 00_start folder we have code implementation changes in regard to CQRS pattern with the help of Axon Framework. Since we want to see plain Saga pattern without any CQRS and Axon framework by using Spring projects alone, we will be using the code from the 00_end folder. You can open this from intellij for contD:
 *
 * Introduction to orchestration Saga Pattern.
 * --------------------------------------------
 * Check slide for details.
 *
 * Implementing Happy flow to Orchestration Saga Pattern.
 * --------------------------------------------------------------
 * In the pom.xml of the common project, add a new dependency which is related to the validations i.e., spring-boot-starter-validation. Reason: We are going to use some validations related annotations inside the common project/module. In the src folder, under the dto package, create a new dto class (MobileNumberUpdateDto) which is going to be used as part of the update mobile number transaction flow/request. Check this docstring for more details.
 * Now, create 2 new packages i.e., event and command inside the common src folder. Now, we will create some command and event classes which are going to help us inside oru saga pattern implementation. You can check out those respective classes for more detail.
 * The command classes that are going to take care of the update operation are: UpdateCusMobNumCommand, UpdateAccMobNumCommand, UpdateCardMobileNumCommand and UpdateLoanMobileNumCommand. Very similarly, we need the command classes which are going to take care of the rollback/compensation operation i.e.,RollbackCusMobNumCommand,RollbackAccntMobNumCommand and RollbackCardMobNumCommand. Next, do you think we need to create a command class for the loans ms? haha! there is no need because loans ms is the last ms that is going to be executed inside our Orchestration Saga pattern. Therefore, there is no need of executing a compensation txn separately because the loans ms for any RTEs, it can roll back the txns in its own DBs(Write/ES and Read) - that's why we are not going to create any roll back command class for the loans ms.
 * I hope you recall the CQRS pattern vividly - Everything is going to be triggered with the help of a command, process the commands in the aggregates >> store the data inside the write DB and then, we are going to trigger the events, then process the events and finally, we are going to store the data inside the reader DB. Once the command classes are created we need to create the associated event classes. I hope you remember that in the CQRS pattern, for every command class there should be an associated event class as well. That's what we have been following and implementing in all our ms's. For example, if you go and check the accounts ms, under the command package we have the commands: CreateAccountCommand, UpdateAccountCommand, DeleteAccountCommand and the associated event classes under the events package are: AccountCreatedEvent, AccountUpdatedEvent, AccountDeletedEvent.
 * Very similarly, we need to create the event classes for the command classes i.e., UpdateCusMobNumCommand, UpdateAccMobNumCommand, UpdateCardMobileNumCommand and UpdateLoanMobileNumCommand - during the happy path of the Saga and during the exception path/flow we need to create respective event classes for the compensation commands i.e., RollbackCusMobNumCommand,RollbackAccntMobNumCommand and RollbackCardMobNumCommand. So, under the event package of the common project create the event classes i.e., CusMobNumUpdatedEvent, AccntMobileNumUpdatedEvent, CardMobileNumUpdatedEvent and LoanMobileNumUpdatedEvent - for the happy path/flow of the Saga and CusMobNumRollbackedEvent, AccntMobNumRollbackedEvent and CardMobNumRollbackedEvent - for the exception path/flow of the Saga. You can check out these respective classes docstring for more details. We don't have to create the rollbacked event class for the loans ms because there is no associated command class - In addition, we will never be initiating the compensation txn on the loans ms manually because it is the last ms that is going to be executed inside the Saga pattern. With the changes we have done so far, you should now be crisp clear.
 * We have created all the required command classes and respective event classes inside the common project. As a next step, we need to create a REST API that is going to accept the request from the client application to update the mobile number inside all the ms's. To create this new REST API, we need to go to the customer ms because customer ms is the very 1st ms which is going to accept the request and hand over the responsibility of maintaining the saga pattern to the Saga Manger. Inside the customer ms, we have implemented all the REST APIs using the Command and Query style - this you clearly know! We have a controller i.e.,CustomerCommandController - here we have created all the REST APIs that are going to change/modify/alter the data. Since update mobile number is also going to modify the data, inside this same controller class we need to create a new REST API that is of type patch mapping with the path /mobile-number. Check this out for more details.
 *
 *  Implementing Compensation txns to Orchestration Saga Pattern.
 * ---------------------------------------------------------------
 * As of now, we have only been dispatching the Rollback commands but we have not handled them. We will be handling these rollback commands as well so that we are going to handle the negative scenarios also inside our Saga pattern. We can do this in the Reverse order. Nothing but, while the updates are happening on the loans ms, if there is a RTE then the logic to the command RollbackCardMobNumCommand is going to be executed. This command needs to be handled in the cards ms aggregate class. Check out its docstring for more details.
 *
 * Demo of Orchestration Saga Pattern.
 * ----------------------------------
 * Re-validate all the code that we have written so far to make sure everything is good. If any issues, we will come to learn of them during the demo. Now do a build (Ctrl + f9). Next follow the documentation "Testing your Event driven Ms's from scratch" to spin up your supporting components and your respective services. If all your services started successfully, you should be able to see the respective icons in the Axonserver dashboard. From the postman 1st create a new customer, account, card, and loan with the mobile number "3254537688". Next, you can try to invoke the fetchCustomerSummary request with the same mobile number to verify the data is coming as expected. This means all the required data inside all the ms's is created successfully. Next we can try to invoke the orchestration-mobile-number request that we have under the customer collection. Here we need to send the customerId, accountNumber, loanNumber, cardNumber,currentMobileNumber, and newMobileNumber. You can get the customerId value by invoking the fetch customer API. The other input values you can either get them from the fetchCustomerSummary request o/p or fetchProfile request output. I just thought out aloud that instead of getting all this input data from different requests, you could just make some adjustments to your MV to also return the customer id and with this using one request output you can pick all this required data to feed to the request orchestration-mobile-number inputs. But of course you will have to think about the business implications of that.
 * Now, before you click the send button - clear all the logs in your respective services consoles. I.e., Customer, Account, Cards, and finally Loans. On clciking the send button we happilly got a successful response - but we should never consider it as the overal status, we instead need to check the loggers properly to make sure everything is working fine. What we can 1st do is go to the customer service application logs, Inside these logs we can try to search logs with our Saga class name which is - UpdateMobileNumberSaga - make sure to match the whole word by clicking on the icon "W" on the RHS of the search input box. If you keep searching/scrolling, you should be able to see a log statement i.e., "Saga Event 1 [Start] : Received CusMobNumUpdatedEvent for customerId:..." followed by there is a log conveying "Saga Event 2 : Received AccntMobileNumUpdatedEvent for accountNumber:...", followed by there is a log statement conveying "Saga Event 3 : Received CardMobileNumUpdatedEvent for cardNumber:..." and towards the end we have a log statement "Saga Event 4 [END] : Received LoanMobileNumUpdatedEvent for loanNumber...". This means that the Saga manager executed all the Saga event handlers based upon the sequence that we have decided. Now, back to the postman we can try to invoke the Gatewayserver API - fetchCustomerSummary with the help of the new mobile number. As soon as I click on the send button, you can visualize we are getting the response with the new mobile number  all the services. Inside the axon dashboard also we can validate what events executed behind the scenes. If you click on the Search button on the LHS nav, you will be able to see all the events that have been triggered in the last 1 hr when you click on the 🔍 icon on the RHS of the Query input pox (But don't enter anything in it) - the only thing you need to make sure is selected is "last 1hr" in the "Query time window" input. Like this you should be able to see a tabulated result/output just below. You can add/select the "Payload data" input under the "filter columns" input as well to be able to see the same.
 * If you visualize, the last 4 events that are published are CusMobNumUpdatedEvent followed by AccntMobileNumUpdatedEvent then CardMobileNumUpdatedEvent and finally, LoanMobileNumUpdatedEvent. You shold also be able to see the associated payload data. Hurrey! this confirms that the happy path is working fine as expected. As a next step, we shou test the Negative path as well. To achieve this test case, what we are going to do is - Inside the LoanAggregate class - Since loans ms is the last service that is going to be executed, I I simulate a RTE from thhis service all the my roll back/ compensation txns should be executed and with that we should be in a pole position to confirm that our rollback code/compensation code is working fine or not. To do the simulation, inside the LoanAggregate class @CommandHandler method#handle that handles the command UpdateLoanMobileNumCommand, towards the end just after the line of code i.e., AggregateLifecycle.apply(loanMobileNumUpdatedEvent); I am going to throw an explicit exception of type RTE with the message "An error occurred in loans service while processing UpdateLoanMobileNumCommand". By the time this Command handler method for UpdateLoanMobileNumCommand command, the chnages will have already been committed in all the other three services i.e., customer, accounts and cards ms's. Both the read and write DB's. Since there is an exception happening inside the 4th ms, it is going to trigger the comphe compensation txn in the 3 services i.e., cards, accounts and finally customer ms. Since we wre throeing the RTE while handling the command UpdateLoanMobileNumCommand, what will happen is - inside the Saga manager class, UpdateMobileNumberSaga, that we have written, if you can scroll down at the place where we are trying to dispatc the command "UpdateLoanMobileNumCommand", here, there is a check to check whether there is an excpetion - if yes, we are trying to trigger the 1st compensation txn by using the command RollbackCardMobNumCommand. Now, lets hope for the best - save the changes in the LoanAggergate class, do a build and restart the loans service (Though all of this saving, build and restart will be done automatically by IntelliJ and dev tools as the changes we have done asre small".
 * Now, inside the postman for the request orchestration-mobile-number, this time give the new mobile number as "3254537624". On clicking the send button you should get a successful response. Now, let us visualize what happened behind the scenes. If I go to my Axon dashboard and try to search for the latest events you will be able to see that 1st CusMobNumUpdatedEvent is processed, followed by AccntMobileNumUpdatedEvent, then CardMobileNumUpdatedEvent, but during the loans ms, since we got a RTE, the LoanMobileNumUpdatedEvent never got dispatched and ence never processed. After this CardMobileNumUpdatedEvent, the next immediate event that get's dispatched is CardMobNumRollbackedEvent, followed by AccntMobNumRollbackedEvent and finally CusMobNumRollbackedEvent. Now, if you go to the gatewyaserver api and try to invoke the request fetchCustomerSummary with the old mobile number i.e., "3254537623", we should get a successful response. As can be visualized, all the services are still holding the same old mobile num which confirms that the negative scenario is also working as expected based upon our compensation logic implementation. Hurrey! you shoud be very happy with the progress that we have made as everything is working as expected as has been visualized in the demo. Now you can go ahead and comment out the explicit RTE that we introduced for testing the negative scenario. Do a build and since no CE's we should be good!
 *
 * Demo of Subscription Queries.
 * ------------------------------
 * As of now, when we invoke the API /mobile-number, we are getting the successful message as a response immediately regardless of the outcome of the Saga pattern. Instead, we want to enhance our code to provide us the overall status of the Saga Pattern on whether the Saga pattern execution is successful or if any exceptions occurred. With this enhancement, the client application will keep waiting for the overall status until either the Saga pattern is completely successful or completely rolled back, then the status will be sent back to the client application. There is a very easy way to achieve this inside axon framework. If you go to the axon website, under the developers tab >> documentation >> Reference guide (You will see a good comprehensive documentation around the axon framework). Right now we are looking for the documentation around "Queries" - so, click on that in the LHS nav >> Click "Query dispatchers/Dispatching" you will be able to see that axon framework supports 4 different kinds of queries. 1. Point-to-point queries - This is a normal query which we have been implementing/using/leveraging in our previous sessions. If you open any of the query controller classes like AccountsQueryController, there you will see we are trying to invoke the query method using the QueryGateway. This is in turn going to invoke the query handler logic asynchronously and when the response is ready, it is going to dispatch/send it in the form of AccountsDto. Since we want to wait for the response from the query handler we are using the join method. This join method will force my completable future to wait for the result to come and assign to the variable account. i.e., AccountsDto account = queryGateway.query(findAccountQuery, ResponseTypes.instanceOf(AccountsDto.class)).join(); So, this is a normal point to point query which is going to help you to query a single record. 2. Scatter-gather queries - If you click on it, it should take you to the documentation. This queries we need to use when we want responses from all the query handlers matching your query message. In case if you have multiple query handlers for a query message and if you want the responses from all such query handlers then you can use this Scatter-gather queries. The name itself is self-explanatory, while querying we are going to query multiple query handlers (That's wht Scatter stands for) whereas the responses we are going to gather as a single response (That's wht the gather word indicates). 3. Subscription queries - This is what we are going to use in pour scenario. The subscription query allows a client to get the initial state of the model it wants to query, and to stay up-to-date as the queried view model changes. What this means is - when we use this subscription queries, we are going to get an ability to wait for the results that are going to be emitted by the business logic. We can get an initial state response - followed by we can keep getting the updated responses as well until this subscription query is completely executed. To implement the Subscription queries we need to use the QueryUpdateEmitter. We will see the demo in the next few. 4. Streaming queries - This is the last query type is Streaming Queries. The streaming query allows a client to, for example, stream large database result sets. he streaming query relies on the reactive stream model, specifically the Publisher type. Instead of loading and handling all the thousands of records at a time with the help of this Streaming queries we can try to stream the query results just like how stream the videos from the YouTube. When we open a Video inside YouTube, it's going to stream the video based upon where the cursor is present. Very similarly, this Streaming Queries is going to work.
 * For now we want to use the Subscription Query and implement the enhancement inside our Saga pattern. 1st I need to go to the place where we are starting the Saga pattern flow which is CustomerCommandController where we have the API /mobile-number. Here before we try to write the logic 1st lets create a Query class - FindUpdateMobileSagaQuery - under the customer ms query package. As of now, we only hand a single query class i.e., FindCustomerQuery. So, just copy and paste the same and rename the copy to FindUpdateMobileSagaQuery. Check out this class for more details. Next, inside the CustomerCommandController in the API /mobile-number just before dispatching the command i.e., just before the line of code, commandGateway.sendAndWait(updateCusMobNumCommand); What we will do is, we will try to invoke a query but to invoke a query we need to inject QueryGateway dependency in this class. With the help of this QueryGateway, I am going to invoke the method#subscriptionQuery. Check out CustomerCommandController class API /mobile-number for more details.
 *
 * Testing the Happy and negative flow to ascertain that the subscription query results are loaded properly.
 * ----------------------------------------------------------------------------------------------------------
 * Follow the documentation "Testing your Event driven Ms's from scratch" to spin up your supporting components and services in order to perform the tests. Create a customer, accounts,loan and card for the mobile number 3254537688. Now invoke the fetchCustomerSummery gateway request by passing the mobile number 3254537688 - you should get the expected successful response. As a next step, open the orchestration-mobile-number request and populate the necessary inputs. As soon as you click on the send button, I am getting an error with "errorMessage": "CANCELLED: AXONIQ-5000". To check if the update operation is successful or not I am taking the new mobile number and trying to invoke the fetch customer summery. It didn't return the expected successful response as well. But for the old mobile number the response was coming as expected meaning the update didn't go through. huuh!😢. Based upon my analysis the problem is arising becuase w are trying to use a query class which does not have any fields i.e., FindUpdateMobileSagaQuery while writing the subscription query. Not sure if this is a bug with Axon IQ or if it is intentional. To fix this issue, instead of a marker class FindUpdateMobileSagaQuery, we will try to use some other query class i.e., FindCustomerQuery - this has a field with the name mobileNunber. And for this query class, if you click on the Axon orange icon on the LHS of it you will be able to see we have query handlers registered. I am assuming this should fix the problem. In the controller class where we are writing the subscription query, in the line of code try(SubscriptionQueryResult<ResponseDto,ResponseDto> queryResult = queryGateway.subscriptionQuery(new FindUpdateMobileSagaQuery(), ResponseTypes.instanceOf(ResponseDto.class),ResponseTypes.instanceOf(ResponseDto.class))) {...}, for the method#subscriptionQuery 1st param, instead of passing the marker class instance i.e., new FindUpdateMobileSagaQuery() pass the object of FindCustomerQuery and since the object requires some mobile number, pass/populate the constructor mobile number input from the dto class that we have received i.e., new FindCustomerQuery(mobileNumberUpdateDto.getNewMobileNumber()). Since we are using the FindCustomer query object while writing the subscription query, we need to make similar changes inside the Saga manager class as well in the end saga methods method#emit 1st parameter from FindUpdateMobileSagaQuery.class to FindCustomerQuery.class.
 * Once I made these changes I can do the saving, building and spinning of the supporting components and services for testing again to see if we are going to achieve our goal for the Happy and negative flow to ascertain that the subscription query results are loaded properly. Now, from the postman I will try to send the orchestration-mobile-number request one more time. Hurreey! this time we are getting teh overall message which is "Mobile Number updation successful in all services". If I go ahead and now invoke the fetchCustomerSummery request with the help of the new mobile number 3254537624, I should get the expected successful response. Now lets test the negative scenario as well, I can go ahead and uncomment the explicit RTE that we introduced in the loan service aggregate class. That is in the CommandHandler method#handle of UpdateLoanMobileNumCommand. Once the build is completed and the restart is done - which is automatic for intelliJ I can go ahead and invoke the API orchestration-mobile-number this time swapping the current and the new mobile number inputs or you can use any value for the new mobile number this time around - no biggie! Now, if I click on the send button you can see that now I am getting a message saying that "Mobile Number updation failed in all services". Hureey! This confirms that the negative scenario is also working fine. Now we can go ahead and try to invoke the fetchCustomerSummery request with this new mobile number and we should get any successful response becuase the trnsactions are rolled back. If you g o ahead and invoke with the old mobile number it should give you the expected successful response. That's it for the happy and negative flow to ascertain that the subscription query results are loaded properly. With this you should now be crisp clear on ow to use the subscription queries to tell the overall status of the saga txn flow. Before we close this session remember to comment the explict RTE that we introduced in the loan service aggregate class.
 *
 * What happens in ES when a new event is being stored
 * ----------------------------------------------------
 * Check slides for more details.
 * Now, let us prove all the points that we have discussed in the slides. What we are going to do is - we are going to keep some breakpoints inside the aggregator class and using these breakpoints we are going to visualize the demo on how the replay of all the events are happening inside the aggregator class.
 * Spin up all your supporting components and ms's as documented in "Testing your Event driven Ms's from scratch". 1st lets try to create some data iside one of the ms's i.e., customer ms. We will create a new customer. You can then try to load the customer details by invoking the fetch customer API. Next, invoke the update customer this time mentioning the name as "Colince Linus Temoi". Now inside the Axon server if you go to the search nav on the LHS nav and try to click on the 🔍 icon on the RHS of the "query" input with the query time window input set to the last 1hr - for the new customer that we have created you can be able to visualize that there are 4 events stored inside this Event Storage system. That's why you will be able to see we have aggregate sequence numbers starting from 0 to 3. The very 1st event is of type CustomerCreatedEvent followed by CustomerDataChangedEvent then CustomerUpdatedEvent and finally CustomerDataChangedEvent. Now what we will do is - we will go to the aggregator class of customer ms i.e., CustomerAggregate. Here we have defined all the logic of Event Sourcing inside the methods that are annotated with the @EventSourcingHandler. I am going to keep break points inside CustomerCreatedEvent method#on at the first line of code, followed by I am going to keep another breakpoint inside the method#on of CustomerDataChangedEvent (This is not handled inside the Customer ms for Event Sourcing - it is handled inside the Profile ms for MV impl and projection purposes), so, you can ignore that. Next put a break point in the method#on of CustomerUpdatedEvent.  In both of CustomerCreatedEvent and CustomerUpdatedEvent Event Sourcing Handlers whenever the thread stops at the breakpoints we have introduced we also want to log debugging statements inside our customer ms console. To do this, we can follow the very basic approach which is with the help of log statements or System.out.pln. But because we are growing, lets learn of one more other better and efficient approach 😂. Inside IntelliJ, you can right-click on a break-point and a pop-up will appear >> Click more >> Check ✅ the option evaluate and log. In the Input, mention the object name/variable which is "customerUpdatedEvent". So, whenever my breakpoint is being stopped this object details will be printed onto the console. Very similarly, in the same pop-up on the LHS you can select the next breakpoint which we have put in the other method#on that handles CustomerCreatedEvent and do the same thing. This time the object that we will be evaluating and logging is "customerCreatedEvent". Click on Done to close the pop-up. Now, I will go to the Customer ms console and clean the existing logs, followed by inside my postman, I am going to invoke the update operation one more time with the email being tutor1@getttright. Now, when I click on the send button, ideally behind the scenes in a new event of UpdateCustomerEvent needs to be triggered.😂😂 But like we said, the aggregator 1st will process the saved create Event i.e., CustomerCreatedEvent, then followed by the saved CustomerUpdatedEvent and the at last this new event that is now being dispatched i.e., CustomerUpdatedEvent is going to be stored. Very interesting!!
 * If you now click on the send button you can see that 1st the break point stops in the method#on where we are trying to handle the CustomerCreatedEvent - in the customer ms console you should also be able to see the log statement of that old event. If you resume/release the breakpoint, you will see the execution now stops on the 2nd breakpoint method#on that handles the CustomerUpdatedEvent and here, if you visualize the data inside that object we have the old CustomerUpdatedEvent object data as can be clearly verified by the email data i.e., tutor@getttright. Now, if you again release/resume the execution you will now finally visualize the new data related to the 3rd event i.e., CustomerUpdatedEvent that we are now trying to dispatch/publish as can be noted in the email data which now has tutor1@getttright. Now, if you resume the execution everything should have now been executed as expected (Even though if you visist the postman response you will see some error i.e., "Connection lost while executing command on: 20800@DESKTOP-CR6ONMF" - the reason is simple, we keep the breakpoint for too long time, but what matters is the execution completed as expected as can be verified in the Axon dashboard/ES DB we have 2 more new events created i.e., CustomerUpdatedEvent (This new event data if you check it is going to have the email value tutor1@getttright) and CustomerDataChangedEvent respectively) and this time you are having crisp clarity of what happens behind the scenes. With this, right now you should be satisfied from the demo visualization concering the discussion we have had previously. This should now make you confident with what we have discussed so far. You can try the visualization demo one more time this time sending the update request with the email as tutor2@getttright. Like this now, the breapoint is now going to stop total 4 time (Once in the CustomerCreatedEvent method#on and 3 times in the CustomerUpdatedEvent method#on where we 1st has the name being updated from "Colince Temoi" to "Colince Linus Temoi", 2nd where we had the email being updated from "tutor@getttright" to "tutor1@getttright" and 3rd where we are now having the email being updated from "tutor1@getttright" to "tutor2@getttright". Same drill! no rocket science. That how things go on here! With all we have discussed and visualized now, you should be crisp clear and confident with the replay functionality. Next, we will try to understand what are Snapshots and how they are going to save us from the performance problems that we have just discussed as a result of the replay functionality. Also, how to define them in our ms's.
 *
 * SnapShots in Event-Sourcing Pattern - Theory
 * ------------------------------------------------
 * Check slides for more details.
 *
 * Implementation and Demo of Snapshots
 * --------------------------------------
 * Every product or framework that supports Event Sourcing pattern should also support the snapshots functionality to overcome the performance challange in the scenario of high volume of events. As of now, inside our ms's we are leveraging the Axon framework to implemnt the Event Sourcing pattern. This Axon framework supports the snapshots functionality. If you go to the documentation of Axon framework, under tuning, you should be able to see something to do with snapshots. Under the tuning, there is a section highlighting about the Event Snapshots - inside that page all the details about snapshots are highlighted, if interested you can read that entire page. We will see a small demo on how to create these snapshots and leverage them inside our ms's. So to implement this, 1st we need to create a bean inside our respective ms's main class. This bean should be of type SnapshotTriggerDefinition. This SnapshotTriggerDefinition is an interface inside Axon framework which is going to let you define on how the snapshots are going to be created/triggered. If you look for the implementations of this interface - on the LHS of the interface, you can click on the fn icon to see them - you will see there are 3 impl classes as of now. i.e., NoSnapshotTriggerDefinition - So, if you are looking for an option where at any case you don't want to create snapshot events in the future, then in such scenarios you can leverage this implementation class. But if you are looking for an option to configure Snapshots, you have 2 alternative implementation classes. 1. EventCountSnapshotTriggerDefinition - Use this based on the number of events/event count to create a Snapshot. For example, you can configure a bean with the help of this class by conveying that for every 100 events I want to create/trigger a snapshot. Such configurations we can make with the help of this class. 2. AggregateLoadTimeSnapshotTriggerDefinition - We know that the aggregator is going to load and replay all the previous events. If this entire replay operation is going to be taking more than a certain amount of time, then you can consider that as a triggering point to create a snapshot. If you open these respective implementation classes you should be able to see and read through the documentations provided. Inside our ms's we can use the EventCountSnapshotTriggerDefinition class.
 * Since we are looking to implement the Snapshots inside the Customer ms, 1st I need to open the CustomersApplication which is the Spring Boot main class - here we will create a new bean(That's why we are mentioning the @Bean annotation on top of a method#customerSnapshotTrigger). The method is going to return a bean of type SnapshotTriggerDefinition. To this method, I need to accept SnapShotter as an input. Check out this method docstring for more details.
 * Now, you can save the changes >> do the build. Once the build is complete, spin up the supporting components and you services by following the documentation "Testing your Event driven Ms's from scratch". Now, let's try to create new events for a new customer. Inside postman, I am going to create a new customer. Using the same mobile number I am going to fetch the customer details. Copy the response and paste that in the body of the update request. Here we will try to update the name of the customer. This update operation is the 2nd event.  If you now go and check the Axon dashboard, as of now you will be able to see that there are 4 events(With the same aggregate Identifier) that have been triggered for the customer that we have created. If you check the snapshots tab you will see that a new snapshot event/record has been created behind the scenes with the same aggregate identifier. So, under the events section we can see 4 events,but behind the scenes on what we can see in the snapshots section, a 5th event (The snapshot - actually it came at number 4) has been created. So, total we have 5 events now. Now, it is expected, when we fire another event i.e., another update, we expect to see 3 more events coming. CustomerUpdatedEvent at number 6, A snapshot at number 7,  and a CustomerDataChangedEvent at number 8. So total we will now be having 8 events. For the 1st snapshot the token field/value will be 0 and for the 2nd snapshot the token will be 1 and so on. With this you should now be crisp clear. You can increase the count configuration to something like 6 to see this even more better. Now, let test/visualize the replay functionality. I will keep the breakpoint in the CustomerAggregate class method#on for CustomerCreatedEvent and CustomerUpdated event. If you don't have the snapshots concept all the events from the 1st one are going to be replayed, but in this scenario, since we have a snapshot, the method#on of CustomerCreated event should never be invoked, only the latest snapshot need to be loaded and after the snapshot, if there are some other event entries, those events including the latest snapshot will be considered fro the replay. Now, lete fire another update operation by updating the email. If you click on the send button you will be able to see that directly the breakpoint stops inside the CustomerUpdatedEvent method#on. If I release the breakpoint / resume the execution, the execution will complete. Reason: Behind the scenes the framework loaded the current state value from the slatest snapshot and after that snapshot, we only had 1 event available inside the event store. That's why the breakpoint stopped only once in the method#on of CustomerUpdated event while trying to store the new event details.
 * This time if I try to invoke the update operation one more time , the breakpoint is going to stop 2 times in the method#on of CustomerUpdatedEvent. If it stops more number of times i.e., 3 times when you are expecting it to stop 2 times ideally - simply means that a new snapshot is being created behind the scenes as we have reached the configured event count. This tell us that all the events from the previous latest snapshot including it + the current new snapshot that is now being created they are going to be replayed by the framework behind the scenes to calculate the new snapshot event. That's why sometimes you will see the breakpoint stopping +1 the expected count of time it should stop. But this is only unique for this scenario explained. Otherwise if it is a normal event, the brekpoint will stop based upon the number of events that you have after the snapshot. For example, after changing the count configuration to 6, right now in my Axon dashboard I have 5 events from my latest snapshot, so If I fire another update request, since a new snapshot is going to be created, I expect to have the breakpoint stopping 2 time + 1 for this scenario. With the demo that we have visualized, you should now be crisp clear. You can now go ahead and implement the snapshots relad configurations in the remaining ms's as well. Once done, debug the same way we have been doing.
 *
 * And it's a wrap matters Event Driven Ms's. You should be proud of the progress you have made so far. But as always our thumb rule, always treat everything that we have learnt as a stepping stone. Now you can go and do wonders in the industry! hurreey!
 * * * */


@SpringBootApplication
public class GatewayserverApplication {

    public static void main(String[] args) {
        SpringApplication.run(GatewayserverApplication.class, args);
    }

    @Bean
    public RouteLocator eazyBankRouteConfig(RouteLocatorBuilder routeLocatorBuilder) {
        return routeLocatorBuilder.routes()
                .route(p -> p
                        .path("/eazybank/customer/**")
                        .filters(f -> f.rewritePath("/eazybank/customer/(?<segment>.*)", "/${segment}")
                                .addResponseHeader("X-Response-Time", LocalDateTime.now().toString()))
                        .uri("lb://CUSTOMER"))
                .route(p -> p
                        .path("/eazybank/accounts/**")
                        .filters(f -> f.rewritePath("/eazybank/accounts/(?<segment>.*)", "/${segment}")
                                .addResponseHeader("X-Response-Time", LocalDateTime.now().toString()))
                        .uri("lb://ACCOUNTS"))
                .route(p -> p
                        .path("/eazybank/loans/**")
                        .filters(f -> f.rewritePath("/eazybank/loans/(?<segment>.*)", "/${segment}")
                                .addResponseHeader("X-Response-Time", LocalDateTime.now().toString()))
                        .uri("lb://LOANS"))
                .route(p -> p
                        .path("/eazybank/cards/**")
                        .filters(f -> f.rewritePath("/eazybank/cards/(?<segment>.*)", "/${segment}")
                                .addResponseHeader("X-Response-Time", LocalDateTime.now().toString()))
                        .uri("lb://CARDS"))
                .route(p -> p
                        .path("/eazybank/profile/**")
                        .filters(f -> f.rewritePath("/eazybank/profile/(?<segment>.*)", "/${segment}")
                                .addResponseHeader("X-Response-Time", LocalDateTime.now().toString()))
                        .uri("lb://PROFILE")).build();


    }


}
